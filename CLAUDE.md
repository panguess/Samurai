# Samurai Project Context

## ⚠️ Repo นี้มี 2 แอปแยกกันโดยสิ้นเชิง — ห้ามข้ามแตะกันเว้นแต่ผู้ใช้สั่งชัดเจน

| | แอปเช็คสต๊อก | แอปสั่งของ (order web app) |
|---|---|---|
| ไฟล์หน้าเว็บ | `stock-check.html` | `index.html` |
| ไฟล์ backend | `Code.gs` | `order-app-Code.gs` (เก็บใน repo ตั้งแต่ 8 ก.ย. 69 — ดูคำเตือนเรื่อง sync ด้านล่าง) |
| ใช้โดย | ลูกจ้างเช็คสต๊อก + เจ้าของสั่งของกับซัพพลายเออร์ | ลูกค้าสั่งซื้อสินค้า + แอดมินจัดการออเดอร์/จัดส่ง/ชำระเงิน |
| Google Sheet | คนละไฟล์สเปรดชีตกับแอปสั่งของ | คนละไฟล์สเปรดชีตกับแอปเช็คสต๊อก |

**กติกา**: ทำงานกับแอปไหน ห้ามอ่าน/แก้/อ้างอิงไฟล์ของอีกแอปโดยไม่ถูกขอ แม้จะดู "เกี่ยวข้องกัน" ก็ตาม
(ทั้งสองแอปเคยถูกโยงกันผิดพลาดมาแล้ว — ผู้ใช้ต้องเตือนหลายรอบ) ถ้าไม่แน่ใจว่ากำลังคุยเรื่องแอปไหน ให้ถามก่อน
ไม่ต้องเดา

## จบ session ทุกครั้ง ต้องสร้าง session ถัดไปต่อเองทันที — ห้ามรอให้ผู้ใช้สั่ง

ทำแบบนี้กันมาทุก session จริงๆ แต่ไม่เคยเขียนไว้เป็นลายลักษณ์อักษรมาก่อน (เพิ่งโดนผู้ใช้ทักเพราะต้องขอ 2 รอบ
กว่าจะสร้างให้) — Claude **ไม่มีความจำข้าม session เลย** สิ่งที่ไม่ได้เขียนไว้ในไฟล์นี้ถือว่าไม่เคยรู้มาก่อน
ดังนั้นทุกครั้งที่งานของ session หนึ่งจบลง (context ใกล้เต็ม/จบวัน/ผู้ใช้บอกให้พัก) ให้ทำตามลำดับนี้ **เอง
ทันทีโดยไม่ต้องรอให้ผู้ใช้สั่ง**:
1. อัปเดตไฟล์นี้ (`CLAUDE.md`) เพิ่ม section ใหม่ `### สถานะล่าสุด (อัปเดต [วันที่ พ.ศ.]) — [หัวข้อสรุปสั้นๆ]`
   ใต้แอปที่เพิ่งทำงาน (ก่อน section ของอีกแอป) สรุปสิ่งที่ทำ/บั๊กที่เจอ/งานค้าง ให้ session ถัดไปอ่านแล้วต่อได้
   ทันทีโดยไม่ต้องถามซ้ำ
2. ขอ commit + push ตามปกติ (ต้องรอผู้ใช้อนุญาตก่อนเหมือนเดิม — ข้อนี้ไม่เปลี่ยน ห้าม push เอง) รอจน push
   สำเร็จแล้วค่อยทำข้อ 3 (session ใหม่ต้องเห็นงานล่าสุดที่ push ไปแล้ว ไม่งั้นจะเช็คเอาท์โค้ดเก่า)
3. **สร้าง session ใหม่ต่อทันทีด้วย `mcp__Claude_Code_Remote__create_session` โดยไม่ต้องรอให้ผู้ใช้สั่ง** —
   - `title`: ตาม pattern `<ชื่อแอปภาษาอังกฤษสั้นๆ> DDMMYYYY` เสมอ (เช่น `Stock check 10092026`,
     `Order app 08092026`) วันที่ = วันที่วันนี้ — ถ้าไม่แน่ใจ pattern ให้เรียก
     `mcp__Claude_Code_Remote__list_sessions` (`mine:true`) ดูชื่อ session เก่าเป็นตัวอย่างก่อน
   - โฟกัสเฉพาะแอปที่เพิ่งทำงานด้วยเท่านั้น ห้ามปนอีกแอปเข้าไปใน session ใหม่ (ตามกติกาแยก 2 แอปด้านบน)
   - `source_url`/`source_revision` ชี้ไปที่ `main` (จุดที่เพิ่ง push ไปในข้อ 2)
   - `prompt`: สรุปสั้นๆ ให้อ่าน CLAUDE.md หัวข้อที่เพิ่งเขียนในข้อ 1 + งานค้างสำคัญที่สุด ปิดท้ายด้วย
     "รอรับคำสั่งจากผู้ใช้ต่อได้เลย ไม่ต้องเริ่มทำอะไรเองจนกว่าจะมีข้อความมา"
4. แจ้งผู้ใช้ว่าสร้าง session ใหม่ชื่ออะไรแล้ว พร้อม session ID

## แอปเช็คสต๊อก (stock-check.html + Code.gs) — บริบทที่ต้องรู้ก่อนแก้

### Deploy `Code.gs` ต้องแปะเองเสมอ
`git push` **ไม่มีทาง** deploy `Code.gs` ให้ — มันรันอยู่ใน Google Apps Script Editor คนละที่กับ repo
ทุกครั้งที่แก้ `Code.gs` ต้องส่งไฟล์เต็มให้ผู้ใช้ผ่าน SendUserFile เพื่อให้เขาก็อปไปแปะใน Apps Script Editor แล้วกด Save/Deploy เอง —
ห้ามบอกว่า "push แล้วเสร็จ" เฉยๆ ถ้าไฟล์ที่แก้คือ `Code.gs`

### กติกาการทำงานที่ผู้ใช้กำหนดไว้ (ยึดตลอด ไม่ใช่แค่ session เดียว)
1. แก้ `Code.gs` เมื่อไหร่ → ส่งไฟล์เต็มทาง SendUserFile ทุกครั้ง (ตามข้อบนสุด)
2. ห้าม push ขึ้น GitHub โดยไม่ถูกขอ ต้องรอให้ผู้ใช้พิมพ์บอกให้ push ก่อนเสมอ แม้จะเพิ่งแก้เสร็จก็ตาม
3. การเปลี่ยนแปลงที่กระทบ UI/หน้าตา หรือมีหลายตัวเลือกให้เลือก → ทำ demo เป็น Artifact ให้ดูก่อน แล้วค่อย implement ในโค้ดจริง (หรือรอผู้ใช้เลือกตัวเลือกก่อน)
4. ก่อนบอกว่า "เช็คแล้วไม่มีบั๊ก" ต้องตรวจสอบอย่างจริงจัง (syntax check, เทส E2E จริงด้วย Playwright ที่ mock API, ไล่ดู edge case) ไม่ใช่อ่านโค้ดผ่านๆ แล้วสรุปว่าโอเค
5. **ก่อนแก้/ส่ง `BillCapture-Code.gs` ทุกครั้ง ต้องเช็คก่อนว่าไฟล์ในเครื่อง/repo มีค่าที่ใช้งานจริงอยู่ครบ
   โดยเฉพาะ `SPREADSHEET_ID` (บรรทัด ~37)** — เกิดเหตุจริง 10 ก.ย. 69: repo เก็บไฟล์ template ที่ `SPREADSHEET_ID`
   ยังเป็น placeholder (`PASTE_YOUR_SPREADSHEET_ID_HERE`) เพราะผู้ใช้กรอก ID จริงตรงในหน้า Apps Script Editor
   เองตอนตั้งโปรเจกต์ครั้งแรก ไม่เคยซิงก์ค่านั้นกลับมาไว้ใน repo แก้ฟีเจอร์อื่น (เพิ่มแคช `checkDuplicateBill`)
   จากไฟล์ repo แล้วส่งไปแปะทับ ทำให้ deployment ที่ใช้งานได้อยู่แล้วพังกลับไปเป็น placeholder อีกรอบ ต้องไล่แก้
   3 รอบกว่าจะจบ (ทับ placeholder → พิมพ์ ID จากอ่านสกรีนช็อตแต่อ่านตัว `l`/`1` ผิด → ค่อยได้ ID ถูกจากผู้ใช้
   copy มาเอง) **ห้ามพิมพ์/transcribe ค่าพวกนี้จากสกรีนช็อตเองเด็ดขาด** ถ้าจำเป็นต้องรู้ค่าจริง (Spreadsheet ID,
   Folder ID ฯลฯ) ให้ขอให้ผู้ใช้ copy-paste มาให้ตรงๆ เสมอ และก่อนส่งไฟล์ `.gs` ที่แก้แล้วทุกครั้ง ให้ `grep` เช็ค
   ว่าไม่มี placeholder ตกค้าง (`PASTE_YOUR_`) หลุดออกไปโดยไม่ตั้งใจ

### Permission model: `PRIVILEGED_STAFF_NAME = 'Mile'`
ฟีเจอร์ฝั่งลูกจ้างบางอย่าง (แก้ชื่อหน่วยนับ, เพิ่มสินค้าใหม่) ถูกจำกัดให้ทำได้เฉพาะพนักงานที่ชื่อ "Mile" เท่านั้น —
ฝั่งลูกจ้างต้องส่ง `staffName` มาด้วยเสมอ และ backend เช็คว่าต้องตรงกับ `PRIVILEGED_STAFF_NAME` เป๊ะๆ

ฝั่งเจ้าของ (owner) ไม่มี concept "staffName" เลย — auth ของฝั่งเจ้าของคือ PIN screen (`renderOwnerPin`/`checkPin`) ที่กันแค่การเข้าหน้า SPA เท่านั้น
ฟังก์ชัน backend ที่เจ้าของเรียกได้ (เช่น `setSupplierCoverPhoto`, `createOrderBatch`, `setSupplierOrderConfirm`, `setProductSkipDate`, และตอนนี้รวม `setUnitLabel` เมื่อเรียกแบบไม่มี `staffName`) ไม่มีการเช็คสิทธิ์ฝั่ง server เพิ่มเติม — เป็น pattern เดิมของทั้งแอป ไม่ใช่ช่องโหว่ที่พึ่งเกิด
ดังนั้นถ้าจะเปิดฟีเจอร์ไหนให้เจ้าของใช้ได้ด้วย ให้ยึด pattern นี้: เช็คเฉพาะเมื่อมี `staffName` ส่งมา (`if (body.staffName && body.staffName !== PRIVILEGED_STAFF_NAME) throw ...`) อย่าบังคับให้ฝั่งเจ้าของต้องส่ง staffName ปลอมๆ

### Helper ป้องกันบั๊กที่ห้ามลบ/ห้าม bypass
`Code.gs` มี helper 3 ตัวที่แก้บั๊กจริงที่เคยเกิดจาก Google Sheets แปลงชนิดข้อมูลเองแบบเงียบๆ (auto-type เป็น Date/Number เวลาเจ้าของพิมพ์มือ) — ถ้าจะแก้โค้ดส่วนที่เกี่ยวข้อง ต้องใช้ helper เหล่านี้ต่อ ไม่ใช่เขียนโค้ดอ่านค่าตรงๆ แบบเดิม:
- `isActiveFlag(v)` — เช็คคอลัมน์ Active/boolean-like จาก Sheet อย่างถูกต้อง (กัน bug ที่ string `"FALSE"`/`"0"` เป็น truthy ใน JS)
- `normalizeSkipDatesCell(raw)` — แปลงค่าคอลัมน์ SkipDates ให้เป็น array ของ string `yyyy-MM-dd` เสมอ ไม่ว่า cell จะถูก Sheets แปลงเป็น native Date object หรือพิมพ์เป็น string ก็ตาม ใช้ทั้งตอนอ่าน (`bootstrap`) และตอนเขียน (`setProductSkipDate`) เพื่อให้ข้อมูลเก่าที่พังซ่อมตัวเองได้ทุกครั้งที่ถูกแตะ (self-healing) — ห้ามย้อนกลับไปใช้ `String(dateObj)` ตรงๆ เด็ดขาด เพราะจะได้ format แบบ `"Tue Sep 01 2026 07:00:00 GMT+0700 (เวลาอินโดจีน)"` ที่ผิด
- ใน `checkPin(pin)` — การ pad PIN ด้วย `.padStart(4, '0')` เมื่อ Sheet เก็บ PIN เป็น number (กัน PIN ที่ขึ้นต้นด้วย 0 หายไป)

### สถานะล่าสุด (อัปเดต 5 ก.ย. 69) — แยกป้ายหน่วย + performance ตอนโหลด/บันทึก

**แยกป้ายหน่วยเช็คสต๊อกออกจากป้ายหน่วยสั่งของเสร็จแล้ว** — ตอนนี้มี 2 ฝั่ง 3 จุด:
1. หน้าเช็คของฝั่งลูกจ้าง (หน่วยเล็ก+ใหญ่ ใช้ฟิลด์ `label` / คอลัมน์ `UnitLabel`) แก้ได้เฉพาะ Mile
2. หน้าสั่งของฝั่งเจ้าของ ฝั่งซ้าย "คงเหลือ" — **mirror จุดที่ 1 ตรงๆ** (อ่าน `u.label` เหมือนกัน) แก้จุด 1 กระทบจุดนี้ด้วยเสมอ ตั้งใจให้เป็นแบบนั้น ไม่ใช่บั๊ก
3. หน้าสั่งของฝั่งเจ้าของ ฝั่งขวา (หน่วยที่จะสั่ง supplier) — ใช้ฟิลด์แยก `orderLabel` / คอลัมน์ `OrderLabel` แก้ผ่าน action `setOrderUnitLabel` ไม่กระทบจุด 1/2 เลย

⚠️ **ถ้าสินค้าตัวไหนยังไม่เคยกดตั้งชื่อฝั่งสั่งของเองสักครั้ง `OrderLabel` จะว่าง แล้ว `bootstrap()` จะ fallback ไปยืม `UnitLabel` มาโชว์แทน** — เจอบั๊กจริงจากจุดนี้เมื่อ 4 ก.ย. 69 (แก้ป้ายฝั่งเช็คสต๊อกแล้วป้ายฝั่งสั่งของขยับตาม) แก้แล้วด้วย `backfillOrderLabels()` (ฟังก์ชัน one-off ใน `Code.gs`) ที่ต้องรันครั้งเดียวหลังอัปเดตโค้ดชุดนี้เพื่อ "ตรึง" ค่าเดิมลง `OrderLabel` ให้สินค้าเก่าทุกตัว — **ถ้าเจอ Code.gs เวอร์ชันเก่าที่ไม่มีฟังก์ชันนี้ หรือผู้ใช้บอกว่ายังไม่เคยรัน ให้เตือนก่อนแก้เรื่องหน่วยต่อ** สินค้าใหม่ที่สร้างผ่าน `addProductFromApp`/`submitNewProduct` ตั้ง `OrderLabel` ให้อัตโนมัติตั้งแต่สร้างแล้ว ไม่ต้องรอ backfill

**Retry-on-timeout** — `API_TIMEOUT_MS` เดิม 25 วิ พบว่า cold connection ครั้งแรกของ session (ไม่มี sessionStorage cache) มักหลุดเวลานี้บ่อย (DNS+TLS 2 โดเมนซ้อนกันเพราะ `script.google.com` redirect ไป `script.googleusercontent.com` + Apps Script อาจ cold start) ทั้งที่ backend ไม่มีปัญหาเลย (ยืนยันจาก Executions log สะอาดทุกครั้งที่ไล่บั๊กนี้) แก้โดย:
- `apiGet`/`apiPost` รับ parameter เพิ่ม (`timeoutMs` / `retryOnTimeout`) — `bootstrap()` เรียกรอบแรกด้วย timeout สั้น (10 วิ) ถ้าพังจะ retry เงียบๆ อีกรอบด้วย timeout เต็ม (25 วิ) ก่อนค่อยโชว์ error จริง
- `apiPost` เพิ่ม flag `retryOnTimeout` (ใส่ `true` เป็น argument ที่ 3) — **ใช้ได้เฉพาะ action ที่ "set" ค่าตรงๆ เรียกซ้ำแล้วผลเหมือนเดิม** (`setProductSkipDate`, `setUnitLabel`, `setOrderUnitLabel`, `setSupplierCoverPhoto`, `setSupplierOrderConfirm`) — **ห้ามใส่กับ action ที่สร้างแถวใหม่/อัปโหลดไฟล์ทุกครั้งที่เรียก** (`createOrderBatch`, `addProduct`, `saveStock`, `saveProductPhotos`, `saveStaffAvatar`) เพราะ retry ซ้ำจะสร้างข้อมูล/ไฟล์ซ้อนจริง (เทียบเคส `createOrderBatch` ไม่ idempotent ที่แอปสั่งของเคยเจอปัญหาเดียวกันมาแล้ว)
- เพิ่ม `<link rel="preconnect">` ให้ `script.google.com` และ `script.googleusercontent.com` ใน `<head>` ให้เริ่ม DNS/TLS ตั้งแต่หน้าเพิ่งโหลด ไม่ต้องรอ JS ท้าย body เรียก fetch ก่อน

⚠️ **เคย "แก้ไม่ครบ" มาก่อน 1 ครั้งในหัวข้อ OrderLabel** (fallback ทำให้ยังเชื่อมกันอยู่จนกว่าจะรัน backfill) ทำให้ผู้ใช้เสียเวลาไล่บั๊กเดิมซ้ำและหงุดหงิดมาก — ก่อนบอกว่าแก้เรื่องหน่วยเสร็จ ต้องเช็คให้แน่ใจว่าไม่มี fallback ที่ยังโยงสองฟิลด์เข้าด้วยกันเหลืออยู่

**เรื่อง "หมดเวลาเชื่อมต่อ" ที่ไล่กันมายาวมาก (4-5 ก.ย. 69) — สรุปแล้วไม่เกี่ยวกับโค้ด**: มีรอบหนึ่งที่ผู้ใช้เจอ error ซ้ำๆ แม้จะมี retry แล้ว ไล่จนพิสูจน์ได้ว่า**ไม่ใช่บั๊กแอป** เพราะยิง URL ตรงๆ ใน Safari (ไม่ผ่านแอปเลย) ก็ยังค้างเหมือนกัน ทั้งที่ Executions log โชว์ backend รับ request สำเร็จเร็วต่อเนื่องตลอดเวลานั้น — สรุปว่าเป็นปัญหาเน็ต/อุปกรณ์เฉพาะจุดที่เกิดขึ้นเป็นครั้งคราว (ท้ายที่สุดหายเองหลังรีสตาร์ทอุปกรณ์ ไม่ได้แก้ตั้งค่าอะไรเลย) **ถ้าเจอรายงาน "โหลด/บันทึกไม่สำเร็จ" อีก ให้ขอเช็ค Executions log ก่อนเป็นอันดับแรกเสมอ** (ถ้า backend ตอบเร็ว/สำเร็จตลอดช่วงเวลานั้น แปลว่าไม่ใช่โค้ดเราแน่ๆ ไม่ต้องรีบแก้โค้ดเพิ่ม) และลองให้ผู้ใช้เปิดลิงก์ `?action=bootstrap` ตรงๆ บนเครื่อง/เน็ตเดียวกับที่เจอปัญหาเทียบดูก่อนเสมอ

### สถานะล่าสุด (อัปเดต 6 ก.ย. 69) — เตรียมทดสอบอ่านบิลเก่าย้อนหลัง 101 ใบ + ไล่บั๊กความเสถียรของ analyzeBillPhoto

**บริบท**: ผู้ใช้มีรูปบิลเก่าสะสมอยู่ใน Google Drive 3 โฟลเดอร์ ("รูปบิลซามูไร 1/3, 2/3, 3/3" — id `19aAyJ_sGYq8hjp2irKjROL1il6sB2cue`, `1_jBi8QE9aphajIECq_mEq9hHjJipRU0a`, `12NIYMiA85R1vcoTBtxsjNjRHR0eH-hmL` รวม ~101 ใบ) ต้องการเอาเข้าฟีเจอร์ "รับของ — ถ่ายบิล" ทีละใบเพื่อให้ `ProductAlias` เรียนรู้การจับคู่สินค้าล่วงหน้า **ยังไม่ได้เริ่มไล่ backlog จริงจังเลยจนจบ session นี้** (ทั้ง session ใช้ไปกับการแก้บั๊ก/เช็คความเสถียรของฟีเจอร์นี้ก่อน) — session ถัดไปเริ่มไล่ backlog ได้เลย

⚠️ **ข้อควรจำสำคัญเรื่อง backlog นี้**:
- ต้องทำ**ทีละบิล/ทีละวันเท่านั้น** ห้ามใส่รูปบิลหลายวัน/หลายใบพร้อมกันในการอัปโหลดครั้งเดียว — `analyzeBillPhoto` ถูกออกแบบให้อ่าน "เอกสาร 1 ใบ" ต่อครั้ง (รูปหลายใบพร้อมกัน = หน้าเดียวกันของบิลใบเดียวกันเท่านั้น ไม่ใช่บิลคนละใบ) ใส่ปนกันจะทำให้ AI อ่านวันที่/ยอดรวม/รายการปนกันเละ
- เป้าหมายจริงคือให้ `ProductAlias` เรียนรู้ ไม่ได้ต้องการให้บิลเก่า 101 ใบเป็นประวัติซื้อจริงถาวรใน `PurchaseReceipts` — แผนคือ finalize ให้ครบทุกใบก่อน (เพื่อให้ ProductAlias เขียนจริง) แล้ว**ค่อยลบแถวที่มาจาก batch นี้ออกจาก PurchaseReceipts ทีหลัง** (ลบแค่ PurchaseReceipts ไม่กระทบ ProductAlias เพราะคนละชีต ไม่ผูกกัน) — ผู้ใช้ยังไม่ได้ขอให้เขียนฟังก์ชันลบเป็นชุดจริงๆ ถ้าถึงตอนนั้นให้เสนอ/เขียนให้ (ระบุ BatchID หรือช่วงวันที่)

**บั๊ก/การแก้ไขที่ทำไปจริงใน session นี้** (commit `ae604fb` → `3873db7`, push แล้วทุกอัน):
1. ปุ่ม "ถ่ายรูป"/"เลือกจากคลังภาพ" แยกกัน 2 ปุ่ม (เดิมเป็นปุ่มเดียว "ถ่ายรูป / เลือกไฟล์" ที่เปิดเมนูกลางของเบราว์เซอร์) + แก้บั๊ก `capture` attribute ให้ใช้ `setAttribute('capture','environment')` แทน property assignment (2 จุด: `renderBillCapture` และหน้าเช็คสต๊อกหลักที่ปิดใช้งานอยู่ตอนนี้ `CAMERA_PER_PRODUCT_ENABLED=false`) — ทดสอบผ่าน Playwright E2E จริงแล้ว
2. `ProductAlias`: เดิม `analyzeBillPhoto` อ่านทั้งชีตทุกครั้งที่มีคนถ่ายบิล + `upsertProductAlias` (เดิม) อ่านทั้งชีตซ้ำทุกรายการในบิล — เปลี่ยนเป็น `getProductAliasIndex(supplierId)` (cache ผ่าน `cacheGet/cacheSet` เดิมของไฟล์ 10 นาทีต่อซัพพลายเออร์) + `batchUpsertProductAlias()` อ่าน/เขียนครั้งเดียวต่อบิล invalidate cache ทันทีตอนเรียนรู้ alias ใหม่
3. `finalizePurchaseReceipt`: คอลัมน์ `Date` เดิม hardcode เป็นวันนี้เสมอ (ปัญหาตรงๆ กับ backlog บิลเก่า — จะทำให้ยอดซื้อ "วันนี้"/"เดือนนี้" พุ่งผิดปกติ) เปลี่ยนเป็น parse จาก `billHeader.billDate` จริง (`parseBillDateToSheetFormat()` รองรับปี พ.ศ./ค.ศ.) fallback เป็นวันนี้เฉพาะ parse ไม่ได้ (บิลเขียนมือไม่มี header)
4. `analyzeBillPhoto` timeout ฝั่งเว็บ: ไล่ขยับ 90 วิ → 150 วิ → **270 วิ (ปัจจุบัน)** ตามหลักฐานจริงใน Executions log (เจอ doPost ใช้เวลาถึง 88-113 วิ ทั้งที่ backend สำเร็จ แต่หน้าเว็บตัดก่อน)
5. `callGemini()`: เพิ่ม retry อัตโนมัติ 1 ครั้ง (รอ 3 วิ) เมื่อเจอ Gemini 503 "high demand" หรือตอบ 200 แต่ parse ไม่ได้ข้อความเลย (พบทั้งคู่จริงคืนนี้ น่าจะอาการเดียวกันจากโหลดสูงชั่วคราวฝั่ง Google) ทำฝั่ง backend ไม่ใช่ client auto-retry เพราะยังไม่เขียนอะไรลงชีตตอนนั้น ปลอดภัย

⚠️ **เหตุการณ์แปลกที่ยังไม่รู้สาเหตุแน่ชัด**: ระหว่างไล่บั๊กคืนนี้ ผู้ใช้เปิด Apps Script Editor แล้วพบว่า `Code.gs` **ว่างเปล่าไม่มีโค้ดเลย** ทั้งที่เพิ่งแปะไปไม่นาน (ทำให้ error ทุกครั้งเป็น "Load failed" เพราะไม่มี `doGet`/`doPost` ให้เรียก) แก้โดยส่งไฟล์ใหม่ให้แปะซ้ำ + ย้ำขั้นตอน Deploy ที่ถูกต้อง (**Manage deployments → แก้ deployment เดิม → New version** ไม่ใช่ "New deployment" ที่จะได้ URL ใหม่คนละอันไม่อัปเดต URL เดิม) หลังจากนั้นกลับมาใช้งานได้ปกติ — ไม่ทราบสาเหตุที่แท้จริงว่าไฟล์หายไปได้ยังไง ถ้าเจอซ้ำอีกให้สังเกตว่าเกิดจากอะไร

**ปัญหาที่เจอแล้วสรุปว่าไม่ใช่บั๊กโค้ด**: error "Load failed" อีกครั้งหนึ่งที่เจอทั้งที่ Executions log โชว์ backend สำเร็จเร็ว (27.82 วิ) — สงสัยว่าเกี่ยวกับ iOS Safari ตัด network ของแท็บที่ถูกส่งไปพื้นหลัง (ล็อกจอ/สลับแอประหว่างรอ AI อ่านบิลซึ่งกินเวลาเป็นนาที) ยังไม่ได้ยืนยันจากผู้ใช้ชัดเจนว่าใช่สาเหตุนี้จริงไหม — ถ้าเจอ "Load failed" อีกครั้งที่ backend สำเร็จเร็วใน log ให้ถามผู้ใช้ว่าล็อกจอ/สลับแอประหว่างรอไหมเป็นอันดับแรก

**ค้างไว้ยังไม่ตัดสินใจ**: เสนอเพิ่ม `LockService` กันข้อมูลชนกันตอนใช้แอปพร้อมกันหลายคน (`saveStock`/`batchUpsertProductAlias` ไม่มีการล็อกตอนนี้ ความเสี่ยงต่ำมากในการใช้งานจริงเพราะมักแตะคนละแถวคนละสินค้า) ผู้ใช้ยังไม่ตอบว่าจะทำหรือไม่ทำ — ถ้าคุยเรื่องนี้ต่อให้หยิบมาถามใหม่

### สถานะล่าสุด (อัปเดต 7 ก.ย. 69) — ฟีเจอร์ใหม่: แยกบิลอัตโนมัติเมื่อถ่ายหลายบิลพร้อมกัน

**บริบท**: ระหว่างเริ่มไล่ backlog 101 ใบ ผู้ใช้เจอ error `[analyzeBillPhoto] อ่านบิลไม่สำเร็จ` เพราะเผลอถ่ายบิลคนละใบ/คนละวันปนกันมาในการอัปโหลดครั้งเดียว (ตรงกับกติกาเดิมที่ห้ามไว้) แต่ผู้ใช้ชี้ว่านี่เป็น use case จริงที่เกิดซ้ำได้ ไม่ใช่แค่ backlog — ซัพพลายเออร์บางรายเอาบิลตกหล่นจากรอบก่อนมาพร้อมบิลวันนี้ พนักงานอยากสแกนพร้อมกันได้เลยไม่ต้องแยกทีละใบ

**ออกแบบ 3 ชั้นความเสี่ยง** (คุยผ่าน Artifact demo ก่อนได้ผู้ใช้เห็นชอบแล้วค่อย implement จริงตาม workflow เดิม):
1. **แยก action ใหม่ทั้งหมด** — `analyzeBillBatch` ใน `Code.gs` **ไม่แก้ `analyzeBillPhoto` เดิมแม้แต่บรรทัดเดียว** เส้นทางถ่ายบิลปกติทุกวันไม่ถูกกระทบเลย
2. **คนต้องตรวจ/ยืนยันก่อนเขียนจริงเสมอ** — `analyzeBillBatch` แค่เสนอการแบ่งกลุ่มรูป ไม่เขียนชีตเลย ฝั่งเว็บบังคับให้ตรวจทีละบิล (`renderBillBatchItemReview`) ก่อนกด "ส่งทั้งหมด" ซึ่งก็แค่เรียก `submitBillForReview` เดิมวนหลายรอบ (ไม่มี action ใหม่เขียน `PurchaseReceipts`/`PendingBillReceipts` ตรงๆ)
3. **backlog 101 ใบเป็นสนามทดสอบจริงก่อนเปิดใช้กว้าง** — ยังไม่ได้เริ่มใช้งานจริงกับ backlog เลย

**กลไกแก้ไขเมื่อ AI แบ่งกลุ่มผิด** (`renderBillBatchRegroup`): **ไม่พยายาม "หั่น" รายการที่ AI อ่านมาแล้วเอง** (ไม่รู้ว่าแต่ละรายการมาจากรูปไหนในกลุ่มที่ผสมกันไปแล้ว) — ให้คนเลือกรูปที่ควรเป็นบิลเดียวกันเอง แล้ว**เรียก `analyzeBillPhoto` ตัวเดิมที่ใช้งานจริงทุกวันซ้ำ** กับรูปที่เลือกเท่านั้น รับประกันผลลัพธ์ถูกต้องเสมอแทนที่จะเดา

**Deploy/push รอบแรก (เวอร์ชัน single-call) เจอบั๊กจริงตอนทดสอบ**: หลัง deploy+push แล้วผู้ใช้ทดสอบโหมดหลายบิลจริง เจอ "Load failed" ซ้ำๆ **ทุกรอบ** — ไล่ผ่าน Executions log เจอว่า backend สำเร็จจริง (ไม่มี error) แต่คำขอ `analyzeBillBatch` เดิม (ให้ Gemini ทั้งแยกกลุ่ม+อ่านรายการทุกบิลพร้อมกันในคำขอเดียว) กินเวลาถึง **103.8 วิ** เทียบกับ `analyzeBillPhoto` บิลเดี่ยวที่ 45-49 วิแล้วผ่านทุกครั้งในช่วงเวลาเดียวกัน — สรุปว่าการเชื่อมต่อ ทนคำขอที่ยาวขนาด 100+ วิไม่ไหว (ไม่ว่าจะเพราะเน็ตมือถือ/ไวไฟตอนนั้น หรือ backgrounding — ยังไม่ยืนยันสาเหตุแน่ชัด แต่ตัวเลขเวลาสัมพันธ์ตรงกับผลลัพธ์ชัดเจน)

**แก้แล้ว (7 ก.ย. 69 เวอร์ชัน 2)**: ตัดขอบเขตงานของ `analyzeBillBatch` ให้เหลือแค่ "แยกกลุ่มรูป" อย่างเดียว (สั่ง Gemini ห้ามอ่านรายการสินค้าเลย เร็วขึ้นมาก) แล้วให้ `renderBillBatchAnalyzing` ฝั่งเว็บวนเรียก `analyzeBillPhoto` ตัวเดิม (พิสูจน์แล้วว่านิ่ง ~45-49 วิ) **ทีละกลุ่มเป็นคำขอสั้นๆ หลายครั้งแทน** — ผลลัพธ์/หน้าจอเหมือนเดิมทุกอย่าง เปลี่ยนแค่จำนวน/ความยาวของคำขอเบื้องหลัง **ยังไม่ได้ทดสอบซ้ำกับของจริงหลัง fix รอบนี้**

**สิ่งที่ทำไปแล้วใน session นี้**:
- `Code.gs`: เพิ่ม `analyzeBillBatch(body)` (action ใหม่ใน `doPost`, แก้ 2 รอบตามที่เล่าด้านบน) — ไม่แตะ `analyzeBillPhoto` เลยตลอดทั้ง session กันรูปตกหล่น/รูปซ้ำข้ามกลุ่มจากการแบ่งของ Gemini ด้วย + เพิ่มความละเอียด timestamp ของ `submitBillForReview`'s BatchID เป็นระดับมิลลิวินาที (เดิมระดับวินาที เสี่ยงชนกันตอนโหมดใหม่เรียกซ้ำเร็วๆ ให้ซัพพลายเออร์เดียวกัน)
- `stock-check.html`: เพิ่ม toggle "โหมดหลายบิลรวมกัน" ใน `renderBillCapture` (ปิดโดย default) + หน้าจอใหม่ 5 หน้า: `renderBillBatchAnalyzing` (2 phase: แยกกลุ่มเร็วๆ แล้ววนอ่านรายการทีละบิล), `renderBillBatchReview` (การ์ดต่อบิล พร้อมปุ่ม "แก้กลุ่มรูป"/"ตรวจสอบรายการ"/"ส่งทั้งหมด"), `renderBillBatchItemReview` (โครงเดียวกับ `renderBillReview` เดิม คัดลอกมาแยกต่างหาก), `renderBillBatchRegroup`, `renderBillBatchSuccess`
- ทดสอบผ่าน Playwright E2E จริง 2 ไฟล์ (mock request ที่ `page.route()` เหมือนแพทเทิร์นเดิมของแอปสั่งของ, อัปเดตให้ตรงกับ contract ใหม่ของ `analyzeBillBatch` แล้ว): (1) flow เต็มของโหมด batch รวม regroup-correction + จำลอง `submitBillForReview` พังกลางทางแล้วกด "ส่งทั้งหมด" ซ้ำเพื่อลองบิลที่เหลือ (2) regression เช็คว่า flow บิลเดี่ยวเดิม (toggle ปิด) ยังทำงานถูกต้องเป๊ะเหมือนก่อนแก้ — **แต่ทั้งหมดนี้คือ mock ฝั่ง Playwright เท่านั้น ยังไม่ได้ทดสอบกับ Gemini จริง/เน็ตจริงหลัง fix เวอร์ชัน 2**

**ขั้นต่อไป**: ผู้ใช้ต้อง deploy `Code.gs` ใหม่ (แปะทับใน Apps Script Editor → New version) แล้วทดสอบโหมดหลายบิลอีกรอบ ถ้ายัง "Load failed" อยู่ ให้เช็ค Executions log ก่อนเป็นอันดับแรกเสมอตามธรรมเนียม (ครั้งนี้จะเห็นเป็น `doPost` หลายรายการสั้นๆ แทนที่จะเป็นรายการเดียวยาว — ถ้ายังหลุดแม้แต่คำขอสั้นๆ แปลว่าไม่ใช่เรื่องความยาวคำขอแล้ว ต้องหาสาเหตุใหม่)

### สถานะล่าสุด (อัปเดต 7 ก.ย. 69 — ต่อ) — deploy จริง + บั๊กที่เจอจากการใช้งานจริงครั้งแรก + บั๊กค้าง 1 ตัวที่สำคัญ

**บริบท**: ต่อจากหัวข้อด้านบน (โหมดแยกหลายบิล) — session นี้ผู้ใช้ deploy `Code.gs` เวอร์ชันที่มีโหมดแยกหลายบิลจริงแล้ว เริ่มทดสอบกับบิลจริงเป็นครั้งแรก เจอบั๊ก/ปัญหาจริงหลายจุดเรียงกัน แก้ไปแล้ว 5 อัน ยังค้างอยู่ 1 อันที่สำคัญ (ดูหัวข้อสุดท้าย)

**สิ่งที่แก้ไปแล้ว push แล้วทั้งหมด (commit `85f71f2` → `df03723`)**:

1. **แจ้งเตือนซัพพลายเออร์ไม่ตรง** — เพิ่มเข้า `analyzeBillPhoto` (จุดเดียวที่แตะฟังก์ชันนี้ตั้งใจ ตามคำขอผู้ใช้ตรงๆ) คืนค่า `supplierMismatchWarning` (string หรือ null) เทียบโลโก้/ชื่อบริษัทบนบิลกับซัพพลายเออร์ที่เลือกไว้ ฝั่งเว็บโชว์ banner สีแดงพร้อมปุ่ม "เลือกซัพพลายเออร์ใหม่" ทั้งในหน้ารีวิวปกติและโหมดหลายบิล
2. **รูปบิลฝั่งเจ้าของไม่ขึ้น (broken image)** — บั๊กเดิมที่ไม่เคยถูกเจอมาก่อน (ทางฝั่งพนักงานถ่ายบิลใหม่ใช้ base64 data URL ไม่เคยผ่านโค้ดจุดนี้) `saveBillPhotosOrganized` เดิม `return file.getUrl()` คืน URL หน้า viewer ของ Drive ที่ embed เป็น `<img>` ไม่ได้ — เปลี่ยนเป็น `https://drive.google.com/thumbnail?id=X&sz=w2000` + เพิ่ม `toEmbeddableDriveUrl()` normalizer ใน `getPendingBillReceipts()` ให้ซ่อม URL แบบเก่าที่เก็บไว้แล้วอัตโนมัติตอนอ่าน (self-healing แบบเดียวกับ `normalizeSkipDatesCell`)
3. **VAT-inclusive mismatch false positive** — บิลบางเจ้า (ยืนยันแล้วกับ PFP) พิมพ์ราคาต่อหน่วยรวม VAT ไว้แล้ว ทำให้ยอดรวมรายการที่คำนวณเองตรงกับ "ยอดหลังแวต" ไม่ใช่ "ยอดก่อนแวต" ที่ `updateBillHeaderMismatch()` เดิมเทียบอย่างเดียว เลยเตือนหลอกทุกครั้ง แก้โดยเทียบกับทั้ง `subtotal` และ `total` ตรงอันใดอันหนึ่งถือว่าไม่ผิด (ผู้ใช้ยืนยันให้ apply กับทุกบิลเพราะ logic เดียวกัน ไม่ใช่เฉพาะ PFP) แก้พร้อมกัน 2 จุด (`renderBillReview` + `renderBillBatchItemReview` มีฟังก์ชันนี้แยกกันคนละชุด)
4. **หน้า "บิลรอตรวจสอบ" ฝั่งเจ้าของไม่มี retry เลย** — ต่างจากทั้งแอปที่มี retry-on-timeout กันไว้แล้ว (ดู bootstrap) จุดนี้ `apiGet('pendingBills')` ยิงครั้งเดียวจบ พังแล้วไม่มีแม้แต่ปุ่ม "ลองใหม่" ต้องกดย้อนกลับแล้วเข้าใหม่เอง (เจอจริง 3 รอบติด ทั้งที่ Executions log ยืนยันว่า backend ตอบเร็ว <3 วิสำเร็จตลอด) แก้โดยใส่ retry แบบเดียวกับ `bootstrap()` (สั้นก่อน 10 วิ ไม่ทันค่อยลองเต็ม 25 วิ) + เพิ่มปุ่ม "ลองใหม่" ถ้ายังพังทั้งสองรอบ
5. **ซูมรูปบิลไม่ได้บนคอมพิวเตอร์** — มือถือบีบนิ้วซูมได้เองอยู่แล้ว (ความสามารถเบราว์เซอร์ ไม่เกี่ยวกับโค้ด เพราะ viewport meta ไม่ได้ปิด user-scalable) แต่คอมไม่มีนิ้วให้บีบ เพิ่มดับเบิลคลิก/สกอลล์เมาส์ซูมให้ พร้อมรวมโค้ด lightbox ที่ซ้ำกัน 2 จุด (หน้ารีวิวปกติ/โหมดหลายบิล) เป็นฟังก์ชันเดียว `openBillPhotoLightbox(urls)`

⚠️ **บั๊กค้างที่สำคัญที่สุด ยังไม่เจอสาเหตุ — ต้องแก้ก่อนไล่ backlog 101 ใบต่อ**: เจอ error `The string did not match the expected pattern.` (ไม่มี `[analyzeBillPhoto]` นำหน้า แปลว่าพังฝั่ง client ไม่ใช่ backend) ตอนอ่านบิลจริง (ทดสอบกับบิล TVI) **เกิดซ้ำ 3 ครั้งติดกัน ทั้งที่ Executions log ยืนยันว่า backend สำเร็จทุกครั้ง** (doPost 64.35s, 50.29s = เวลาปกติของการอ่านบิลสำเร็จ ไม่มี error เลย) แปลว่า**บิลถูกอ่านสำเร็จแล้วจริง แต่หน้าเว็บพังตอนเอาผลลัพธ์มาแสดง/ประมวลผล** — ไล่โค้ดหาสาเหตุตรงๆ แล้วยังไม่เจอจุดที่ชัดเจน (ไม่ใช่ querySelector ที่เห็นได้ชัด, ไม่ใช่ regex ที่หาเจอ) เพิ่ม stack trace โชว์ในหน้า error ชั่วคราวไว้แล้ว (`renderBillAnalyzing`'s catch + batch mode's `showError`) แต่**ยังไม่ได้รับ screenshot ของ stack trace จริงกลับมาดู** ข้อสังเกตสำคัญ: ข้อความ error นี้เคยเจอมาก่อนแล้วครั้งหนึ่งที่หน้า `bootstrap()` (ดูหัวข้อ 5 ก.ย. 69 ด้านบน — ตอนนั้นสรุปว่าเป็นปัญหา cold-connection timeout แล้วแก้ด้วย retry ไม่ได้ยืนยันสาเหตุ query selector จริงๆ) **ถ้าเจอ error นี้อีกครั้งพร้อม stack trace ให้ไล่ตามบรรทัดที่ระบุในนั้นตรงๆ ก่อนเดาอย่างอื่น** เป็นไปได้ว่าสาเหตุจริงอาจไม่ใช่โค้ดของแอปเลยก็ได้ (ดู pattern ปัญหาเน็ต/อุปกรณ์เฉพาะจุดที่เคยสรุปไว้ก่อนหน้านี้) แต่ยังฟันธงไม่ได้จนกว่าจะเห็น stack trace จริง

**เรื่องแคชที่พบเพิ่ม**: นอกจากไอคอนหน้าโฮมที่เคยรู้แล้วว่าแคชเหนียว **แท็บที่ปักหมุด (pinned tab) ก็แคชเหนียวแบบเดียวกัน** — ยืนยันแล้วจากผู้ใช้จริงว่าเปิดผ่านแท็บปักหมุดเจอโค้ดเก่าค้างอยู่ ทั้งที่ deploy ใหม่ถูกต้องแล้ว (เช็คโค้ดที่ deploy จริงบน Vercel ผ่าน `mcp__Vercel__web_fetch_vercel_url` ยืนยันว่าถูกต้อง) แก้โดยเปิดผ่าน URL ตรงๆ ในแท็บใหม่แทน — **ถ้าเจอรายงานว่า "แก้แล้วแต่ยังเหมือนเดิม" อีก ให้ถามก่อนเสมอว่าเปิดผ่านแท็บ/ไอคอนที่ปักหมุด/บันทึกไว้หรือเปล่า**

**สถานะ backlog 101 ใบ**: ยังไม่ได้เริ่มไล่จริงจังเลย ถูกบั๊ก "did not match expected pattern" ขวางอยู่ (ทดสอบอ่านบิลจริงกี่ครั้งก็ชนบั๊กนี้) — **session ถัดไปควรขอ screenshot ของ error พร้อม stack trace จากผู้ใช้เป็นอันดับแรกก่อนทำอย่างอื่น** ถ้าแก้ได้แล้วค่อยเริ่มไล่ backlog จริง

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69) — ฟีเจอร์ใหม่: เตือนบิลซ้ำ + push เข้า main

**บริบท**: ผู้ใช้ขอให้ระบบตรวจจับว่าบิลที่กำลังสแกนถูกบันทึกเข้าระบบไปแล้วหรือยัง (กังวลเรื่องสแกนบิลใบเดิมซ้ำ โดยเฉพาะตอนไล่ backlog 101 ใบที่ยังไม่ได้เริ่ม — ดูหัวข้อด้านบน บั๊ก "did not match expected pattern" ยังไม่ได้แก้ ไม่ได้แตะเรื่องนี้ session นี้เลย) คุยผ่าน `AskUserQuestion` ตกลง 3 จุดก่อนเริ่ม implement (ตามกติกา demo ก่อนของแอปนี้):
1. เกณฑ์ตัดสิน: เลขที่บิล (BillNumber) ตรงกันก่อนเป็นอันดับแรก ถ้าไม่มี (บิลเขียนมือ ส่วนใหญ่ของซัพพลายเออร์รายวัน ไม่มี billHeader เลย) → เทียบรายการสินค้าทั้งหมด (ชื่อ+จำนวน+ราคา) แทน
2. ช่วงเวลาตรวจ: ไม่จำกัดเวลาย้อนหลัง (เผื่อบิล backlog ถูกสแกนซ้ำห่างกันเป็นเดือน)
3. พฤติกรรมเมื่อพบว่าอาจซ้ำ: เตือนอย่างเดียว ไม่บล็อกการบันทึก (เหมือน `supplierMismatchWarning` เดิม)

ทำ demo เป็น Artifact (mockup แบนเนอร์ 4 กรณี ใช้ข้อมูลบิล TVI จริงเป็นตัวอย่าง) ให้ดูก่อน ได้รับ "โอเคทั้งสามข้อ" แล้วค่อย implement จริง

**สิ่งที่ทำจริงใน `Code.gs`** (จุดเดียวที่แตะ `analyzeBillPhoto` ตั้งใจ เพิ่ม field `duplicateBillWarning` ใน return object):
- `checkDuplicateBill(supplierId, billHeader, items)` — เช็คกับทั้ง `PendingBillReceipts` (รอเจ้าของตรวจ) และ `PurchaseReceipts` (บันทึกจริงแล้ว) ของซัพพลายเออร์เดียวกัน เลขที่บิลตรงกันเป๊ะ → ฟันธงทันที (`matchType:'billNumber'`) ไม่งั้นเทียบรายการด้วย `itemOverlapRatio` (หารด้วยจำนวนรายการที่ **มากกว่า** กันบิลบางส่วน/บิลแก้ไขได้คะแนนสูงเกินจริง) ≥0.7 ถือว่าน่าจะซ้ำ (`matchType:'items'`)
- `buildItemFingerprint(items)` — สร้าง key เทียบกันได้จาก `normalizeAliasKey(billText)+qty+unitPrice` (ใช้ normalizer ตัวเดียวกับ ProductAlias)
- คืน `{ matchType, batchId, date, billNumber, photoUrl, status:'pending'|'finalized', overlapRatio }` หรือ `null` — **ไม่ persist ลงชีตไหนเลย** (เหมือน `supplierMismatchWarning`) เป็นคำเตือนแบบ ephemeral เฉพาะตอนสแกน ไม่โผล่ตอนเจ้าของเปิดบิลค้างจาก `PendingBillReceipts` มาดูทีหลัง (owner-resume ไม่เรียก `analyzeBillPhoto` ซ้ำ)

**ฝั่งเว็บ (`stock-check.html`)**: เพิ่ม `.duplicate-bill-warning` (สีส้ม `--amber` แยกระดับความรุนแรงจาก `.supplier-mismatch-warning` สีแดง) + ปุ่ม "ดูรูปบิลเดิม" (เปิด `openBillPhotoLightbox` เดิมด้วยรูปบิลเก่า ไม่ใช่รูปที่เพิ่งถ่าย) เพิ่มครบทั้ง 2 เส้นทางที่เรียก `analyzeBillPhoto`: หน้ารีวิวบิลเดี่ยว (`renderBillReview`, `state.billDuplicateWarning`) และโหมดหลายบิล (`renderBillBatchItemReview` เต็ม + มินิแบดจ์ในการ์ดลิสต์ `drawBatchCards`, ต่อ group `g.duplicateBillWarning`) รวมทั้ง path วิเคราะห์ครั้งแรก (`renderBillBatchAnalyzing`) และ path แก้กลุ่มรูป (`renderBillBatchRegroup`)

**ทดสอบจริงจัง (ตามกติกาข้อ 4)**:
- Unit test 13 เคส รัน `Code.gs` จริงในแซนด์บ็อกซ์ Node (`vm` + stub `SpreadsheetApp`/`CacheService` ขั้นต่ำ) ครอบคลุมเลขบิลตรงแม้รายการไม่ตรง, รายการตรง 75% เทียบกับ `PendingBillReceipts`, รายการตรงไม่ถึง 70% ต้องไม่เตือน, ไม่ข้ามเทียบคนละซัพพลายเออร์, overlap ratio หารด้วยจำนวนที่มากกว่าเสมอ — ผ่านหมด
- Playwright E2E จริง (mock ที่ `page.route()`) 4 สถานการณ์ (เลขบิลตรง/รายการตรง 80%/ไม่ซ้ำ-regression/โหมดหลายบิลครบมินิแบดจ์+แบนเนอร์+lightbox) — ผ่านหมด ข้อสังเกตสำหรับ session หน้า: ห้ามเรียก global `render()` (รีเซ็ต DOM ทั้งหน้าใหม่) หลังติ๊ก checkbox ด้วย Playwright เพราะ `render()` จะสร้าง checkbox ใหม่ unchecked ทับของเดิมเงียบๆ (ต้อง set state+render() ก่อน ค่อยติ๊ก checkbox ทีหลังเสมอ)

**Deploy**: ส่ง `Code.gs` เต็มไฟล์ให้ผ่าน SendUserFile แล้ว — **ยังไม่ได้รับการยืนยันจากผู้ใช้ว่าแปะ/deploy จริงหรือยัง** ถ้าคุยเรื่องบิลซ้ำต่อ ให้เช็คก่อนว่า deploy แล้วหรือยัง

**Push เข้า `main` แล้ว** (คำสั่งชัดเจนจากผู้ใช้ "Push เลย"): ตอน push เจอว่า `origin/main` ขยับไปไกลกว่าที่ local รู้จัก — อีก session หนึ่งดันงานฝั่ง **แอปสั่งของ** เข้า `main` ไปแล้ว 9 commits (แก้ XSS, ออเดอร์ซ้ำ, แยก endpoint แอดมิน ฯลฯ ดูหัวข้อ Order Web App ด้านล่าง) คนละไฟล์กับที่ session นี้แก้เลย (`Code.gs`/`stock-check.html` เท่านั้น เทียบกับ `index.html`/`order-app-Code.gs`/`CLAUDE.md` ของอีกฝั่ง) merge สะอาดไม่มี conflict — commit บน `main` ตอนนี้: `d5e7d5c` (ฟีเจอร์เตือนบิลซ้ำ) → `070a03a` (merge commit) หมายเหตุ: GitHub แจ้งว่า repo ถูกเปลี่ยนชื่อเป็น `panguess/Samurai` (S ใหญ่) แต่ redirect อัตโนมัติ push ผ่านปกติ ไม่ต้องแก้ remote URL

⚠️ **ค้างเล็กน้อยที่ไม่กระทบอะไร**: แถวทดสอบใน `PurchaseReceipts` จาก session ก่อน (BatchID ขึ้นต้น `RB0906`/`RB0907` ของ S012/S006 — ข้อมูลทดสอบตอนไล่บั๊กฟีเจอร์แยกหลายบิล ไม่ใช่ backlog จริง) ยืนยันแล้วว่าลบได้ (ไม่กระทบ `ProductAlias`) แต่ผู้ใช้ยังไม่ได้ลบเองใน Sheets (Claude แก้ตรงไม่ได้ sandbox ต่อ `google.com` ไม่ได้)

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69 — ต่อ) — ไล่บั๊กความหน่วงหน้า "สั่งของ" จนถึงแยกฟีเจอร์ถ่ายบิลออกเป็นโปรเจกต์ Apps Script ต่างหาก

**บริบท**: ผู้ใช้บ่นว่ากด "สั่งของ" (โดยเฉพาะเจ้าลุงทวี S001 ที่มีของเยอะสุด ~20-30 รายการ) แล้วช้า สงสัยว่าเกี่ยวกับ
ลูกจ้างเช็คสต๊อกพร้อมกันไหม — ไล่บั๊กยาวทั้ง session จบที่พบ 3 บั๊ก performance จริง + สมมติฐานใหญ่เรื่องโควต้า
Apps Script ที่ยังไม่ยืนยัน 100%

**บั๊ก performance ที่เจอ+แก้แล้ว (push แล้ว, ยืนยันด้วย Executions log ว่า Version ใหม่ทำงานเร็วปกติ 0.5-4 วิ)**:
1. `createOrderBatch` เดิมอ่านทั้งชีต `StockLogs` (โตขึ้นเรื่อยๆ ไม่มีสิ้นสุด) ทุกครั้งที่กด "สั่งของ" เพื่ออัปเดตคอลัมน์
   `OrderTriggered` ที่ grep ทั้งแอปแล้วไม่มีจุดไหนอ่านเลยสักที่ — ลบ logic นั้นทิ้งทั้งบล็อก (คอลัมน์ยังอยู่ในชีตเหมือนเดิม
   แค่เลิกคำนวณ/เขียนทับจากจุดนี้)
2. `upsertStockLogsLatest` (ทำงานตอนลูกจ้างกด "บันทึก" เช็คสต๊อก) เดิมเขียนทับแถวเดิมทีละแถว (`setValues()`
   แยกคนละคำสั่งต่อ 1 รายการ) — เจ้าที่มีของเยอะ (ลุงทวี 20-30 รายการ) ยิง Sheets API 20-30 ครั้งต่อเนื่อง แก้เป็น
   merge ในหน่วยความจำแล้วเขียนทีเดียว (unit test ยืนยัน: 25 รายการเหลือ `setValues` แค่ 1 ครั้ง จากเดิม 25 ครั้ง)
3. หน้า "สั่งของ" (`renderOrderPage`) เดิมยิง 4 คำขอพร้อมกัน (`stockStatus`/`orderedToday`/`confirmedToday`/
   `orderedItemsToday`) — รวมเป็น action เดียว `getOrderPageData()`/`orderPageData` ตัดจำนวนคำขอต่อการเปิดหน้า
   1 ครั้งจาก 4 เหลือ 1 (E2E ยืนยันแล้ว)
4. เพิ่ม `Logger.log('[doGet] '+action)` / `Logger.log('[doPost] '+body.action)` ที่จุดเริ่มของทุกคำขอ (เดิม log
   แค่ตอน error) — ตอนนี้ Executions log บอกชื่อ action ได้ทันทีโดยคลิกดูรายละเอียดแถว ไม่ต้องเดา

⚠️ **ลองแล้วถอนออก — บทเรียนเรื่อง "แก้จุดหนึ่งกระทบจุดอื่น" ตามที่ผู้ใช้เตือน**: เคยเพิ่ม prefetch ยิง
`orderPageData` ล่วงหน้าตั้งแต่หน้าเมนูเจ้าของ (`renderOwnerMenu`) ขึ้นจอ หวังซ่อน latency — แต่ผู้ใช้ทักว่า
"หลัง deploy นี้เปิดอะไรก็ช้าไปหมดหรือเปล่า" เพราะ prefetch ยิงทุกครั้งที่เข้าเมนูไม่ว่าจะกด "สั่งของ" ต่อจริง
หรือไปเปิด "สต๊อก & วิเคราะห์"/"บิลรอตรวจสอบ" แทน กลายเป็นคำขอเปล่าประโยชน์แย่งคิว/โควต้ากับหน้าอื่น — **ถอนออก
ก่อน push** (ไม่เคยขึ้น production) ถ้าจะลองแนวทาง prefetch อีกในอนาคต ต้องผูกกับ intent ที่ชัดเจนกว่านี้
(เช่น debounce ตามเวลาที่ผู้ใช้ค้างอยู่หน้าเมนูจริงๆ ก่อนตัดสินใจว่าจะไปไหน) ไม่ใช่ยิงทันทีที่ render

⚠️ **สมมติฐานใหญ่ (ยังไม่ยืนยัน 100% — ต้องเฝ้าดูผลจริงต่อ)**: ผู้ใช้สังเกตเองว่าช่วงที่หน้าอื่นค้าง/error โดยไม่มี
Executions log โผล่เลยแม้แต่บรรทัดเดียว (แม้ refresh แล้ว — พิสูจน์ว่าคำขอไม่เคยไปถึง `doGet` จริง) มักตรงกับช่วงที่
ลูกจ้างกำลังถ่ายบิลให้ AI อ่านพอดี (`analyzeBillPhoto`/`analyzeBillBatch` กินเวลา 45-100+ วิ/ครั้ง เทียบกับ action
อื่นทั้งแอปที่ 1-4 วิ) — ตั้งสมมติฐานว่า Apps Script Web App รันในนามบัญชีเดียว ("Execute as: Me") ทุกคำขอจากทุกคน
แชร์โควต้า "จำนวนการรันพร้อมกัน" ก้อนเดียว ถ่ายบิลที่ช้าผิดปกติจองที่นั่งไว้นาน อาจทำให้คำขออื่นที่ชนจังหวะเดียวกัน
โดน Google ปฏิเสธตั้งแต่ชั้น infrastructure เลย **ไม่มีทางยืนยันจากใน sandbox นี้ได้ 100%** (ต่อ google.com ไม่ได้
เลย ไม่มีเอกสารโควต้าที่แท้จริงให้เปิดดู ไม่มีทาง live-test) — บอกผู้ใช้ตรงๆ ไปแล้วว่านี่คือการเดาที่มีหลักฐาน
รองรับหนักแน่นที่สุดเท่าที่มี ไม่ใช่การยืนยันจริง

**แก้ตามสมมติฐาน (ทำแล้ว push แล้ว 346d02c)**: ย้ายฟีเจอร์ "รับของ — ถ่ายบิล" ทั้งหมด (24 ฟังก์ชัน ~570 บรรทัด
— `analyzeBillPhoto`, `analyzeBillBatch`, `submitBillForReview`, `finalizePurchaseReceipt`,
`getPendingBillReceipts` + helper ที่ใช้เฉพาะกลุ่มนี้ เช่น `callGemini`/`checkDuplicateBill`/
`getProductAliasIndex`/`batchUpsertProductAlias` ฯลฯ) ออกจาก `Code.gs` ไปเป็นไฟล์ **`BillCapture-Code.gs`**
(อยู่ที่ root repo แล้ว) ที่ต้อง deploy เป็น **Apps Script โปรเจกต์แยกต่างหาก** (standalone script ใช้
`SpreadsheetApp.openById(SPREADSHEET_ID)` แทน `getActiveSpreadsheet()` เพราะไม่ได้ bound กับชีต) — คนละโควต้า
การรันพร้อมกัน อ่าน/เขียน Google Sheet ตัวเดิมได้ปกติ (grep ยืนยันแล้วว่า 24 ฟังก์ชันนี้ไม่มีจุดไหนในโค้ดที่เหลือ
เรียกใช้เลย ปลอดภัยที่จะย้ายทั้งก้อน)

**Frontend (`stock-check.html`)**: เพิ่ม `BILL_API_URL` แยกจาก `API_URL` เดิม + `apiGet`/`apiPost`/
`apiPostSilentRetry` รับ parameter `baseUrl` เพิ่ม (ตัวสุดท้าย) — เรียก 5 action ของฟีเจอร์ถ่ายบิลทั้งหมด
(`analyzeBillPhoto`×3 จุด, `analyzeBillBatch`, `submitBillForReview`×2 จุด, `finalizePurchaseReceipt`,
`pendingBills`×2 จุด) ผ่าน `BILL_API_URL` ตอนนี้ — action อื่นทั้งหมดยังใช้ `API_URL` เดิมเหมือนเดิมทุกจุด

**ทดสอบแล้ว**: unit test 10 เคสกับ `BillCapture-Code.gs` จริงในแซนด์บ็อกซ์ Node (`vm`) ครอบคลุม
`checkDuplicateBill`/`findAliasMatch`/`buildItemFingerprint`/`sanitizeBillHeader`/`parseBillDateToSheetFormat`/
`getProductAliasIndex` caching/`submitBillForReview`/`finalizePurchaseReceipt`/doGet-doPost dispatcher — ยืนยันว่า
logic เหมือนเดิมทุกประการหลังย้ายไฟล์ + Playwright E2E ยืนยัน routing ถูกต้อง (5 action ไปที่ `BILL_API_URL`,
action อื่นยังไปที่ `API_URL` เหมือนเดิม)

**Deploy จริงทำไปแล้ว**: ผู้ใช้สร้างโปรเจกต์ Apps Script ใหม่ วาง `BillCapture-Code.gs` (ใส่ `SPREADSHEET_ID` จริง
+ ตั้ง `GEMINI_API_KEY` เอง) แล้ว deploy ได้ URL จริงมา — ระหว่างตั้งค่าเจอ Google OAuth error
`Error 401: invalid_client` ("The OAuth client was not found") ตอนกด authorize ครั้งแรก **ไม่เกี่ยวกับโค้ดเราเลย**
เป็นปัญหาฝั่ง Google OAuth ชั่วคราว แก้ได้ด้วยการรีเฟรชหน้า Apps Script Editor แล้วลอง authorize ใหม่ — ถ้าเจอซ้ำ
อีกให้แนะนำแบบเดียวกัน (รีเฟรช/ลอง incognito/สร้างโปรเจกต์ใหม่ถ้ายังไม่หาย) ไม่ต้องสงสัยโค้ด
- อัปเดต `BILL_API_URL` ใน `stock-check.html` เป็น URL จริงแล้ว push เข้า `main` แล้ว (commit `346d02c`)

⚠️ **ค้างที่ไม่เร่งด่วน**: `Code.gs` หลัก (เวอร์ชันตัดฟีเจอร์ถ่ายบิลออกแล้ว ส่งให้ผู้ใช้ผ่าน SendUserFile แล้ว)
**ยังไม่ได้ deploy ทับโปรเจกต์เดิม** — ไม่กระทบอะไรถ้ายังไม่ deploy (ฟังก์ชันเก่าที่เหลืออยู่ในนั้นแค่ไม่มีใครเรียก
ใช้แล้วเพราะ frontend ชี้ไปโปรเจกต์ใหม่หมดแล้ว) แต่ควร deploy ทับตอนสะดวกเพื่อเก็บกวาดโค้ดที่ไม่ใช้แล้วออกให้ตรงกับ
ของจริง — ถ้าเจอผู้ใช้อัปโหลด `Code.gs` เวอร์ชันเก่าที่ยังมี `analyzeBillPhoto` ฯลฯ อยู่มาให้ดูในอนาคต อย่าแปลกใจ
ให้ถามว่า deploy เวอร์ชันตัดแล้วหรือยัง

⚠️ **ยังไม่ได้ยืนยันผลจริง**: ผู้ใช้บอก "ทำทุกอย่างเสร็จหมดแล้วไม่มีอะไรค้าง" ก่อนจบ session แต่**ยังไม่ได้ทดสอบ
ถ่ายบิลจริงพร้อมเปิดหน้าอื่นพร้อมกันหลัง deploy รอบนี้เพื่อยืนยัน/ปัดตกสมมติฐานเรื่องโควต้า** — session ถัดไปถ้าคุย
เรื่องความหน่วงต่อ ให้ถามก่อนว่าทดสอบสถานการณ์นี้แล้วหรือยัง ผลเป็นอย่างไร (หายไปแล้ว = ยืนยันสมมติฐาน, ยังเจอ
เหมือนเดิม = ต้องหาสาเหตุใหม่ทั้งหมด ไม่ใช่การแยกโปรเจกต์ทำไม่สำเร็จ)

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69 — ต่อ 2) — ตั้งค่า BillCapture จริงเจอบั๊ก SPREADSHEET_ID + เพิ่มแคชแก้ช้า

**บั๊กจริงที่เจอตอน deploy `BillCapture-Code.gs` ครั้งแรก — คนละสาเหตุกับสมมติฐานเรื่องโควต้าเลย**: หลังผู้ใช้สร้าง
โปรเจกต์ Apps Script ใหม่ วาง `BillCapture-Code.gs` แล้ว deploy (สิทธิ์ "Execute as: Me" / "Anyone" ถูกต้องตั้งแต่แรก)
หน้า "บิลรอตรวจสอบ" ขึ้น "Load failed" ทันที — ไล่ด้วยการเปิด URL ของ BillCapture ตรงๆ ในแท็บใหม่ (เทคนิคเดิมที่ใช้มา
ตลอด) เจอ error ชัดเจน: `Exception: Illegal spreadsheet id or key: PASTE_YOUR_SPREADSHEET_ID_HERE (line 36, file
"Code")` — **ผู้ใช้ยังไม่ได้แทนที่ค่า `SPREADSHEET_ID` placeholder ด้วย ID จริงตามขั้นตอนในคอมเมนต์หัวไฟล์** แก้โดย
หา `SPREADSHEET_ID` ในไฟล์ (`Ctrl+F` ในหน้า Editor) ใส่ ID จริงจาก URL ของ Google Sheet แล้ว **ต้องกด Deploy →
Manage deployments → New version ด้วย** (แก้แค่โค้ดในหน้า Editor เฉยๆ ไม่มีผลกับ URL ที่ deploy ไปแล้ว — ธรรมเนียม
เดิมของทั้งแอป) หลังแก้แล้วเปิด URL ตรงๆ เห็น `{"error":"unknown action: undefined"}` ยืนยันว่าใช้งานได้จริง

**บทเรียนสำหรับ session หน้า**: ถ้าเจอ Apps Script โปรเจกต์ใหม่ (ทั้ง BillCapture หรือโปรเจกต์แยกอื่นในอนาคต) พังทันที
หลัง deploy ครั้งแรก **ให้สงสัย `SPREADSHEET_ID`/placeholder ที่ยังไม่ได้แทนที่เป็นอันดับแรกก่อนเดาเรื่องอื่น** (เร็ว
กว่าการไล่ CORS/สิทธิ์/โควต้ามาก) วิธีเช็คเร็วสุดคือเปิด URL ของ Web App นั้นตรงๆ ในแท็บใหม่เลย ถ้ามันเป็น script-level
exception จะเห็นข้อความ error ยาวๆ พร้อมเลขบรรทัด/ชื่อไฟล์ทันที ไม่ต้องเดา

**หลังจากนั้นผู้ใช้รายงานว่า "โมดูลตรวจสอบบิลช้าไปหมด กดแล้วรอตลอด"** — ถามแยกให้ชัดว่าช้าจุดไหน (ตอนรอ AI อ่านบิล
ซึ่งช้าโดยธรรมชาติ 45-100+ วิเสมอมา vs จุดอื่นที่ควรเร็ว) ผู้ใช้ยืนยันว่า**ไม่เกี่ยวกับ Gemini** เป็นจุด "ดูรายการบิล
รอตรวจสอบ" กับขั้นตอนอื่น — ไล่โค้ดเจอ 2 จุดจริงใน `BillCapture-Code.gs`:
1. `getPendingBillReceipts()` ไม่เคยมีแคชเลยตั้งแต่ไฟล์เดิมก่อนแยกโปรเจกต์ด้วยซ้ำ (ต่างจากทุกหน้าอื่นในแอปหลักที่มี
   แคชกันหมดแล้ว) — เพิ่มแคช 30 วิ + เคลียร์ทันทีใน `submitBillForReview`/`finalizePurchaseReceipt` (ทั้งคู่แก้ไข
   รายการ pending)
2. `getBillPhotoFolderForSupplier()` ไล่ `getFoldersByName()` 2 ชั้น (ปี→ซัพพลายเออร์) ทุกครั้งที่ถ่ายบิล ทั้งที่
   โฟลเดอร์แทบไม่เปลี่ยนเลยหลังสร้างครั้งแรก — เพิ่มแคช Drive folder ID ต่อซัพพลายเออร์ 1 ชม. (fallback หาใหม่ถ้า
   cache เพี้ยน/โฟลเดอร์ถูกลบ)

**ทดสอบแล้ว**: unit test 4 เคสใหม่ (cache hit ไม่อ่านชีตซ้ำ, invalidate ทันทีตอน submit/finalize, ไม่ค้นหาโฟลเดอร์
Drive ซ้ำครั้งที่ 2 ของซัพพลายเออร์เดียวกัน) + test เดิม 10 เคสผ่านหมดไม่มี regression — push แล้ว (commit `f443f6d`)
ผู้ใช้ deploy แล้ว **ยังไม่ได้รับการยืนยันผลจริงว่าเร็วขึ้นตามที่ต้องการหรือยัง** ถ้าคุยเรื่องความเร็วโมดูลนี้ต่อ ให้ถามผล
ก่อนเป็นอันดับแรก

⚠️ **ของเดิมที่ยังค้างอยู่เหมือนเดิม ไม่เปลี่ยนแปลง**: `Code.gs` หลักยังไม่ได้ deploy ทับ (ไม่เร่งด่วน) + สมมติฐาน
เรื่องโควต้า Apps Script (การถ่ายบิลแย่งทรัพยากรกับหน้าอื่น) ยังไม่ได้ทดสอบยืนยัน/ปัดตกจริง — ทั้งสองเรื่องนี้แยกจาก
บั๊ก SPREADSHEET_ID และเรื่องแคชที่เพิ่งแก้ในรอบนี้โดยสิ้นเชิง คนละสาเหตุกัน อย่าเข้าใจผิดว่าแก้เรื่องแคชแล้วจะไป
ยืนยัน/ปัดตกสมมติฐานโควต้าไปด้วย (คนละกลไกกันคนละคำถามกัน)

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69 — ต่อ 3) — โปรเจกต์ BillCapture มีโค้ดผิด, perf หน้าสั่งของ, บั๊ก factor หาย, ออโต้เซฟ

**บริบท**: session ต่อเนื่องยาวมาก เริ่มจากไล่ทำไม "บิลรอตรวจสอบ" ยัง Load failed อยู่ทั้งที่ deploy caching fix
ไปแล้วเมื่อ session ก่อน จบด้วยการแก้บั๊กจริงหลายจุดในหน้ารีวิวบิล — ผู้ใช้หงุดหงิดหนักหลายรอบระหว่างทาง
(โดยเฉพาะเรื่อง SPREADSHEET_ID ที่ผมทำพลาดเอง 2 ครั้งติด) **อ่านหัวข้อนี้ให้ครบก่อนแตะฟีเจอร์ถ่ายบิล/รีวิวบิลต่อ**

**1. โปรเจกต์ Apps Script "Samurai BillCapture" มีโค้ดผิดวางอยู่ — ไม่ใช่ปัญหา permission/CORS อย่างที่สงสัยตอนแรก**
ไล่ยาวมาก (URL คืน "Sorry, unable to open the file", ปัญหาบัญชี Google ซ้อนกัน `/u/1/` ที่ทำให้งงไปพักหนึ่ง) สุดท้ายพบว่า
โปรเจกต์ "Samurai BillCapture" (คนละโปรเจกต์กับ "Samurai Stock-check Web App" หลัก — เช็ค `script.google.com/home`
ถ้าสงสัยว่ามีกี่โปรเจกต์) มีโค้ดของ `Code.gs` หลัก (ตัวที่ `getActiveSpreadsheet()`) วางอยู่ผิดที่ ไม่ใช่
`BillCapture-Code.gs` ตัวจริง (ที่ต้อง `SpreadsheetApp.openById(SPREADSHEET_ID)`) — แก้โดยแปะโค้ดที่ถูกต้องทับ

⚠️ **ระหว่างแก้ ผมเองทำ SPREADSHEET_ID พังซ้ำ 2 รอบ** (ตรงกับกติกาข้อ 5 ที่เพิ่งเขียนเพิ่มในหัวข้อด้านบนของไฟล์นี้
หลังเหตุการณ์นี้เอง): รอบแรกส่งไฟล์จาก repo ที่ยังเป็น placeholder ทับของจริงที่ผู้ใช้กรอกไว้เอง รอบสองพิมพ์ ID กลับ
จากการอ่านสกรีนช็อตแต่อ่านตัว `l`/`1` ผิดที่ตัวท้าย (`...K4Xs0Z1c` ผิด vs `...K4Xs0Zlc` ถูก) สุดท้ายแก้ได้เพราะขอให้
ผู้ใช้ copy-paste URL ของ Google Sheet จริงมาให้เทียบเอง — **ห้ามพิมพ์ ID ยาวๆ จากภาพเองอีกเด็ดขาด ขอ copy-paste เสมอ**
ID ที่ถูกต้องตอนนี้ (ล็อกไว้ในทั้ง repo และ deployment แล้ว): `1649bCLwyWnfJyjmIQn54738Wv0vyDh3ITF-K4Xs0Zlc`

**Deploy จริงสำเร็จแล้ว** — ทดสอบเปิด URL ตรงๆ (`?action=pendingBills`) ได้ JSON ปกติ, ทดสอบผ่านแอปจริงก็ใช้งานได้แล้ว

**2. Backlog 101 ใบ — ผู้ใช้ตัดสินใจไม่ทำแล้ว** จะให้ `ProductAlias` เรียนรู้จากบิลใหม่ที่ถ่ายไปเรื่อยๆ ตามปกติแทน
**อย่าหยิบเรื่อง backlog 101 ใบขึ้นมาเสนออีกโดยไม่ถูกถาม** (ปิดเคสแล้ว ต่างจากที่บันทึกไว้ก่อนหน้าว่า "ยังไม่เริ่ม")

**3. Performance หน้า "สั่งของ" — เจอ 2 จุดจริง แก้แล้ว push แล้ว (commit `87df099`)**:
- `renderOrderPage()` เดิมยิง `apiGet('orderPageData')` ใหม่ทุกครั้งที่เข้าหน้านี้ ไม่มี cache เลย (ต่างจากฝั่งลูกจ้าง
  ที่มี `stockStatusCache`/`STOCK_STATUS_TTL` อยู่แล้ว) ระหว่างสั่งของหลายเจ้าในรอบเดียว (สั่งเจ้า A → ข้อความ →
  เสร็จสิ้น → กลับมาหน้าสั่งของ → เลือกเจ้า B) แต่ละรอบคือ round-trip เต็มๆ ใหม่ — เพิ่ม `ensureOrderPageData()`
  แคช TTL เดียวกับ `STOCK_STATUS_TTL` (45 วิ) ใช้ `stockStatusCache` ร่วมกับฝั่งลูกจ้างเลย
- ปุ่ม "ยกเลิกงด" (สินค้าที่งดไว้หลายวันไม่ติดกัน) เดิมยิง `Promise.all` พร้อมกันทุกวันที่ — pattern เดียวกับที่เคย
  ทำให้ Apps Script คิวช้ามาก่อน (เหตุผลเดิมที่ทำให้ต้องรวม 4 คำขอเป็น `orderPageData`) เปลี่ยนเป็นยิงทีละคำขอแทน

**4. `checkDuplicateBill()` ใน `BillCapture-Code.gs` ไม่มีแคชเลย — เพิ่มแล้ว push+deploy แล้ว (commit `c847296`)**:
เพิ่ม `getDuplicateCandidates(supplierId)` แคช fingerprint ของบิลเก่า (`PendingBillReceipts`+`PurchaseReceipts`)
10 นาทีต่อซัพพลายเออร์ (pattern เดียวกับ `getProductAliasIndex`) invalidate ทันทีใน `submitBillForReview`/
`finalizePurchaseReceipt` — **เฉพาะช่วยความเร็วตอนสแกนบิลเท่านั้น ไม่เกี่ยวกับความเร็วหน้าอื่นในแอปเลย** (เคยอธิบาย
ผิดจุดกับผู้ใช้ตอนแรกจนต้องแก้ความเข้าใจ)

**5. หน้ารีวิวบิล — 3 การแก้ไข push แล้วทั้งหมด (commit `04d48c3`, `cb42cc1`, `cb8d94b`)**:

a) **ปุ่ม "ข้ามรายการนี้" ตอนนี้ขึ้นทุกรายการ ไม่ใช่แค่ที่ AI เดาว่าไม่ใช่สินค้า** — เจอ use case จริง: บิลจากเจ้าที่
   มีสินค้ามาตรฐานสำหรับทุกร้าน (เช่นลุงทวี) แต่บางร้านไม่ได้สั่ง/ไม่มีสินค้าตัวนั้นในระบบตัวเองเลย รายการแบบนี้
   หน้าตาเหมือนสินค้าจริง (ไม่ใช่ `likelyNonProduct`) แต่ไม่มีอะไรให้จับคู่ได้ เดิมติดค้างกดบันทึกไม่ได้เลย —
   ตอนนี้กดข้ามได้ (ไม่นับต้นทุนรายการนั้นเลย เหมือนรายการ "ไม่ใช่สินค้า" เดิม)

b) ⚠️ **บั๊กจริงที่สำคัญ — ช่องกรอก "อัตราแปลงหน่วย" หายทุกครั้งที่มีการรีเฟรชรายการ** (`drawAll()`) ไม่ว่าจะกด
   เปลี่ยนจับคู่/กดข้ามรายการไหนก็ตาม (ไม่ใช่แค่รายการที่กำลังแก้) — สาเหตุคือ `<input class="factorInput">`
   ไม่เคยมี `value="${it.factor}"` ผูกไว้เลยตั้งแต่แรก (ต่างจากช่องอื่นทุกช่องในการ์ดที่ผูกค่าถูกต้องหมด) ข้อมูลจริง
   ไม่ได้หายไป (`it.factor` ยังถูกต้องใน state) แค่ไม่โชว์ในกล่อง ทำให้เข้าใจผิดว่าข้อมูลหาย — **ไล่ทั้งไฟล์หาช่อง
   input อื่นที่ไม่มี value binding แบบเดียวกันแล้ว ไม่พบจุดอื่นเลย เป็นจุดเดียวที่หลุด** แก้แล้วทั้ง 2 จุด (หน้ารีวิว
   เดี่ยว + โหมดหลายบิล)

c) **เพิ่มออโต้เซฟ localStorage** สำหรับหน้าตรวจสอบบิลค้าง (`owner-resume` เท่านั้น คีย์ด้วย `billResumeBatchId`)
   — บิลใหญ่ (ลุงทวี 20-30+ รายการ) กว่าจะจับคู่ครบใช้เวลานาน กลัวปัดนิ้ว/ออกจากหน้าโดยไม่ตั้งใจแล้วงานหาย ตอนนี้
   บันทึกทุกครั้งที่แก้อะไร กลับเข้ามาใหม่ (แม้ปิดแท็บ/รีสตาร์ทเบราว์เซอร์) ข้อมูลยังอยู่ครบ ล้างทิ้งอัตโนมัติหลัง
   กด "บันทึกต้นทุน" สำเร็จจริง — **ไม่ได้ทำกับโหมดหลายบิล** (ยังไม่มีรายงานปัญหานี้ที่นั่น + ไม่มี batchId ให้คีย์
   ก่อน submit)

**ทดสอบทุกข้อ**: Playwright E2E จริงทั้งหมด (mock `page.route()`) รวมถึงเทสที่พิสูจน์ auto-save รอดจาก
`page.reload()` จริง (ไม่ใช่แค่ SPA state) และเทสที่รัน against โค้ดก่อนแก้เพื่อพิสูจน์ว่า catch บั๊กได้จริงก่อน
ยืนยันว่า fix ได้ผล (ดู pattern นี้ไว้ใช้ต่อ — รันเทสกับโค้ดเก่าก่อนเพื่อพิสูจน์ regression test ใช้งานได้จริง)

⚠️ **เสนอไปแล้วแต่ผู้ใช้ปฏิเสธ — อย่าหยิบขึ้นมาอีกโดยไม่ถูกถาม**: แนวคิด "อ่านยอดรวมท้ายบิลเขียนมือ + โชว์ยอดเทียบสด
ระหว่างกรอก" (เพื่อช่วยหาความผิดพลาดในบิลที่มี 50-60 รายการ) — ทำ Artifact demo ให้ดูแล้ว ผู้ใช้บอกว่า "ดูยืดเยื้อ
เยิ่นเย้อเลอะเทอะเปรอะเปื้อนหน้าจอ" ไม่เอา ปิดเรื่องนี้แล้ว **ไม่ได้แก้อะไรในแอปจริงเลยสำหรับเรื่องนี้**

### สถานะล่าสุด (อัปเดต 12 ก.ย. 69) — ปุ่ม "ไปที่รายการที่ยังไม่จับคู่" + ไล่เคสบิลซ้ำจริงที่ค้างใน Pending

**บั๊ก/ฟีเจอร์ที่ผู้ใช้รายงาน**: บิลรายการเยอะ (ลุงทวี) ตรวจแล้วระบบบอกว่ายังไม่จับคู่อีก 1 รายการ แต่หาด้วยตาเปล่า
ในลิสต์ 50-60 รายการไม่เจอว่าเป็นตัวไหน (มักเป็นรายการ fuzzy-match ที่ AI เดาให้แล้วแต่ยังไม่มีใครกด "ใช่ ยืนยัน" —
`isResolved()` นับว่ายังไม่เสร็จจนกว่าจะ confirm เอง) — **แก้แล้ว push แล้ว (commit `1424574`)**:
- เพิ่มปุ่ม **"ไปที่รายการที่ยังไม่จับคู่"** ในแถบล่าง (`.review-footer`) ของหน้าตรวจสอบบิล ขึ้นเฉพาะตอนมีรายการค้าง
  กดแล้วเลื่อน+ไฮไลต์กะพริบ (`.jump-highlight`, keyframe `jumpPulse`) ไปหารายการนั้นทันที กดซ้ำวนไปรายการถัดไป
  (โชว์ตัวนับ "(1/2)" เมื่อค้างมากกว่า 1 รายการ) จับคู่ครบแล้วปุ่มหายเองกลับเป็น "จับคู่ครบแล้ว" เหมือนเดิม
- ทำ Artifact demo ให้ดูก่อนตามธรรมเนียม (จำลองบิลลุงทวี 26 รายการ, ผู้ใช้ตอบ "ดีมาก ขอเดโม่" แล้ว "ดีมาก ทำเลย")
  ค่อย implement จริงในทั้ง `renderBillReview` (บิลเดี่ยว) และ `renderBillBatchItemReview` (โหมดหลายบิล) — โครง
  เหมือนกันทั้งคู่ (แต่ละจุดมี `jumpCursor`/`unresolved*Items()`/`jumpToUnresolved*Item()` แยกชุดของตัวเอง ตาม
  pattern โค้ดซ้ำที่มีอยู่แล้วระหว่างสองโหมดนี้) — เพิ่ม `card.id = 'card-'+it._id` ให้ทุก item-card เพื่อ scroll target ได้
- ทดสอบผ่าน Playwright E2E จริงทั้ง 2 โหมด (โหมดหลายบิลทดสอบโดยตั้ง `state.billBatchGroups`/`billBatchActiveIndex`
  ตรงๆ ผ่าน `page.evaluate` แล้วเรียก `go('billBatchItemReview')` เอง ข้ามขั้นตอนถ่ายรูป/`analyzeBillBatch` ที่
  sandbox ทำไม่ได้) + รันซ้ำเทสเดิมทั้ง 5 ไฟล์ (autosave, factor-persist ×2, view-photo, skip-any-item) ผ่านหมด
  ไม่มี regression — ระหว่างทางเจอ race condition ในตัวเทส autosave เอง (เช็ค localStorage เร็วเกินไปก่อน
  `clearDraft()` ในเบราว์เซอร์ทำงานทัน ไม่ใช่บั๊กแอป) แก้เทสให้รอ `.success-wrap` ก่อนเช็คแทน
- เป็นการแก้ฝั่ง `stock-check.html` ล้วนๆ (ฝั่ง client ทั้งหมด) ไม่กระทบ `Code.gs`/`BillCapture-Code.gs` เลย ไม่ต้อง
  deploy Apps Script อะไรเพิ่ม แค่รีเฟรชแอปก็ใช้ได้ทันทีหลัง push

**เคสบิลซ้ำจริงที่ค้างใน `PendingBillReceipts`** — ผู้ใช้ส่งสกรีนช็อตมาว่าเห็นบิล 1 ใบ (BatchID `RB0911-...`,
SupplierID S006, BillNumber `IV26090454`) ค้างอยู่ในคิว "บิลรอตรวจสอบ" ทั้งที่บอกว่า "verify ไปแล้วว่าซ้ำ" — ไล่จน
ยืนยันสาเหตุครบ (ไม่ใช่แค่เดา):
- เช็คโค้ด `finalizePurchaseReceipt` (`BillCapture-Code.gs` บรรทัด 663: `if (pendingRowIdx !== -1)
  pendingSh.deleteRow(pendingRowIdx + 1);`) ยืนยันว่าถ้า finalize (กด "บันทึกต้นทุน") สำเร็จ แถวจะถูกลบออกจาก
  `PendingBillReceipts` ทันทีเสมอ — **แถวที่ยังอยู่ในชีต = หลักฐานเชิงโครงสร้างว่ายังไม่เคย finalize จริง** ไม่ใช่
  แค่ข้อสันนิษฐาน
- ผู้ใช้เช็ค `PurchaseReceipts` แล้วเจอ `BillNumber IV26090454` อยู่ในนั้นจริง — ยืนยันทฤษฎีที่ตั้งไว้ครบ: ลูกจ้าง
  ถ่ายบิลใบเดียวกัน 2 รอบ รอบแรก finalize จบแล้ว (อยู่ใน `PurchaseReceipts`) รอบสองถูกฟีเจอร์เตือนบิลซ้ำ
  (`checkDuplicateBill`, เพิ่มไป 10 ก.ย. 69) จับได้ถูกต้อง แต่ค้างใน Pending เพราะไม่มีทางทิ้งมันออกจากคิวได้เลย
  นอกจากลบมือใน Sheets — ระบบทำงานถูกต้องตามที่ออกแบบไว้ทุกจุด ไม่ใช่บั๊ก
- บอกผู้ใช้แล้วว่าแถว `RB0911-...` ลบออกจาก `PendingBillReceipts` ได้เลยปลอดภัย 100% (ไม่กระทบ `ProductAlias`
  เพราะไม่เคย finalize ไม่กระทบ `PurchaseReceipts` เพราะต้นทุนถูกบันทึกจากรอบแรกไปแล้ว) — **ไม่ทราบว่าผู้ใช้ลบแถว
  นี้ในชีตเองแล้วหรือยัง ณ ตอนจบ session** (sandbox ต่อ `google.com` ไม่ได้เลย เช็ค/ลบเองไม่ได้ ต้องถามผู้ใช้ต่อ)

⚠️ **ค้างให้ตัดสินใจ — ถามไปแล้ว 2 รอบยังไม่ได้คำตอบ**: เสนอเพิ่มปุ่ม **"ทิ้งบิลนี้ (ไม่บันทึก)"** ในหน้ารีวิวบิล
(โผล่เฉพาะตอนมี `duplicateBillWarning` ขึ้นเตือน) ให้เจ้าของทิ้งบิลที่ยืนยันว่าซ้ำออกจากคิวได้จากในแอปเลย ไม่ต้อง
เปิด Sheets มาลบมือทุกครั้งที่ลูกจ้างถ่ายซ้ำ — **ยังไม่ได้ implement เลย รอผู้ใช้ตอบว่าจะทำหรือไม่** ถ้าคุยเรื่องบิล
ซ้ำ/บิลค้างใน Pending ต่อ ให้หยิบคำถามนี้มาถามใหม่ก่อนเริ่มแก้อะไร

### สถานะล่าสุด (อัปเดต 13-15 ก.ย. 69) — ไล่บั๊ก "เข้าแอปไม่ได้" จนพบว่าเป็นแคช Safari + เพิ่มแคชหน้าบิลรอตรวจสอบ

**บริบท**: ผู้ใช้รายงานเปิดแอปไม่ได้ตอนเช้า ("เล่นอย่างอื่นได้ปกติ ยกเว้นเข้าแอปนี้ไม่ได้") ไล่จนจบพบว่า **ไม่ใช่บั๊ก
โค้ด ไม่ใช่เน็ตของร้าน**:
- เช็ค git log ก่อนแล้วว่าไม่มีการแก้ไฟล์ที่เกี่ยวกับการเชื่อมต่อเลยก่อนหน้านี้ ตัดข้อสงสัยเรื่องโค้ดพังออกไปได้
- ให้เปิด `?action=bootstrap` ตรงๆ ในเบราว์เซอร์ปกติ เจอหน้า error ของ **Google Drive เอง** ("ไม่สามารถเปิดไฟล์ได้
  ในเวลานี้") + ในแอปเจอ error client-side "The string did not match the expected pattern" (อาการเดียวกับที่เคย
  เจอค้างไว้ตั้งแต่ 5/7 ก.ย. 69 — คราวนี้สรุปสาเหตุได้ชัดขึ้น: เกิดตอน `res.json()` พยายาม parse หน้า HTML error ของ
  Google ที่ได้แทน JSON)
- **จุดเปลี่ยนสำคัญ**: ให้ลองเปิดผ่าน **incognito** → เข้าได้ปกติทันที ขณะที่โปรไฟล์ Safari ปกติยังพังอยู่ — สรุปได้ว่า
  เป็น **แคช/Google account session ที่ค้างอยู่ในโปรไฟล์ Safari ปกติของเครื่องนั้นเอง** (คุกกี้/session เก่าที่ผูก
  กับเบราว์เซอร์ไปชนกับตอนต้อง auth เข้า Drive) ไม่ใช่ Google ล่มทั้งระบบ (เพราะ incognito ก็ต้องเจอเหมือนกันถ้าใช่)
  ไม่มีอะไรในโค้ดแอปที่ไปสั่งลบคุกกี้/session ของเว็บไซต์อื่นแทนผู้ใช้ได้เลย (ข้อจำกัดความปลอดภัยของเบราว์เซอร์เอง)
- **วิธีแก้ที่เครื่อง (ไม่เกี่ยวกับโค้ด)**: ล้าง Website Data ของ `script.google.com`/`script.googleusercontent.com`
  ใน Safari Settings หรือลบ+เพิ่มไอคอน/แท็บปักหมุดใหม่ — **ถ้าเจอรายงาน "เข้าแอปไม่ได้" อีกในอนาคต ให้ลอง incognito
  เทียบก่อนเป็นอันดับแรกเสมอ** (ถ้า incognito เข้าได้ = ปัญหาแคช/session ของเครื่อง ไม่ต้องรีบแก้โค้ด)

⚠️ **ยังไม่ได้ยืนยันว่าผู้ใช้ล้างแคชแล้วหายจริงหรือไม่** — ถ้าคุยเรื่องนี้ต่อให้ถามผลก่อน

**ฟีเจอร์ที่ทำเพิ่ม**: ระหว่างคุย ผู้ใช้บ่นว่าหน้า "บิลรอตรวจสอบ" เปิดช้า (ไม่ error แค่รอนาน) — ระบุสาเหตุว่าเป็น
**Apps Script cold start ของโปรเจกต์ BillCapture** (ถูกเรียกใช้ห่างๆ กว่าโปรเจกต์หลักมาก โดนพักเครื่องยนต์บ่อย)
แคช backend เดิม (`CACHE_TTL.pendingBills = 30 วิ`) ช่วยแค่เปิดซ้ำถี่ๆ เท่านั้น ไม่ช่วยตอนเปิดครั้งแรกของรอบที่เจอ
cold start ทุกที — **แก้แล้ว push+merge เข้า `main` แล้ว** (commit `a7f9998` merge เป็น `d8485ba`):
- `stock-check.html`/`renderBillPendingList`: เพิ่ม client-side cache ด้วย `sessionStorage`
  (`samurai_pending_bills_cache_v1`) แบบเดียวกับ pattern ของ `bootstrap()` — เปิดหน้านี้ครั้งต่อไปโชว์รายการล่าสุด
  ที่เคยโหลดได้**ทันที** แล้วรีเฟรชเงียบๆ เบื้องหลัง ถ้ารีเฟรชพังไม่รบกวนหน้าจอที่โชว์ cache อยู่แล้ว (ปล่อยผ่านเงียบๆ)
  ครั้งแรกสุด (ไม่มี cache เลย) ยังคงพฤติกรรมเดิมทุกอย่าง (retry 10วิ→25วิ + ปุ่ม "ลองใหม่")
- เป็นการแก้ฝั่ง client ล้วนๆ ไม่ต้อง deploy Apps Script อะไรเพิ่ม
- `node --check` ผ่าน — **ยังไม่มี Playwright E2E สำหรับจุดนี้โดยเฉพาะ** (ต่างจากฟีเจอร์อื่นๆ ก่อนหน้าที่มักมี E2E
  ประกบด้วยเสมอ — ถ้าจะแก้ต่อจุดนี้ควรเพิ่มเทสให้ครบตามธรรมเนียมเดิม)
- ⚠️ **ยังไม่ได้รับการยืนยันจากผู้ใช้ว่ารู้สึกเร็วขึ้นจริงหรือไม่** — ถามไปแล้วรอผลอยู่ ถ้าคุยเรื่องนี้ต่อให้ถามผล
  ทดสอบจริงก่อนเป็นอันดับแรก

⚠️ **หมายเหตุ workflow เรื่อง push**: session นี้ถูก harness กำหนดให้พัฒนา/push เข้า branch เฉพาะ
(`claude/duplicate-bill-discard-button-b8vclt`) แทนที่จะ push ตรงเข้า `main` ตามธรรมเนียมเดิม — พอผู้ใช้สั่ง
"push"/"merge" ต้องเช็คว่า branch ที่ push ไปจริงๆ ถึง `main` หรือยัง (ใช้ `git merge --ff-only origin/main` แล้ว
merge branch ของงานเข้าไปแล้ว push `main` อีกที) **ถ้า session ถัดไปเจอ setup เดียวกัน (มี branch ที่กำหนดมาให้ใน
system prompt) ให้ทำตามขั้นตอนเดียวกันนี้ — commit+push ขึ้น branch นั้นก่อน แล้วค่อย merge เข้า `main` แยกต่างหาก
ไม่ใช่ push ตรงเข้า `main` เลยเหมือนที่เคยทำมาตลอดก่อนหน้านี้**

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69) — พบต้นตอจริงของ "เข้าแอปไม่ได้"/ค้างแบบสุ่ม: trigger `syncProductUnits` ที่ไม่เคยถูกบันทึกไว้

**บริบท**: ผู้ใช้ (เจ้าของร้าน ใช้ iOS) รายงานว่าเข้าแอปไม่ได้บ่อยๆ แบบสุ่ม แต่เด็กที่ร้าน (ใช้ Android) ไม่เคยบ่น —
ไล่ทีละสมมติฐาน (แคช Safari, บัญชี Google ซ้อน, เน็ต WiFi/มือถือ) ตัดออกไปทีละข้อเพราะไม่ตรงกับข้อเท็จจริงที่ผู้ใช้
ยืนยัน (เปิดลิงก์ใหม่ทุกครั้ง, ไม่มี Google login ใน Safari, incognito ก็ยังช้า, เกิดทั้ง WiFi และมือถือ) จนกระทั่ง
ผู้ใช้ยิง URL ของ backend ตรงๆ ได้ข้อมูลเร็วปกติ (ยืนยันว่าเน็ต/DNS ไปหา `script.google.com` ไม่มีปัญหาเลย) —
ระหว่างเปิดหน้า Executions log ดูสดๆ ตอนกำลังค้างจริง (`เข้าเช็คของลุงทวี` ผ่านหน้า "สั่งของ") พบว่ามีแถว
**`syncProductUnits` (Time-Driven trigger)** สถานะ **"Running" ค้างนานผิดปกติ (เกิน 1 นาที ทั้งที่ปกติทุก action
เสร็จใน 0.3-4 วิ)** ชื่อฟังก์ชันนี้ไม่เคยถูกบันทึกไว้ในไฟล์นี้เลยสักครั้ง

**ไล่ที่มา**: `git log -S "syncProductUnits" -- Code.gs` เจอว่าฟังก์ชันนี้มีอยู่แล้วตั้งแต่คอมมิตแรกที่เอา `Code.gs`
เข้า repo (`c4d1d06`, 30 ส.ค. 69, ผู้เขียน Claude) — แปลว่า**เป็นฝีมือ session ก่อนหน้าของ Claude เองที่เขียนฟังก์ชันนี้
และน่าจะขอให้ผู้ใช้ไปตั้ง trigger เอง (เหมือน pattern `keepWarm()` ของแอปสั่งของ) แต่ลืมบันทึกไว้ใน CLAUDE.md** —
**ไม่ใช่ความผิดของผู้ใช้เลย** (ผู้ใช้ไม่มีความรู้ coding ไม่มีทางตั้ง trigger เองได้อยู่แล้ว)

**สาเหตุที่ trigger นี้ทำให้แอปค้าง**: `syncProductUnits()` (`Code.gs` บรรทัด ~1261) ถูกออกแบบมาเป็นงาน **backfill
ครั้งเดียว** (สินค้าไหนมีหน่วยครบ 2 แถวใน `ProductUnits` แล้วจะข้าม ไม่ทำอะไรต่อ) แบบเดียวกับ `backfillOrderLabels()`
ที่เคยบันทึกไว้ว่า "รันครั้งเดียวจบ" — **แต่กลับถูกตั้งเป็น time-based trigger ให้รันซ้ำตามตารางเวลาตลอดไป** ทั้งที่
สินค้าเก่าถูก backfill ครบหมดแล้วไม่มีประโยชน์อะไรเพิ่ม แต่ยังคงสแกนทั้งชีต `Product`+`ProductUnits` ทุกรอบที่รัน —
Google Sheets มีพฤติกรรมล็อกภายในอัตโนมัติเวลามี script เขียนชีตพร้อมกับอีก script ที่กำลังอ่าน/เขียนชีตเดียวกัน
(ไม่ต้องมี `LockService` ก็เกิดได้) ทำให้ request จริงของแอป (`bootstrap`, `checkPin`, `orderPageData` ฯลฯ) ที่บังเอิญ
เข้ามาพร้อมกับรอบที่ trigger กำลังรันอยู่ ต้องรอคิวจนค้าง/timeout — อธิบายได้ครบทุกอาการที่แปลกก่อนหน้านี้: **เกิด
แบบสุ่ม** (ตามตารางเวลาของ trigger เอง ไม่เกี่ยวกับว่าเด็กที่ร้านเพิ่งใช้แอปหรือเปล่า), **เกิดทั้ง WiFi/มือถือ** (เพราะ
เป็นปัญหาฝั่ง server ไม่ใช่เครือข่าย), **เด็กที่ร้านไม่ค่อยเจอ** (อาจแค่จังหวะไม่ชน หรือเจอแต่ไม่ทันสังเกต/หน้าที่เด็ก
ใช้บ่อยมี retry ซ่อนไว้อยู่แล้วที่ `bootstrap()` ทำให้ self-heal เงียบๆ)

**Error `The string did not match the expected pattern`** ที่เคยเจอค้างคาไว้หลายรอบในไฟล์นี้ (5 ก.ย., 7 ก.ย., และ
ล่าสุด 15 ก.ย. ที่หน้ากรอกรหัสเจ้าของ) **น่าจะเป็นอาการเดียวกันนี้ทั้งหมด** — เกิดตอน `res.json()` พยายามแปลงคำตอบที่
ไม่ใช่ JSON สะอาด (เช่น หน้า error/timeout ที่ Google ส่งกลับมาแทน) ให้เป็น object

**แก้แล้ว (ยืนยันผลจริงแล้ว 15 ก.ย. 69)**: ผู้ใช้ลบ trigger `syncProductUnits` ออกจากหน้า Triggers ของ Apps Script
Editor (โปรเจกต์ "Samurai Stock-check Web App" — ตัวเดียวกับที่รัน `Code.gs` หลัก) — หลังลบ เข้าหน้ากรอกรหัสเจ้าของ
ที่เพิ่งค้างอยู่ได้ทันที **ฟังก์ชัน `syncProductUnits()` ยังอยู่ใน `Code.gs` เหมือนเดิม ไม่ได้ลบโค้ดออก** แค่ไม่มี
trigger auto-run แล้ว — ถ้าในอนาคตมีการเพิ่มสินค้าใหม่จำนวนมากที่ต้องการ backfill หน่วยเล็ก/ใหญ่ ให้กดปุ่ม Run รัน
ฟังก์ชันนี้**ด้วยมือครั้งเดียว**จากหน้า Apps Script Editor เท่านั้น **ห้ามตั้งเป็น trigger ซ้ำอีกเด็ดขาด**

⚠️ **บทเรียนสำคัญสำหรับ session ถัดไป**: ถ้าเจอรายงาน "เข้าแอปไม่ได้"/"ค้าง"/timeout แบบสุ่มอีกในอนาคต (ทั้งแอปเช็ค
สต๊อกและแอปสั่งของ เพราะเป็นความเสี่ยงเชิงโครงสร้างเดียวกัน) **ให้เช็คหน้า Triggers ของ Apps Script Editor โปรเจกต์
ที่เกี่ยวข้องเป็นอันดับต้นๆ เสมอ** (คู่กับการเช็ค Executions log แบบ real-time ตอนที่กำลังค้างอยู่จริง — ถ้าเห็นแถว
สถานะ "Running" ค้างนานผิดปกติเทียบกับ action อื่นๆ ที่ปกติเสร็จในไม่กี่วินาที ให้สงสัย trigger ตัวนั้นก่อนเลย) —
ไม่ใช่แค่เดาว่าเป็นปัญหาเน็ต/แคช/อุปกรณ์เหมือนที่เคยสรุปผิดมาหลายรอบก่อนหน้านี้

⚠️ **ยังไม่ยืนยัน 100%**: เป็นการทดสอบสดครั้งเดียว (ลบ trigger → เข้าได้ทันที) หลักฐานหนักแน่นมากแต่ยังไม่ได้เฝ้าดูผล
ระยะยาวว่าอาการค้างหายไปจริงถาวรหรือไม่ — session ถัดไปควรถามผู้ใช้ก่อนว่ายังเจออาการนี้อีกไหมหลังจากนี้

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69 — ต่อ) — แก้หน้า "สั่งของ" หน่วงเวลาแก้ไขเจ้าที่มีของเยอะ (ลุงทวี)

**บริบท**: ผู้ใช้รายงานว่าหน้า "สั่งของ" (`renderOrderPage`) ที่เจ้าของเปิดดู "คงเหลือ" แล้วกดสั่งจริง หน่วงมากเวลา
ทำงานกับลุงทวี (S001, รายการเยอะสุดในร้าน ~50-60 แถวรวมหน่วย) — เกิดกับเจ้าอื่นด้วยแต่ไม่หนักเท่า (ยืนยันจากผู้ใช้ตรงๆ
"หน่วงทุกเจ้าแต่ไม่เท่ากัน") **หมายเหตุ: ไม่ใช่หน้าเดียวกับที่พนักงานกรอกจำนวนสต๊อก (`renderSupplierCheck`) — คนละ
หน้ากันคนละบั๊กกัน อย่าสับสน** (session นี้เข้าใจผิดจุดนี้ไปก่อนหนึ่งรอบ ผู้ใช้ต้องแก้ให้)

**สาเหตุที่พบ**: `renderGroups()` เดิม**รื้อสร้าง DOM ใหม่ทั้งหน้า (ทุกเจ้าทั้งร้าน ~24 เจ้า) ทุกครั้ง** แม้จะแค่กด
ขยาย/ยุบการ์ดเจ้าเดียว, กด "เลือกทั้งหมด", งด/ยกเลิกงดสินค้า 1 ตัว, หรือแก้ชื่อหน่วยสั่งของ 1 ตัว — ปุ่ม +/− และพิมพ์
จำนวนไม่มีปัญหานี้ (แพตช์เฉพาะช่องที่แก้อยู่แล้ว) ต้นทุนของการ rebuild ทั้งร้านนี้เกิดเท่ากันทุกเจ้า แต่**แพงขึ้นตามว่า
การ์ดไหนเปิดค้างอยู่** (ลุงทวีเปิดค้าง = ต้องสร้าง+วาด 50-60 แถวใหม่ทุกครั้ง เจ้าอื่นเปิดค้าง = แค่ไม่กี่แถว) อธิบายได้
ตรงกับที่ผู้ใช้สังเกตว่า "หน่วงทุกเจ้าแต่ไม่เท่ากัน" เป๊ะ

**แก้แล้ว push แล้ว (commit `6c8f3d9`)**: แยกโค้ดสร้างการ์ด 1 เจ้าออกเป็นฟังก์ชัน `buildSupplierCard(s, filter)`
แล้วให้ 4 จุดข้างต้นเรียก `rebuildCard(s)` (หา `[data-supplier-card="..."]` เดิมใน DOM แล้ว `replaceWith()` การ์ดใหม่)
แทนที่จะเรียก `renderGroups()` เต็มรูปแบบ — **ช่องค้นหายังคงเรียก `renderGroups()` แบบเดิมทุกอย่าง ไม่แตะ** เพราะต้อง
กรอง/จัดกลุ่มใหม่ทุกเจ้าจริงๆ เป็นการแก้ฝั่ง client ล้วนๆ ไม่แตะ `Code.gs`/backend เลย ไม่ต้อง deploy อะไรเพิ่ม

⚠️ **ก่อนแก้ ผู้ใช้ขอให้วิเคราะห์ผลกระทบให้ครบก่อนลงมือ** (เคยโดนปัญหา "แก้จุดหนึ่งไปเพิ่มภาระจุดอื่น" มาก่อน — ดู
บทเรียนเรื่อง prefetch ที่ถอนออกใน session 10 ก.ย. 69) — ไล่ grep หาทุกจุดที่เรียก `renderGroups()`/`renderStats()`/
อ่าน `state.orderQty`/`expandedSuppliers`/`orderedProductIds`/`confirmedSupplierIds` ในไฟล์ทั้งหมดก่อน ยืนยันว่า
ลำดับการ์ด/หัวข้อเซกชัน/แถบสถิติด้านบนไม่ขึ้นกับ 4 action ที่แก้เลย จึงปลอดภัยที่จะแพตช์เฉพาะการ์ดโดยไม่กระทบส่วนอื่น
— **การแก้นี้ไม่แตะ backend/API call เลยแม้แต่ครั้งเดียว ไม่มีความเสี่ยงเรื่องโควต้า/infrastructure ร่วมกับแอปอื่นเลย**
(คนละประเภทความเสี่ยงจากบั๊ก `syncProductUnits` ด้านบนโดยสิ้นเชิง)

**ทดสอบแล้ว (ตามกติกาข้อ 4 อย่างเข้มงวดตามที่ผู้ใช้ย้ำ)**: `node --check` ผ่าน + **พิสูจน์ก่อน-หลังด้วย `git stash`**
ว่าโค้ดเดิมทำลาย DOM node ของเจ้าที่ไม่เกี่ยวข้องจริงเวลากดขยายแค่เจ้าเดียว (ยืนยัน bug มีจริง ไม่ใช่แค่เดา) + Playwright
E2E 17 เคสบนโค้ดหลังแก้ผ่านหมด ครอบคลุมขยาย/ยุบ/เลือกทั้งหมด/งด/แก้ชื่อหน่วยของเจ้าหนึ่ง ยืนยันว่าการ์ดเจ้าอื่นไม่ถูกแตะ
เลยสักครั้ง (เช็คด้วย DOM node identity marker) + regression ปุ่ม +/−/ยุบการ์ด/ค้นหายังทำงานถูกต้องเหมือนเดิม

⚠️ **ยังไม่ได้รับการยืนยันจากผู้ใช้ว่ารู้สึกเร็วขึ้นจริงในการใช้งานจริงหรือไม่** — เพิ่ง push ไป ยังไม่มีรายงานผลกลับมา
ถ้าคุยเรื่องความเร็วหน้าสั่งของต่อ ให้ถามผลก่อนเป็นอันดับแรก

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69 — ต่อ 2) — เข้าแอปไม่ได้/หน้าค้างเป็นระยะ: วนลองเชื่อมต่อเงียบๆ ทุกจุด + `keepWarm()`

**บริบท**: ต่อจากหัวข้อ "พบต้นตอจริงของ 'เข้าแอปไม่ได้'... trigger `syncProductUnits`" ด้านบน — หลังลบ trigger นั้นไปแล้ว
ผู้ใช้ยังเจออาการ "เข้าแอปไม่ได้"/ค้างเป็นระยะอีกในวันเดียวกัน (โดยเฉพาะหน้าใส่รหัสเจ้าของ) ไล่ยาวมาก ลองหลายสมมติฐาน
(แคช Google account ซ้อน, VPN, incognito) แต่ตัดออกไปทีละข้อเพราะไม่ตรงกับข้อเท็จจริงที่ผู้ใช้ยืนยัน (ไม่มี Google
login ค้าง, ไม่ได้เปิด VPN, incognito ก็ยังช้า) — ผู้ใช้ย้ำหนักแน่นว่า **"ทุกครั้งที่บอกให้ลองใหม่มันไม่เคยแก้ปัญหาได้
เลย ทางแก้คือต้องเข้าไปดูว่าเกิดปัญหาที่อะไร"** และ sandbox นี้ต่อ `google.com` ไม่ได้เลย (ยืนยัน/เปิดเผยข้อจำกัดนี้
ตรงๆ กับผู้ใช้แล้ว) — สุดท้ายสรุปได้ว่า **ไม่ใช่ปัญหาเดียว** แต่เป็นสาเหตุที่ซ้อนกันหลายชั้น:

**สาเหตุที่ยืนยันได้จริง**: `apiGet()`/`apiPost()` ใช้ `AbortController` กำหนด timeout เอง (`bootstrap()` เดิม 10 วิ
ตามด้วย 25 วิ) แต่การยิง URL ตรงๆ ในเบราว์เซอร์ไม่มี timeout ของตัวเอง รอได้ไม่จำกัด — อธิบายได้ว่าทำไม "ทดสอบยิง URL
ตรงๆ ก็เข้าได้ทุกครั้ง แต่เปิดแอปจริงกลับ error" เพราะ backend แค่ "ช้า" (ไม่ถึงกับพัง) แต่แอปตัดใจเร็วเกินไปแล้วโชว์
error แบบไม่มีทางกู้คืนเอง (ต้องกดปุ่ม/ย้อนกลับ/รีเฟรชเองเท่านั้น) ทั้งที่หน้าจอที่ error หลายจุดไม่เคยมี retry เลย
(ต่างจาก `bootstrap()` ที่มี retry 2 ชั้นอยู่แล้วตั้งแต่ 5 ก.ย. 69)

**แก้แล้ว push แล้ว (`stock-check.html`, commit `0c7837e`→`823abcb` ไล่ตามลำดับ)**:
1. `bootstrap()` (commit `0c7837e`): เปลี่ยนจาก retry 2 ครั้งแล้วโชว์ error เป็น **วนลองเชื่อมต่อเงียบๆ ไม่จำกัดรอบ**
   (`for(;;){ try{...; return; }catch(err){ await new Promise(r=>setTimeout(r,3000)); } }`) — พังกี่ครั้งก็ไม่โชว์
   error/ปุ่มใดๆ ค้างแค่สถานะ "กำลังโหลด" ไว้ ลองใหม่ทุก 3 วิจนกว่าจะสำเร็จ
2. `checkPin()`'s `trySubmit()` ในหน้า `renderOwnerPin()` (commit `7761c94`): pattern เดียวกัน **แต่แยก error เครือข่าย
   ออกจากรหัสผิด** — รหัสผิด (`res.valid===false`) โชว์ "รหัสไม่ถูกต้อง" ทันทีครั้งเดียว ไม่ retry (เทสยืนยันแล้วว่า
   ไม่ loop) ส่วน network error (timeout/abort) เท่านั้นที่วนลองเงียบๆ
3. เพิ่ม `<meta name="apple-mobile-web-app-capable">` +ที่เกี่ยวข้องใน `<head>` — เป็นการลองแก้ตามสมมติฐาน Google
   session ซ้อนที่ภายหลังพิสูจน์ไม่ใช่สาเหตุหลัก **ทิ้งไว้เพราะไม่มีผลเสีย** ไม่ต้องถอนออก
4. **[รอบนี้ — commit ใหม่]** ขยาย pattern เดียวกันไปอีก 4 จุดที่ผู้ใช้อนุมัติ "ทำเลย" ให้ครบ (เดิมทุกจุดนี้ error
   ครั้งเดียวจบ ไม่มี retry เลยสักจุด):
   - `renderBillPendingList`'s `load(silent)` — เดิมมี retry 2 ชั้น (10วิ→25วิ) + ปุ่ม "ลองใหม่" ให้กดเอง เปลี่ยนเป็น
     วนเงียบๆ ไม่จำกัดรอบแทน (ตัดปุ่ม "ลองใหม่" ออก ไม่จำเป็นอีกต่อไป) — **เฉพาะ path ที่ไม่มี cache/foreground load
     เท่านั้น** ที่วนไม่จำกัด — `silent=true` (background refresh ตอนมี sessionStorage cache อยู่แล้ว) ยังคงพฤติกรรม
     เดิม: พังแค่ครั้งเดียวก็ปล่อยผ่านเงียบๆ ไม่ retry ซ้ำ (มี cache โชว์อยู่แล้ว ไม่ต้องรบกวนผู้ใช้)
   - `renderSupplierCheck` (ฝั่งลูกจ้าง กดเข้าเช็คของเจ้าใดเจ้าหนึ่ง) — เดิม error ครั้งเดียวจบ ค้างต้องกดย้อนกลับเอง
   - `renderOwnerStockAnalytics` (หน้า "สต๊อก & วิเคราะห์") — เดิม error ครั้งเดียวจบเหมือนกัน
   - `renderOrderPage`'s `ensureOrderPageData()` call (หน้า "สั่งของ") — เดิม error ครั้งเดียวจบเหมือนกัน

**ทดสอบแล้ว**: `node --check` ผ่าน (extract script ทั้งไฟล์) + Playwright E2E จริง (mock `page.route()`) 15 เคสใหม่
ครอบคลุมทั้ง 4 จุด (ไม่โชว์ error ระหว่าง retry, โหลดสำเร็จหลังพัง 2 ครั้ง, เรียก action ซ้ำอย่างน้อย 3 ครั้ง) รวมถึง
regression เฉพาะของ `renderBillPendingList`: มี cache อยู่แล้วโชว์ทันที + background refresh พังแล้วไม่วนซ้ำไม่จำกัด
(หยุดที่ retry ภายใน 2 ครั้งของ `apiGet` fallback เดิม ไม่ลูปทุก 3 วิเพิ่ม) — รันซ้ำเทสเดิมของ `bootstrap()`/`checkPin()`
(15 เคส) และของ `renderOrderPage`'s `buildSupplierCard`/`rebuildCard` (17 เคส) พร้อมกันด้วย ผ่านหมดไม่มี regression
รวม 47 เคสทั้งหมด

**Backend (`Code.gs`) — เพิ่ม `keepWarm()`**: เพิ่มฟังก์ชันเปล่าๆ (แค่ log เวลา ไม่แตะ Sheet/Cache/Lock ใดๆ) ท้ายไฟล์
(commit `823abcb`) mirror pattern เดียวกับที่แอปสั่งของ (`order-app-Code.gs`) ทำไปแล้วเมื่อ 13 ก.ย. 69 — ต้องตั้ง
time-driven trigger เรียกทุก 5 นาทีเองใน Apps Script Editor (Triggers → Add Trigger → keepWarm → Time-driven →
Every 5 minutes) เพื่อกันสคริปต์ "หลับ" (cold start) ตั้งแต่ต้น — **ส่งไฟล์เต็มให้ผู้ใช้ผ่าน SendUserFile แล้ว
(file_uuid `acfa206b-0f22-4ba4-ab46-0d424c8a56cc`) แต่ ณ ตอนจบ session ยังไม่ได้รับการยืนยันว่าแปะ/deploy/ตั้ง
trigger จริงหรือยัง** ผู้ใช้บอกว่า "ทำเลย เดี๋ยวถึงบ้านแล้วจะไป deploy"

⚠️ **สำคัญ — บทเรียนที่ผู้ใช้ย้ำหนักแน่นมากรอบนี้ ต้องยึดตลอดไป ไม่ใช่แค่ session นี้**: ห้ามแนะนำ "ลองใหม่"/"รีเฟรช"
ให้ผู้ใช้ลองเองโดยไม่มีการสืบสวนจริงก่อนเด็ดขาด ถ้าเจอรายงานปัญหาเชื่อมต่อ/ค้าง/เข้าไม่ได้อีก **ให้บอกตรงๆ ทันทีว่า
sandbox นี้ต่อ `google.com` ไม่ได้เลย ไม่มีทางเช็ค Executions log/Triggers/ยิง API ตรงๆ เองได้** แล้วขอให้ผู้ใช้ช่วยเช็ค
สิ่งเหล่านี้แทน (Executions log ตอนกำลังค้างจริง, หน้า Triggers, ยิง URL ตรงๆ ในเบราว์เซอร์เทียบ) ก่อนเดา/แก้โค้ดต่อ —
อย่าอาศัยการ "ยิง URL ตรงๆ ก็เข้าได้" เป็นหลักฐานว่าแอปไม่มีปัญหา เพราะเบราว์เซอร์ตรงๆ ไม่มี timeout ของตัวเองต่างจาก
แอปที่ตั้ง timeout เอง (ดูสาเหตุด้านบน)

⚠️ **ยังไม่ยืนยัน 100% ว่าแก้ครบทุกจุดแล้วจริง**: หลัง deploy `checkPin()` fix ผู้ใช้รายงานว่า "ไวขึ้นมาก" (สัญญาณบวก
จากการใช้งานจริง) แต่ยังไม่มีการยืนยันเรื่อง 4 จุดที่เพิ่งแก้รอบนี้ (ยังไม่ได้ deploy เพราะรอ push) — session ถัดไป
ถ้าคุยเรื่องนี้ต่อให้ถามผลก่อนเป็นอันดับแรก ถ้ายังเจอปัญหาเชื่อมต่อที่จุดอื่นนอกเหนือ 6 จุดที่แก้ไปแล้วทั้งหมด (bootstrap,
checkPin, renderBillPendingList, renderSupplierCheck, renderOwnerStockAnalytics, renderOrderPage) ให้ grep หา pattern
`}catch(err){` ที่ตามด้วยการโชว์ error message แบบไม่มี retry ในจุดอื่นที่ยังไม่ได้ตรวจ (เช่น `renderSupplierList`
ที่พบระหว่างไล่งานนี้ว่ามี pattern เดียวกันแต่ยังไม่ได้แก้ เพราะไม่อยู่ใน 4 จุดที่ผู้ใช้อนุมัติไว้ตอนนั้น — ถามผู้ใช้ก่อน
ว่าจะแก้เพิ่มไหมถ้าเจอปัญหาที่จุดนั้น)

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69 — ต่อ 3) — ปุ่มกลับหลังบันทึกบิลไม่ต้องใส่ PIN ซ้ำ + บิลซ้ำสตาร์อัพ 3 ใบ → แก้ 3 ชั้น

**บริบท**: ต่อจากหัวข้อ "ต่อ 2" ด้านบน (วนลองเชื่อมต่อเงียบๆ + `keepWarm()` ของ `Code.gs` หลัก) — session นี้ทำต่ออีก
4 เรื่อง เริ่มจาก UX เล็กๆ แล้วขยายไปเจอบั๊กจริงเรื่องบิลซ้ำที่ต้องแก้ถึง 3 ชั้นถึงจะครบ

**1. ปุ่มหลังบันทึกต้นทุนบิล (owner-resume) เดิมพา `go('home')` บังคับใส่ PIN ใหม่ทุกครั้ง — แก้แล้ว push แล้ว
(commit `6c60dee`)**: ผู้ใช้รายงานว่าจับคู่บิลเสร็จ 1 เจ้าแล้วอยากทำเจ้าถัดไปต่อ แต่ปุ่ม "เสร็จสิ้น กลับหน้าแรก" พาไปหน้า
เลือกบทบาท (พนักงาน/เจ้าของ) ต้องกด "จัดการร้าน" แล้วใส่ PIN ใหม่ทุกรอบ — สลับให้ปุ่มหลักเป็น **"จับคู่บิลเจ้าอื่นต่อ"**
ไปหน้า "บิลรอตรวจสอบ" ตรงๆ (ปุ่มรองเปลี่ยนชื่อเป็น "เสร็จสิ้น กลับเมนู" ไป `go('ownerMenu')` ตรงๆ แทน) — ทั้งคู่ไม่ต้องใส่
PIN ซ้ำเพราะ `go()` ไม่มีการเช็คสิทธิ์อะไรเลย (PIN gate เป็นแค่ทางเข้าจาก UI เท่านั้น ไม่ใช่ auth จริงในโค้ด) — โหมดลูกจ้าง
(staff, ส่งบิลเข้าคิวรอตรวจ) ไม่ถูกแตะเลย ยังพา `go('home')` เหมือนเดิม — ทดสอบผ่าน Playwright E2E 7 เคส

**2. โปรเจกต์ Apps Script "BillCapture" ไม่มี `keepWarm()` เป็นของตัวเอง — แก้แล้ว push แล้ว (commit `24c158f`)**:
ผู้ใช้ส่งสกรีนช็อตหน้า "บิลรอตรวจสอบ" ค้างที่ "กำลังโหลด..." นานตอนเปิดครั้งแรก — เช็คแล้วพบว่า `keepWarm()` ที่เพิ่งเพิ่ม
ให้ `Code.gs` หลักไปใน "ต่อ 2" ครอบคลุมแค่โปรเจกต์หลักเท่านั้น **โปรเจกต์ "Samurai BillCapture" เป็นคนละโปรเจกต์
Apps Script แยกต่างหากโดยสิ้นเชิง (คนละโควต้า/cold-start กันเอง)** เพิ่ม `keepWarm()` ของตัวเองให้ `BillCapture-Code.gs`
ด้วย — **ต้องตั้ง time-driven trigger แยกต่างหากในโปรเจกต์ BillCapture เอง ไม่ใช่ตัวเดียวกับที่ตั้งให้ `Code.gs` หลัก**

**3. ผู้ใช้เจอบิลซ้ำจริง 3 ใบของสตาร์อัพ (S009) ค้างในคิว "บิลรอตรวจสอบ" — ไล่จนแก้ครบ 3 ชั้น push แล้วทั้งหมด
(commit `f0c4632`, merge เป็น `5b09c77`)**:

- **ชั้น 1 — เตือนบิลซ้ำฝั่งเจ้าของด้วย** (ถามยืนยันผ่าน `AskUserQuestion` ก่อน): เดิม `checkDuplicateBill()` ทำงานแค่
  ตอนพนักงานสแกนสด (`analyzeBillPhoto`) เท่านั้น — เจ้าของเปิดดูจากคิว "บิลรอตรวจสอบ" ทีหลัง**ไม่เคยเห็นคำเตือนเลย**
  แก้โดยให้ `getPendingBillReceipts()` เรียก `checkDuplicateBill()` ให้ทุกบิลในคิว เทียบกับบิลอื่นในคิวเดียวกัน/ที่
  บันทึกจริงแล้ว (เพิ่ม parameter `excludeBatchId` กันบิลจับคู่กับตัวเอง) — เจ้าของเห็นแบดจ์เตือนเล็กๆ ใน list ทันที
  + แบนเนอร์เต็มพร้อมปุ่ม "ดูรูปบิลเดิม" ตอนเปิดดู — **ข้อจำกัดที่บอกผู้ใช้ไปแล้วตรงๆ**: จับได้เฉพาะเลขที่บิลตรงกันเป๊ะ
  หรือรายการสินค้าซ้ำ ≥70% เท่านั้น ถ้าไม่เข้าเกณฑ์นี้จะไม่เตือนเลย ผู้ใช้เลือก "ไม่ต้องเพิ่มเกณฑ์เตือนอ่อนๆ" (ปฏิเสธ
  ตัวเลือกลดเกณฑ์เหลือ 50% / เตือนแค่ "เจ้าเดียวกันวันเดียวกัน" ที่เสนอไป) — ถ้าจะหยิบเรื่องนี้ขึ้นมาอีกให้ถามใหม่ก่อน
- **ชั้น 2 — ปุ่ม "ทิ้งบิลนี้ (ไม่บันทึก)"**: เพิ่ม action `discardPendingBill(batchId)` ใน `BillCapture-Code.gs`
  (ลบแถวออกจาก `PendingBillReceipts` เท่านั้น ไม่แตะ `PurchaseReceipts`/`ProductAlias` เลย idempotent) + ปุ่มในหน้า
  รีวิวบิล (โหมดเจ้าของ-resume เท่านั้น) ให้ทิ้งบิลที่ยืนยันว่าซ้ำออกจากคิวได้จากในแอปเลย ไม่ต้องเปิด Google Sheet ไปลบมือ
  อีกต่อไป (เคยเป็นคำถามค้างที่ถามไป 2 รอบไม่ได้คำตอบตั้งแต่ 12 ก.ย. 69 — ปิดแล้ว)
- **ชั้น 3 — idempotency key กัน `submitBillForReview` สร้างบิลซ้ำที่ต้นตอ**: ผู้ใช้ตั้งสมมติฐานเองว่าบิลซ้ำอาจเกิด
  จากพนักงานกดส่งซ้ำหลังเจอ error/ระบบช้า ไม่ใช่ตั้งใจ — ตรวจสอบแล้วสมเหตุสมผลจริง (`submitBillForReview` ไม่
  idempotent + BillCapture project เพิ่งยืนยันว่ามีปัญหา cold start จริงในหัวข้อ 2 ด้านบน) เพิ่ม `idempotencyKey`
  ให้ทั้ง 2 จุดที่เรียก (`renderBillReview`'s submitBtn โหมด staff + `submitAllBatch()` โหมดหลายบิลรวมกัน — คนละ
  key ต่อบิล ไม่ใช่ key เดียวทั้ง session) — **ตั้งใจไม่ใช้ `LockService`** (ต่างจาก `createOrder` ของแอปสั่งของ)
  เพราะฟังก์ชันนี้มีขั้นตอนอัปโหลดรูปขึ้น Drive ที่ช้าได้ไม่แน่นอน ถ้าล็อกทั้งฟังก์ชันจะเสี่ยงบล็อกคำขอของพนักงานคนอื่นที่
  กำลังส่งบิลคนละใบพร้อมกัน (บั๊กแบบเดียวกับที่เจอใน `updateDelivery`/Telegram ของแอปสั่งของเมื่อ 10 ก.ย. 69) — ใช้
  แค่ `CacheService` เช็ค-แล้ว-จำ (TTL 5 นาที) พอสำหรับเคสจริงที่เจอ (พนักงานกดซ้ำห่างกันหลายวินาที ไม่ใช่สองคำขอ
  มาถึงพร้อมกันในเสี้ยววินาที) — key เป็น optional ไม่บังคับ ไม่กระทบ `finalizePurchaseReceipt` (ฝั่งเจ้าของกด
  "บันทึกต้นทุน" มีความเสี่ยงแบบเดียวกันทางเทคนิค แต่ผู้ใช้ไม่ได้รายงานปัญหานี้ ตั้งใจไม่แตะจนกว่าจะถูกขอ)

**ทดสอบครบทุกชั้น**: unit test (Node `vm` sandbox กับ `BillCapture-Code.gs` จริง) รวม 14 เคส + Playwright E2E
รวม 22 เคสใหม่ (discard button, owner duplicate warning, idempotency key ทั้ง 2 โหมด) รวมกับรันซ้ำเทสเดิมทั้งหมด
**90 เคสผ่านหมดไม่มี regression** ก่อน push

⚠️ **สถานะ deploy ณ ตอนจบ session — สำคัญมาก อ่านก่อนคุยเรื่องบิลซ้ำ/บิลรอตรวจสอบต่อ**: Push เข้า `main` แล้วครบ
(commit `5b09c77` — merge สะอาดกับอีก session ที่ดันงานฝั่งแอปสั่งของเข้ามาพร้อมกัน คนละไฟล์กันเลย ไม่มี conflict)
**แต่ backend ทั้ง 2 โปรเจกต์ (`Code.gs` หลัก + `BillCapture-Code.gs`) ยังไม่ได้ deploy จริง** — ผู้ใช้ทดสอบกดปุ่ม
"ทิ้งบิลนี้" แล้วเจอ error `unknown action: discardPendingBill` ยืนยันชัดเจนว่า **โปรเจกต์ BillCapture ยังรันโค้ดเก่า
อยู่** ส่งไฟล์ทั้ง 2 (`Code.gs`, `BillCapture-Code.gs`) ให้ผู้ใช้ผ่าน SendUserFile ซ้ำอีกรอบล่าสุดแล้ว (รอบสุดท้าย
ก่อนจบ session) **ยังไม่ได้รับการยืนยันว่าแปะ/deploy ทั้ง 2 โปรเจกต์เสร็จหรือยัง** — session ถัดไปต้องถามก่อนเป็น
อันดับแรกถ้าจะคุยเรื่องนี้ต่อ ถ้ายังไม่ deploy `BillCapture-Code.gs` ฟีเจอร์ใหม่ทั้ง 3 ชั้นข้างบน (เตือนบิลซ้ำฝั่ง
เจ้าของ/ปุ่มทิ้งบิล/idempotency key) จะยังใช้งานไม่ได้เลยทั้งหมด ไม่ใช่แค่ปุ่มทิ้งบิลอย่างเดียว

### สถานะล่าสุด (อัปเดต 16 ก.ย. 69) — ยืนยัน deploy ครบทั้ง 2 โปรเจกต์แล้ว + เช็คคำถามเรื่อง `?v=2`

✅ **ปิดเคส deploy ที่ค้างจาก session ก่อน**: ผู้ใช้ยืนยันแล้วว่า deploy ทั้ง 2 โปรเจกต์ Apps Script (`Code.gs` หลัก +
`BillCapture-Code.gs`) เสร็จเรียบร้อยแล้ว — ฟีเจอร์บิลซ้ำครบทั้ง 3 ชั้น (เตือนบิลซ้ำฝั่งเจ้าของ/ปุ่ม "ทิ้งบิลนี้"/
idempotency key) พร้อมใช้งานจริงแล้ว ไม่มีอะไรค้างจากหัวข้อด้านบนอีก

**คำถามเรื่อง `https://samurai-murex.vercel.app/stock-check.html?v=2` เทียบกับ URL เปล่า**: ผู้ใช้ถามว่าต่างกันยังไง
(อ้างว่า Claude เคยส่งลิงก์ `?v=2` ให้ — **ค้นทั้ง CLAUDE.md และ repo แล้วไม่เจอว่ามีการบันทึกไว้ที่ไหนเลยว่าเคยสร้าง/
ส่งลิงก์นี้จริง** ถ้า session ไหนเคยทำจริงแล้วไม่บันทึกไว้ถือเป็นความผิดพลาด) ไล่เช็คแล้วสรุปได้ชัดเจน:
- โค้ด `stock-check.html` ไม่มีจุดไหนอ่าน query string ของหน้าตัวเองเลย (`URLSearchParams` ที่เจอในไฟล์เป็นแค่ตัวสร้าง
  URL ตอนยิง API ไปหา backend เท่านั้น) — **ต่างจากแอปสั่งของที่ใช้ `?ref=`/`?admin=` กำหนดเส้นทางจริง**
- ยิง request ตรงๆ ไปที่ Vercel ผ่าน MCP tool (`mcp__Vercel__web_fetch_vercel_url`) เทียบทั้งสอง URL — **เนื้อหาไฟล์
  เหมือนกันทุกตัวอักษร** ยืนยันว่าไม่มีโค้ด/เวอร์ชันต่างกันจริง
- **สรุป**: `?v=2` ไม่มีความหมายอะไรกับแอปนี้เลย เป็นไปได้มากที่สุดว่าเป็นแค่ cache-busting query string เฉยๆ

**ผู้ใช้รายงานว่า URL เปล่า (ไม่มี `?v=2`) ช้ากว่าตอนเปิดครั้งถัดมา (หน้าแรกก็ช้าแล้ว ไม่ใช่แค่ตอนโหลดข้อมูล) ส่วน
`?v=2` ยังเร็วเหมือนเดิม** — ไล่เช็คด้วย MCP tool เทียบ header ของทั้งสอง URL พบว่า **URL เปล่ากลับโดน
`x-vercel-cache: HIT`** (แคชไว้ที่ edge แล้ว ควรเร็วกว่าถ้ามีผลจริง) ส่วน `?v=2` เป็น `x-vercel-cache: MISS` (เพิ่งถูก
เรียกครั้งแรกตอนนั้น) — **ตัดความเป็นไปได้ว่า Vercel/โค้ดเป็นต้นเหตุได้เกือบหมด** เพราะฝั่ง Vercel พร้อมเสิร์ฟ URL
เปล่าได้เร็วกว่าด้วยซ้ำ เหลือความเป็นไปได้ที่น่าจะเป็นสาเหตุจริงคือ**ฝั่งเครื่อง/เบราว์เซอร์ของผู้ใช้เอง** (ตรงกับ pattern
แคชเหนียวที่เคยเจอมาก่อนหลายรอบ — ดูหัวข้อ "ไล่บั๊ก 'เข้าแอปไม่ได้' จนพบว่าเป็นแคช Safari" ด้านบน) — **ผู้ใช้ขอพักเรื่องนี้
ไว้ก่อน ยังไม่ได้ตอบว่าใช้เบราว์เซอร์/อุปกรณ์อะไร หรือลอง incognito เทียบหรือยัง** ถ้าเจอรายงานเรื่องนี้อีก ให้ถามคำถาม
2 ข้อนี้ก่อน (1. เปิดด้วยเบราว์เซอร์/อุปกรณ์อะไร 2. ลอง incognito เปิด URL เปล่าดูว่ายังช้าไหม)

**ไม่มีการแก้โค้ดใดๆ ใน session นี้** — เป็น session ตรวจสอบ/ตอบคำถามล้วนๆ ไม่ต้อง deploy อะไรเพิ่ม

### สถานะล่าสุด (อัปเดต 17-18 ก.ย. 69) — แก้โควต้า Gemini 429 + ล้าง Drive/สินค้าซ้ำ + ปิดช่องว่างสุดท้ายของ retry เงียบๆ + เพิ่มตัวจับเวลา

**บริบทกว้าง**: session ยาวข้ามคืน เริ่มจากผู้ใช้เจอ error โควต้า Gemini ตอนถ่ายบิล ไล่ยาวไปจนถึง production incident จริงตอนดึก (เข้าแอปไม่ได้นาน ~8 นาที) จบด้วยฟีเจอร์ใหม่ที่เกิดจาก incident นั้นตรงๆ — **ทุกอย่างด้านล่าง push เข้า `main` แล้วครบ ยืนยันจากผู้ใช้ว่า deploy backend (`BillCapture-Code.gs`) เสร็จแล้วด้วย**

**1. โควต้า Gemini API (429) เต็มตอนถ่ายบิล — แก้ทั้ง 2 ฝั่ง (commit `1ee0279`)**: เจอ error `Gemini API error (429): ... limit: 20` — วิเคราะห์จาก `retryDelay` ที่ Google ส่งมา (~3.8 วิ) สรุปว่าเป็นโควต้า**ต่อนาที** (RPM) ไม่ใช่ต่อวัน และ**นับรวมทั้งร้าน** (ใครถ่ายบิลพร้อมกันก็แย่งโควต้าเดียวกัน) เจอบั๊กขยายผลจริงด้วย: `apiPostSilentRetry` ฝั่งเว็บเดิมยิงซ้ำ 3 ครั้งไม่แยกแยะว่า error เป็นโควต้าเต็มหรือแค่เน็ตหลุด รวมกับ backend เองก็ retry อีก 1 ชั้น รวมแล้ว 1 ครั้งที่ผู้ใช้กดอาจไปโดน Gemini ถึง 6 ครั้ง ยิ่งซ้ำเติมปัญหา
- `BillCapture-Code.gs`: `callGeminiOnce` แยก 429 ออกมามาร์ค `[QUOTA]` ในข้อความ + ดึง `retryDelay` มาใช้ตั้งเวลารอ, `callGemini` รอนานขึ้น (อย่างน้อย 12 วิ, cap ที่ 30 วิ กัน retryDelay ที่อาจยาวผิดปกติ) แทนรอ 3 วิเหมือน error อื่น
- `stock-check.html`: `apiPostSilentRetry` เจอ marker `[QUOTA]` หยุดยิงซ้ำทันที (ไม่ลองอีก 2 ครั้งที่เหลือ) + เพิ่ม `friendlyBillError()` แปลข้อความเป็น "AI อ่านบิลเต็มคิวชั่วคราว... รอสัก 1 นาที" ใช้ทั้ง 3 จุดที่โชว์ error จากการถ่ายบิล
- ทดสอบ 24 เคส (Node `vm` sandbox รัน `BillCapture-Code.gs`/สคริปต์จริงจากไฟล์) ครอบคลุม cap 30 วิ, fallback ไม่มี `retryDelay`, end-to-end ผ่าน `doPost`, regression error อื่นไม่เปลี่ยนพฤติกรรม — ผ่านหมด
- ✅ ผู้ใช้ยืนยันแล้วว่า deploy `BillCapture-Code.gs` เสร็จ

**2. ล้าง Google Drive — ลบแล้ว 3 รายการ (ไม่ใช่โค้ด แต่ทำผ่าน `mcp__Google_Drive__*` ในเซสชันนี้)**: ผู้ใช้ขอช่วยหาไฟล์/โฟลเดอร์ที่ไม่ใช้แล้ว — เทียบ Folder ID ในโค้ดทุกไฟล์ (`Code.gs`/`BillCapture-Code.gs`/`order-app-Code.gs`) กับของจริงใน Drive ก่อนสรุป ไม่ได้เดา:
- **"รูปบิลซามูไร 1/3, 2/3, 3/3"** (บิลเก่า ~101 ใบ ที่เคยตั้งใจสแกนย้อนหลังแต่ผู้ใช้ยกเลิกแผนไปแล้วตั้งแต่ 10 ก.ย. 69) — ลบแล้ว
- **"🥩 Samurai Staging Sheet — กรอก SafetyStock ก่อน Import"** (ใช้ครั้งเดียวตอน import สินค้าเริ่มต้น มิ.ย. 69) — ลบแล้ว
- **"Backup - Order Web App"** (26 ไฟล์ snapshot `index.html`/`Code.gs` เวอร์ชัน v1.0-v2.6 ก่อนย้ายเข้า git — ตอนนี้มี git history แทนแล้ว) — ลบแล้ว
- ตรวจแล้วว่า **"Samurai Product for Order Web App"** ยังใช้งานจริง (เจอไฟล์รูปอัปโหลดใหม่ล่าสุดวันที่ทำงาน) — **ห้ามลบ**
- ตรวจแล้วว่ารูปที่ดูเหมือน "ลอยนอกโฟลเดอร์" ตอนเปิดดูผ่านแท็บ **"Home"/"ล่าสุด"** ของ Drive (ไม่ใช่ "My Drive") จริงๆ แล้วอยู่ในโฟลเดอร์ถูกต้องหมด (ซ้อน `รูปบิลรับของ for Stock-check web app` → ปี พ.ศ. → ชื่อซัพพลายเออร์) แค่มุมมองนั้นไม่โชว์ path ให้เห็นเฉยๆ — ไม่ต้องย้ายอะไร

**3. สินค้าซ้ำ 2 เคสจริง — ผู้ใช้ลบเองใน Sheet หลังผมช่วยตรวจสอบยืนยัน**:
- **"เบคอนเสียบไม้ 20 ไม้"** (S022 ทรัพย์ไพศาล) — ผู้ใช้กด "เพิ่มสินค้าใหม่" (โปรไฟล์ Mile) 2 ครั้งเพราะจอขึ้น "บันทึกไม่สำเร็จ: หมดเวลาเชื่อมต่อ" — เช็ค Apps Script Executions log พบว่า**ทั้ง 2 ครั้ง `doPost` สำเร็จจริง** (2.845s, 3.491s ไม่มี error) ยืนยันว่า `addProduct` **ไม่มี idempotency key** เหมือน `submitBillForReview`/`createOrder` เลย ทำให้เขียนซ้ำ 2 แถวจริง — ผู้ใช้เช็คแล้วเจอซ้ำจริง ลบส่วนเกินเองแล้ว — **เสนอเพิ่ม idempotency key ให้ `addProduct` แล้ว ผู้ใช้ยังไม่ตอบว่าจะทำหรือไม่** ถ้าคุยเรื่องนี้ต่อให้ถามใหม่
- **P031 "ฮอทดอกบีลัคกี้"** (S001 ลุงทวี) — เป็นชื่อกลางๆ ก่อนที่สินค้าจะถูกแยกเป็น P202 "...1000 กรัม" และ P203 "...(ซอง500กรัม)" — ดึงไฟล์ทั้งชีต (`mcp__Google_Drive__read_file_content` บนตัวชีต `Samurai Stock-check Web App` แล้ว grep หาเทียบ) ยืนยันว่า P031 **ไม่มีประวัติซื้อ/เช็คสต๊อก/จับคู่บิลเลยสักครั้ง** ต่างจาก P202/P203 ที่มีประวัติจริงครบ — ปลอดภัยให้ลบ ผู้ใช้ลบแถว P031 ในชีต `Product (Actual)` + `U031`/`U176` ใน `ProductUnits` เองแล้ว
- ⚠️ **เทคนิคใหม่ที่ใช้ได้ผลรอบนี้**: `mcp__Google_Drive__read_file_content(fileId)` อ่านทั้ง Google Sheet (ทุกแท็บ) ออกมาเป็น markdown/text ยาวได้ (ไฟล์นี้ ~250,000 ตัวอักษร เกิน token limit ตรงๆ แต่ tool เซฟลงไฟล์ในเครื่องให้อัตโนมัติ ใช้ `grep`/`jq` ไล่หาต่อได้) — **มีประโยชน์มากเวลาต้องเช็คว่าสินค้า/ค่าใดๆ ถูกอ้างอิงจริงในชีตไหนบ้างก่อนแนะนำให้ลบอะไร** ไม่ต้องเดา/ขอผู้ใช้ export ให้เหมือนก่อนหน้านี้เสมอไปแล้ว (ยังต่อ `google.com` ตรงๆ ไม่ได้เหมือนเดิม แต่ผ่าน MCP connector นี้ได้)

**4. `renderSupplierList` ไม่มี retry เงียบๆ — จุดสุดท้ายที่ค้างไว้ตั้งแต่ 15 ก.ย. 69 แก้แล้ว (commit `6f32dea`)**: ผู้ใช้เจอ error "โหลดสถานะไม่สำเร็จ" ตอนกดเข้าเมนู "เช็คสินค้า" — ตรงกับที่เคย grep เจอไว้แล้วว่าเป็น pattern เดียวกับอีก 6 จุดที่แก้ไปแล้วแต่ยังไม่ได้ทำจุดนี้ (ไม่ได้อยู่ใน 4 จุดที่อนุมัติไว้รอบก่อน) — วิเคราะห์ผลกระทบก่อนแก้ตามที่ผู้ใช้ขอ (grep หา caller ทั้งหมด 3 จุด, เช็คว่า `getStockStatus`/`apiGet` เป็น read-only ปลอดภัยที่จะ retry ไม่จำกัด) แก้ให้ตรงกับแพทเทิร์นเดิมของ `renderSupplierCheck` เป๊ะ — ทดสอบด้วย Node `vm` sandbox (เจอ+แก้บั๊กของตัวเทสเอง 2 จุดระหว่างทาง: `vm` context ไม่มี `URLSearchParams` ในตัว, ตัวแปร global แบบ `let` ของสคริปต์จริงตั้งค่าจากภายนอกไม่ได้ — ต้องตัด `bootstrap()` ทิ้งก่อนรันเทสกันแย่ง mock fetch queue กัน) ยืนยันกลไก retry ทำงานถูกต้อง (retry 2 ครั้งพังแล้วสำเร็จครั้งที่ 3 ไม่โชว์ error ระหว่างทาง)

**5. Production incident จริงคืนนั้น — เข้าแอปไม่ได้นาน ~8 นาที**: หลัง push ข้อ 4 ผู้ใช้รายงาน "พังกว่าเดิม เข้าไม่ได้เลย" ไล่วิเคราะห์เป็นขั้นเป็นตอน (ไม่รีบสรุปว่าเป็นโค้ดที่เพิ่งแก้):
- ตรวจ `bootstrap()` (โค้ดหน้าแรก) ยืนยันว่า**ไม่มีทางไหนที่จะพาไปเรียก `renderSupplierList` เองอัตโนมัติได้เลย** (`history.replaceState` บังคับกลับไป `screen:'home'` เสมอ) — ตัดขาดว่าไม่ใช่โค้ดที่เพิ่งแก้แน่นอน ก่อนจะไปเช็คอย่างอื่น
- ดึงไฟล์ที่ deploy จริงบน Vercel มาตรวจ (`mcp__Vercel__web_fetch_vercel_url`) เทียบกับไฟล์ที่ push ไป — เหมือนกันเป๊ะ ไม่มี syntax error ไม่ใช่ deploy พัง
- ผู้ใช้ลองยิง URL ของ Apps Script ตรงๆ (`?action=bootstrap`) ก็ค้างเหมือนกัน (ไม่ผ่านแอปเลย ไม่มี JS) → ยืนยันว่าไม่เกี่ยวกับโค้ด/แอปเลยแม้แต่นิดเดียว เป็นปัญหาการเชื่อมต่อ/backend จริง
- เจอหน้า error ของ **Google Drive เอง** ("ขออภัย ไม่สามารถเปิดไฟล์ได้ในเวลานี้") — **ตรงกับเคยเจอมาก่อนแล้วเป๊ะเมื่อ 13-15 ก.ย. 69** (สรุปไว้ตอนนั้นว่าเป็นแคช/session บัญชี Google ค้างในเบราว์เซอร์เครื่องนั้น ไม่ใช่ Google ล่มทั้งระบบ) — ครั้งนี้ยังไม่ทันได้ลอง incognito ยืนยันซ้ำ เพราะ**หายไปเองก่อน** (กลับมาเช็คอีกทีเจอ JSON ข้อมูลจริงตอบกลับมาแล้ว)
- ⚠️ **ยังไม่ยืนยันสาเหตุแท้จริง 100%** — ปัญหาหายเองก่อนจะไล่จนสุด (ไม่ได้ลอง incognito จริง) มีแค่หลักฐานแวดล้อมที่ตรงกับเคสก่อนหน้ามาก ถ้าเจอ "เข้าไม่ได้" อีกให้ไล่ตามลำดับเดียวกันนี้ (เช็ค deploy ตรงกับที่ push จริงไหม → ลองยิง URL backend ตรงๆ → สังเกตว่าเป็นหน้า error ของ Google Drive หรือเปล่า → ลอง incognito เทียบ) และคราวนี้มีตัวจับเวลา (ข้อ 6) ช่วยบอกได้ทันทีว่าค้างนานแค่ไหนแล้ว

**6. เพิ่มตัวจับเวลาบนหน้า "กำลังโหลด" ทั้ง 7 จุด — เกิดจาก incident ข้อ 5 ตรงๆ (commit `ce1319f`)**: ผู้ใช้ชี้ว่ารอ 8 นาทีเต็มโดยไม่รู้เลยว่าแอปยังพยายามเชื่อมต่ออยู่หรือแฮงค์ไปแล้ว (จอนิ่ง "⏳ กำลังโหลด...ข้อความเดียว) ขอตัวจับเวลาไว้แจ้งกลับมาได้ทันทีโดยไม่ต้องเปิด Executions log เอง — ถามขอบเขตก่อนแก้ (`AskUserQuestion`) ได้คำตอบ "ทำทุกจุด":
- เพิ่ม `attachLoadingTimer(el)` (ทำงานบน element ที่ให้มา เช็ค `el.isConnected` ทุก tick กันจอเปลี่ยนไปแล้วแต่ retry loop เดิมยังวนอยู่เบื้องหลังไปเขียนทับจอผิด) + `loadingTimerHtml()` (สร้าง placeholder `<p id="...">` ไม่ซ้ำกันทุกครั้ง) — ไม่โชว์อะไรถ้ายังไม่ถึง 5 วิ (กันกระพริบตอนโหลดเร็วปกติ) หลังจากนั้นโชว์ "รอมาแล้ว X วิ" แล้วเปลี่ยนเป็น "X นาที Y วิ" หลัง 1 นาที
- ใช้ครบทั้ง 7 จุดที่มี retry loop เงียบๆ: `bootstrap()`, `renderOwnerPin`'s `checkPin`, `renderSupplierCheck`, `renderSupplierList`, `renderOwnerStockAnalytics`, `renderOrderPage`, `renderBillPendingList`'s `load()` — จุดที่ใช้ `renderLoading()` อยู่แล้ว (2 จุด) แก้แค่ helper ตัวเดียวจบ ที่เหลือแก้ทีละจุดตาม markup ของตัวเอง (`checkPin` ต้องเพิ่ม `<p id="pinTimer">` ใหม่ในเทมเพลตเพราะ `errEl` เดิมใช้ทั้งสถานะ "กำลังตรวจสอบ"/error ข้อความเดียวอยู่แล้ว)
- ทดสอบ 10 เคสด้วย Node `vm` sandbox **ควบคุมเวลาเอง** (mock `Date.now()`/`setInterval` ให้กระโดดตามที่สั่งได้ ไม่ต้องรอจริง) ครอบคลุม: ไม่โชว์อะไรก่อน 5 วิ, format วินาที/นาทีถูกต้อง, **จำลอง incident จริง 8 นาทีเป๊ะได้ข้อความ "รอมาแล้ว 8 นาที 0 วิ" ถูกต้อง**, `stop()` หยุดนับจริง, element ที่หลุดจาก DOM ไม่ถูกเขียนทับ/ไม่พัง — ผ่านหมด

⚠️ **ยังไม่ได้รับการยืนยันจากผู้ใช้ว่าเจอตัวจับเวลาทำงานจริงในสถานการณ์ค้างจริงหรือยัง** (เพิ่ง push ไปตอนดึก ยังไม่มีรายงานผลกลับมา) — ถ้าคุยเรื่องนี้ต่อให้ถามผลก่อน

### สถานะล่าสุด (อัปเดต 18 ก.ย. 69) — ยืนยันตัวจับเวลาทำงานจริง + พบ+แก้ root cause คนละเรื่อง: `keepWarm` โปรเจกต์หลักไม่เคยถูกตั้ง trigger

✅ **ยืนยันแล้วว่าตัวจับเวลา "กำลังโหลด" ทำงานถูกต้องจริงในสถานการณ์จริง** (ปิดข้อค้างจากหัวข้อด้านบน) — เช้านี้เข้าแอปครั้งแรกของวันแล้วรอหน้าแรก ~30 วิ ผู้ใช้ยืนยันว่าเห็นตัวนับ "รอมาแล้ว X วิ" ขึ้นจริงระหว่างรอ และระบบ self-heal เข้าได้เองโดยไม่ต้องรีเฟรช/ทำอะไรเพิ่ม ไม่มี error โผล่เลย — ตรงตามที่ออกแบบไว้เป๊ะ

**พบสาเหตุจริงของ cold start 30 วิ นี้ — คนละเรื่องกับ incident ค้าง ~8 นาทีของ 17-18 ก.ย. 69 (ห้ามเข้าใจผิดว่าเป็นเรื่องเดียวกัน)**: ไล่เช็คแล้วพบว่า **โปรเจกต์ Apps Script "Samurai Stock-check Web App" (ตัวที่รัน `Code.gs` หลัก) ไม่เคยถูกตั้ง time-driven trigger ให้เรียก `keepWarm()` เลย** ทั้งที่ตัวฟังก์ชันถูกเพิ่มเข้าโค้ดไปแล้วตั้งแต่ 15 ก.ย. 69 (commit `823abcb`) — ต่างจากอีก 2 โปรเจกต์ (BillCapture, order-app) ที่ยืนยันแล้วว่าตั้ง trigger ทำงานจริง ผลคือสคริปต์หลักหลับข้ามคืนได้ตามปกติ คำขอแรกของวันเลยเจอ cold start เต็มๆ

**แก้แล้ว**: อธิบายผลกระทบโควต้าให้ผู้ใช้ฟังก่อน (รัน 288 ครั้ง/วัน × ฟังก์ชันเปล่าไม่แตะ Sheet/Cache/Lock ~0.3-1 วิ/ครั้ง = รวม ~2-5 นาที/วัน จากโควต้า trigger runtime 90 นาที/วัน ไม่กระทบ concurrent execution เพราะจบงานเกือบทันที ไม่เหมือนกรณี `analyzeBillPhoto` ที่เคยค้าง slot นานเป็นนาที) — ผู้ใช้ตั้ง trigger เองแล้ว (Add Trigger → `keepWarm` → Time-driven → Every 5 minutes) **ยืนยันตั้งเสร็จแล้ววันนี้ (18 ก.ย. 69)**

✅ **ตอนนี้ครบทั้ง 3 โปรเจกต์ Apps Script แล้วที่มี `keepWarm` trigger ทำงานจริง**: Samurai Stock-check Web App (หลัก), Samurai BillCapture, order-app — ปิดช่องว่างสุดท้ายเรื่องนี้

⚠️ **ยังไม่ยืนยัน**: ยังไม่เห็นผลจริงว่าคืนนี้/พรุ่งนี้เช้าจะไม่เจอ cold start 30 วิ อีกหรือไม่ (เพิ่งตั้ง trigger วันนี้ ยังไม่ผ่านคืนที่ไม่มีคนใช้แอปเลยสักคืน) — session ถัดไปถ้าคุยเรื่องนี้ต่อให้ถามผลก่อน — **และเรื่องนี้แยกขาดจากสาเหตุ incident ค้าง ~8 นาทีของ 17-18 ก.ย. 69 ที่ยังไม่ยืนยัน 100% เหมือนเดิม** (อาการตอนนั้นเป็นหน้า error ของ Google Drive เอง ไม่ใช่แค่รอนานเฉยๆ แบบ cold start ปกติ) — ห้ามสรุปว่าตั้ง `keepWarm` รอบนี้แก้ incident นั้นไปด้วย

### แผนงานอนาคต — รายงาน/Dashboard ต้นทุนซื้อของจากข้อมูลบิล (ตกลงกับผู้ใช้ 18 ก.ย. 69 — ยังไม่เริ่มทำ)

**บริบท**: ผู้ใช้ (ในฐานะเจ้าของ/คนทำแอป) บอกว่าฟีเจอร์การทำงานฝั่งลูกจ้าง/เจ้าของไปได้ดีแล้ว ทุกคนเริ่มใช้คล่อง — ขั้นต่อไปที่ตั้งใจทำคือเอาข้อมูลบิลที่สะสมมาจากฟีเจอร์ "รับของ — ถ่ายบิล" (เก็บอยู่ใน sheet `PurchaseReceipts` มาตั้งแต่ ~10 ก.ย. 69) มาทำรายงาน/DB/managerial report/accounting

**ตกลงกันไว้แล้ว 3 อย่าง (ผ่าน `AskUserQuestion`)**:
1. **เป้าหมายรายงาน** (เลือกไว้ 3 ข้อ): (ก) ต้นทุนซื้อของแยกตามซัพพลายเออร์/สินค้า over time (ข) สรุปยอดซื้อรายเดือน/รายไตรมาสสำหรับบัญชี-ภาษี (ค) ภาพรวมผู้บริหาร (managerial) ทั้งร้าน
2. **รูปแบบ**: Dashboard ใน **Google Sheets** (Pivot Table/Chart) — ไม่ใช่หน้าใหม่ในแอป ไม่ใช่ PowerBI (แม้ผู้ใช้จะคุ้นเคย PowerBI จากงานประจำ แต่เลือก Sheets เพราะเร็วกว่า ไม่ต้องเขียนโค้ดเพิ่มในแอป)
3. **ช่วงเวลาเริ่ม**: **รอเก็บข้อมูลเพิ่มอีก 1-3 เดือนก่อน** (ข้อมูลตอนนี้ ณ 18 ก.ย. 69 มีแค่ ~1 สัปดาห์กว่า ยังน้อยเกินไปที่จะเห็นเทรนด์ที่มีความหมาย) — **ยังไม่เริ่มลงมือทำ Dashboard จริงในตอนนี้** รอผู้ใช้บอกว่าถึงเวลาแล้ว

⚠️ **4 จุดเสี่ยงของข้อมูลที่ต้องระวังตอนสร้างรายงานจริง** (วิเคราะห์จาก schema จริงของ `PurchaseReceipts` ใน `finalizePurchaseReceipt()`, `BillCapture-Code.gs` บรรทัด ~692 — คอลัมน์: `ReceiptID, BatchID, Date, SupplierID, ProductID, BillText, BillQty, ReceivedQty, BillUnit, ConversionFactor, ConvertedQty, UnitPrice, TotalPrice, PhotoURL, StaffName, BillDate, BillNumber, BillSubtotal, BillVat, BillTotal, Timestamp`):

1. **`BillTotal`/`BillSubtotal`/`BillVat` เป็นค่าระดับ "ทั้งบิล" แต่ถูกเขียนซ้ำทุกแถวของบิลนั้น** (denormalized ตั้งใจ) — ถ้า SUM คอลัมน์นี้ตรงๆ ใน Pivot จะได้ยอดสูงเกินจริงหลายเท่า (ซ้ำตามจำนวนรายการในบิล) ต้อง **dedupe ด้วย `BatchID` ก่อน SUM เสมอ** — ใช้ `TotalPrice` (ต่อแถว) สำหรับต้นทุนแยกรายสินค้า แต่ใช้ `BillTotal` แบบ unique-by-BatchID สำหรับยอดรวมทั้งบิล
2. **รายการที่เจ้าของกด "ข้ามรายการนี้" ตอนตรวจบิลจะไม่ถูกบันทึกลงแถวเลย** — ผลรวม `TotalPrice` ของบิลอาจไม่เท่ากับ `BillTotal` จริงบนบิล ถ้ารายงานบัญชี-ภาษีต้องตรงยอดเป๊ะ ให้ใช้ `BillTotal` (unique) ไม่ใช่ SUM(`TotalPrice`)
3. **ราคาต่อหน่วยไม่มาตรฐานเดียวกันทุกซัพพลายเออร์** — บางเจ้าราคารวม VAT แล้ว บางเจ้ายังไม่รวม (เคยแก้ false-positive เรื่องนี้ไปแล้วเมื่อ 7 ก.ย. 69 ใน `updateBillHeaderMismatch()`) เปรียบเทียบ "ต้นทุนต่อหน่วย" ข้ามซัพพลายเออร์ตรงๆ อาจไม่ apples-to-apples ต้อง flag เจ้าไหน VAT-inclusive ไว้ก่อนเปรียบเทียบ
4. **บิลที่ค้างใน "รอตรวจสอบ" (`PendingBillReceipts`) ไม่นับเข้ารายงานเลย** จนกว่าเจ้าของจะกดบันทึกจริง — ถ้าคิวรอตรวจมีบิลค้างสะสมนาน ยอดซื้อเดือนนั้นในรายงานจะต่ำกว่าความจริง **แนะนำให้ผู้ใช้เช็คหน้า "บิลรอตรวจสอบ" เป็นระยะระหว่าง 1-3 เดือนนี้ อย่าปล่อยค้าง** โดยเฉพาะก่อนปิดยอดเดือน

**ไม่ต้องแก้โค้ดอะไรตอนนี้** — ข้อมูลที่เก็บอยู่พอใช้ได้กับทั้ง 3 เป้าหมาย แค่ต้องระวัง 4 จุดข้างบนตอนออกแบบสูตร Pivot/Chart

**เมื่อไหร่ควรหยิบเรื่องนี้กลับมาคุย**: รอผู้ใช้บอกว่า "ถึงเวลาแล้ว"/พร้อมเริ่มทำ Dashboard จริง (ประมาณ ธ.ค. 69 – มี.ค. 70 ถ้านับจาก 18 ก.ย. 69 + 1-3 เดือน) — **อย่าเสนอเริ่มทำเองก่อนถูกขอ** ตอนเริ่มจริงให้ทวนตารางความเสี่ยง 4 ข้อนี้ก่อนออกแบบสูตรทุกครั้ง

## Order Web App (`index.html`) — รายละเอียดเชิงลึก

**Production**: https://samurai-murex.vercel.app/ (frontend hosted บน Vercel, deploy จาก repo นี้) ต่อกับ
backend Google Apps Script — โค้ดเก็บไว้ใน repo นี้แล้วที่ `order-app-Code.gs` (ตั้งแต่ 8 ก.ย. 69 อย่าสับสนกับ
`Code.gs` ที่ root ซึ่งเป็นของแอปเช็คสต๊อกคนละแอป)

⚠️ **`order-app-Code.gs` ใน repo อาจไม่ตรงกับตัวจริงที่ deploy อยู่เสมอ** — `git push` **ไม่มีทาง** deploy ให้
(เหมือนกับ `Code.gs` ของแอปเช็คสต๊อกทุกประการ) ผู้ใช้ต้องแก้ผ่าน Apps Script Editor เองแล้วก็อปกลับมาเขียนทับไฟล์นี้ใน
repo (หรือขอให้ผมช่วยแก้แล้ว SendUserFile ให้เอาไปแปะ + อัปเดตไฟล์ใน repo ให้ตรงกันในคราวเดียว) — **ถ้าผู้ใช้บอกว่าแก้
backend เองนอกเซสชั่นแล้ว ห้ามเชื่อว่าไฟล์ใน repo ตรงกับของจริงโดยอัตโนมัติ ให้ถามหรือขอไฟล์ล่าสุดมาเทียบก่อนเสมอ**
ก่อนแก้ backend action ไหนต่อ ให้เปิด `order-app-Code.gs` ในนี้ดูก่อนเป็นจุดเริ่ม แต่ถ้าเนื้อหาดูไม่สอดคล้องกับที่ผู้ใช้
อธิบายพฤติกรรมจริง ให้สงสัยว่าไฟล์เก่ากว่าของจริง แล้วขอไฟล์ปัจจุบันมาเทียบ

### Routing (query params บน `index.html`)
- `?admin=true&key=shop123` → เปิดหน้าแอดมิน (ADMIN_KEY ฝังในโค้ดตรงๆ ที่ตัวแปร `ADMIN_KEY`)
- `?ref=<customer_id>` → เปิดหน้าสั่งของของลูกค้าคนนั้นโดยเฉพาะ (ลิงก์แต่ละคนอยู่ใน sheet "Link for Customers")
- ไม่มี param เลย → error screen

### Google Sheet data model (1 workbook หลายแท็บ — คนละไฟล์กับ Sheet ของแอปเช็คสต๊อก)
- `Customer`: `customer_id, name, product_group` — `product_group` คือกลุ่มราคา (A-Z)
- `Product`: `product_id, name, price, group, image_url` — สินค้าชื่อเดียวกันมีได้หลายแถว แถวละ 1 กลุ่มราคา
  (ระบบ per-customer-segment price list ผ่าน `group` — อย่าเข้าใจผิดว่าเป็นข้อมูลซ้ำ/ขยะ)
- `Order_<ปีพ.ศ.>` (ปีปัจจุบัน = `Order_2569`, sheet เก่าสุดที่ไม่มีปีต่อท้ายชื่อ `Order` ก็ยังใช้ fallback ได้):
  `order_id, customer_id, customer_name, items, total, note, status, timestamp, payment_status,
  delivery_status, packing_at, done_at, customer_group`
  - `status` และ `payment_status` **ไม่ได้ใช้งานจริงแล้ว** ค้างค่า `pending` ทุกแถวเสมอ (เพราะฟังก์ชันที่เคยอัปเดตค่าพวกนี้ถูกลบไปแล้ว — ดูหัวข้อ backend actions ด้านล่าง) — ตัวที่บอกสถานะจริงคือ `delivery_status` (`pending`/`packing`/`done`/`cancelled`)
- `BusinessNote`: `note_id, date, text` — โน้ตอิสระของแอดมิน ไม่ผูกกับเดือน
- `Link for Customers`: `customer_id, name, link` — ลิงก์สั่งของเฉพาะตัว (`?ref=...`) ของลูกค้าแต่ละคน

### Backend actions ปัจจุบัน (ยืนยันตรงกับ `order-app-Code.gs` ที่ผู้ใช้ส่งมา 8 ก.ย. 69)
- `doGet`: `getCustomer, getProducts, getOrders, getAdminOrders, getAdminOrdersFull, getBusinessNotes, getThaiHolidays`
  - `getAdminOrders` ตอนนี้กรองช่วงย้อนหลังแล้ว (ดูหัวข้อ `ADMIN_ORDERS_WINDOW_DAYS` ด้านล่าง) — `getAdminOrdersFull`
    คือตัวที่ไม่กรอง ใช้เฉพาะหน้า "ภาพรวม/แดชบอร์ด" (`renderAdminOverview`)
- `doPost`: `createOrder, updateOrder, updateDelivery, cancelOrder, addBusinessNote, deleteBusinessNote`
  - `updateDelivery` มี guard กันเรียกซ้ำอยู่แล้ว: ถ้า `delivery_status` ที่ส่งมาตรงกับค่าปัจจุบันในชีต จะ skip ทั้งหมด
    (ไม่เขียนซ้ำ ไม่ยิง Telegram ซ้ำ) — ดูหัวข้อ "บั๊กแจ้งเตือนซ้ำ" ด้านล่างเรื่องช่องโหว่ race condition ที่ guard นี้ยังกันไม่หมด
- **ลบไปแล้ว** เพราะไม่มี frontend เรียกใช้: `getOwnerOrders, updateStatus, updatePayment, uploadSlip, verifyPayment`
  (ตัวหลังสุดเคยเรียก Claude API ตรวจสลิปโอนเงิน มีค่าใช้จ่ายต่อครั้ง) พร้อม helper ที่กลายเป็นขยะไปด้วย
  (`checkNameMatch`, ตัวแปร `CLAUDE_API_KEY`) — **ถ้าเจอโค้ดพวกนี้ในไฟล์ `.gs` ที่ผู้ใช้อัปโหลดมาใหม่ แปลว่ายังไม่ได้อัปเดตเป็นเวอร์ชันล่าสุด ให้แจ้งผู้ใช้ก่อนแก้อย่างอื่นต่อ**

### Known quirks / เรื่องที่ควรรู้ก่อนแก้ index.html
- Sticky header (`.admin-sticky-wrap`) ทับด้านบนของหน้าแอดมินตลอดเวลา — ถ้าจะทำ scroll-to-element (เช่น การ์ดกดแล้วเลื่อนไป section อื่น) ต้องคำนวณเผื่อ offset ความสูงของมันเองด้วย (`el.offsetHeight`) ไม่งั้นหัวข้อ section ที่เลื่อนไปจะโดนบัง — ห้ามใช้ `scrollIntoView` เปล่าๆ
- `openOwnerDashboard()` เช็ค `window.innerWidth<768` เพื่อกันไม่ให้เปิดแดชบอร์ดบนจอเล็ก — เวลาทดสอบผ่าน artifact/หน้าต่างที่ถูก embed อาจโดน false positive เพราะเฟรมแคบกว่าหน้าจอจริงของผู้ใช้ (แม้เปิดจาก iPad)
- เขียน `<div style="...">` แบบ generate ด้วย string ต้องระวังไม่ใส่ attribute `style="..."` ซ้ำสองรอบในแท็กเดียวกัน (เช่น อันจากปุ่ม/helper ทับกับอันเดิมของการ์ด) — เบราว์เซอร์เก็บแค่ attribute แรกแล้วทิ้งอันหลัง ทำให้ style เงียบๆ หายไปทั้งดุ้นโดยไม่มี error ให้เห็น

### ข้อจำกัดของ sandbox นี้ (สำคัญมาก อ่านก่อนลงมือ)
- เชื่อมต่อโดเมน `google.com` ทั้งหมด (รวม `script.google.com`, `docs.google.com`) ไม่ได้เลย ถูก network policy บล็อกไว้แบบ organization-wide — **ห้ามเสียเวลาลอง curl/fetch ไปหา backend หรือ Google Sheet ตรงๆ** ให้ขอผู้ใช้ export ข้อมูลเป็นไฟล์ (.xlsx/.csv) หรืออัปโหลดไฟล์ `.gs` มาแทนเสมอ
- อยากทำเดโม่ UI ที่มี "ข้อมูลจริงหรือใกล้เคียงจริง" ให้ทำเป็น Claude Artifact ที่ override ฟังก์ชัน `safeFetchJSON`/`postAction` ของแอปตรงๆ ด้วยข้อมูลจำลอง (ประกาศ `function` ซ้ำหลัง script หลักเพื่อ overwrite) — **ห้าม patch `window.fetch`** เพราะ artifact sandbox บล็อกการเชื่อมต่อโดเมนนอกแบบเงียบๆ ไม่มี error ให้เห็น ทำให้ patch ไม่มีผลจริงและข้อมูลจะว่างเปล่าหมด (0 ทุกช่อง) โดยไม่รู้สาเหตุ

### Workflow ที่ผู้ใช้ย้ำไว้หลายรอบ
- **ห้าม commit/push เข้า `main` โดยไม่ถามก่อนเด็ดขาด** ต้องส่งเดโม่ให้ดูก่อนเสมอ — และเดโม่ที่ผู้ใช้ต้องการคือ
  **ลิงก์ที่กดแล้วเปิดเป็นหน้าเว็บใช้งานได้จริง (Claude Artifact)** ไม่ใช่แค่ screenshot ภาพนิ่ง รอ approve ชัดเจนเป็นคำพูดก่อนถึงจะ commit/push ได้ (แม้ stop hook จะเตือนให้ commit ก็ต้องรอ user ยืนยันก่อน ไม่ใช่ทำตาม hook ทันที)
- ก่อน merge ให้รัน code review เช็คความเรียบร้อยของ diff ก่อนเสมอ (ใช้ syntax check + ไล่ diff เทียบว่าไม่มีโค้ดเดโม่/mock หลุดเข้ามาปนในไฟล์จริง)

### สถานะล่าสุด (อัปเดต 4 ก.ย. 69) — สรุปงานประสิทธิภาพ + แจ้งเตือนของ Order Web App

**ช่องทางแจ้งเตือนออเดอร์ปัจจุบันคือ Telegram** (ไม่ใช่ LINE OA แล้ว) — ย้ายเพราะ LINE OA แผนฟรีมีโควต้าข้อความ/เดือน
หมดไวเกินไป (แจ้งเตือนได้แค่ 2-3 วันแรกของเดือน) `Code.gs` มีฟังก์ชัน `sendTelegramNotify(message)` เรียกใน 3 จุด:
`createOrder` (🛒 ออเดอร์ใหม่), `updateDelivery` เมื่อ status เป็น `done` (✅ จัดเสร็จแล้ว), `cancelOrder` (❌ ยกเลิกออเดอร์)
— **ไม่มี event แจ้งตอนเข้าสถานะ `packing`** ต้องตั้งค่า Script Property 2 ตัวก่อนใช้งานได้: `TELEGRAM_BOT_TOKEN`,
`TELEGRAM_CHAT_ID` (ไม่มี fallback ไป LINE แล้ว ถ้าเจอโค้ด `sendLineNotify`/`LINE_CHANNEL_ACCESS_TOKEN` ในไฟล์ `.gs`
ที่ผู้ใช้อัปโหลดมาใหม่ แปลว่าเป็นเวอร์ชันเก่าก่อนย้าย ให้แจ้งผู้ใช้ก่อนแก้อย่างอื่นต่อ)

✅ **เรื่อง Telegram ปิดถาวรแล้ว (อัปเดต 7 ก.ย. 69)** — ผู้ใช้ยืนยันว่าใช้งานได้ปกติ **ห้ามหยิบเรื่องนี้ขึ้นมาพูดอีกโดยไม่ถูกถาม**
(เคยมีช่วงสับสนว่า "ไม่ดัง" แต่ที่จริงหมายถึงฟีเจอร์เสียงในแอปเว็บที่ถูกลบไปแล้ว ไม่เกี่ยวกับ Telegram — ดูหัวข้อถัดไป)

**เสียงแจ้งเตือนในแอปเว็บ (`index.html`) ถูกลบทิ้งไปแล้วตามคำขอ** — เคยมีฟีเจอร์ `playNewOrderChime()`/`orderChimeCtx`
(สร้างเสียงด้วย Web Audio API ตอนหน้าแอดมิน auto-refresh เจอ order_id ใหม่) ถูก revert ออกทั้งหมด **อย่าเพิ่มกลับเข้าไป
โดยไม่ถูกขอใหม่**

**แก้ปัญหาออเดอร์ซ้ำ** (ลูกค้ากดสั่งแล้วเกิดออเดอร์ซ้ำ 2-3 อันราคา/รายการเดียวกัน) — สาเหตุคือ `postAction` มี
auto-retry ในตัว แต่ `createOrder` ที่ backend ไม่ idempotent (ยิงกี่ครั้งก็สร้างแถวใหม่เสมอ) ถ้า response หลุด/ช้าเกิน
timeout ทั้ง auto-retry และการกดซ้ำของลูกค้าจะสร้างออเดอร์ซ้ำได้ แก้ที่ `index.html`/`placeOrder()`:
1. เรียก `postAction(...,'placeOrder',15000,0)` ปิด auto-retry เฉพาะ `createOrder` (retries=0)
2. ก่อนแจ้ง fail จะเรียก `findRecentMatchingOrder(itemsText,total)` เช็คก่อนว่ามีออเดอร์ของลูกค้าคนนี้ที่รายการ+ยอดตรงกันเป๊ะ
   ภายใน 3 นาทีที่ผ่านมาหรือยัง (ไม่นับที่ถูกยกเลิก) ถ้าเจอ ถือว่าสำเร็จแล้ว ไม่ยิงซ้ำ
- Trade-off ที่ผู้ใช้ยอมรับแล้ว: ถ้าลูกค้าสั่งของเซตเดียวกัน ยอดเท่ากันเป๊ะ ภายใน 3 นาที จะถูกมองเป็นออเดอร์เดียวกัน
- ทดสอบผ่าน Playwright E2E จริง (mock request ที่ระดับ `page.route()` ไม่ใช่ mock ฟังก์ชัน) ครอบคลุม 6 เคส — ดูรูปแบบ
  การทดสอบนี้เป็นตัวอย่างอ้างอิงได้เวลาต้องเทส `placeOrder`/`postAction` อีก

**Performance fixes อื่นๆ ที่ทำไปแล้ว**:
- `index.html`: เพิ่ม `loading="lazy"` ในรูปสินค้าทุกจุด, auto-refresh หน้าแอดมิน 10s → 30s
- `Code.gs`: `findOrderLocation()` เรียง sheet ปีปัจจุบันไว้ค้นหาก่อนเสมอ (ใช้ `getOrderSheetByYear()`) ลด scan cost
  ที่โตขึ้นเรื่อยๆ ตามอายุร้าน — ใช้กับทุก action ที่แก้ไข order ที่มีอยู่ (`updateOrder`/`updateDelivery`/`cancelOrder`)

**Backlog ที่ยังไม่ทำ (ตั้งใจพักไว้)**: จำกัดช่วงข้อมูลย้อนหลังที่ `getAdminOrders()` ส่งกลับ (ตอนนี้ส่งออเดอร์ทั้งหมด
ทุกปีทุกครั้ง ไม่มี limit) — ตอนคุยกันข้อมูลมีแค่ ~3 เดือนยังไม่กระทบมาก แต่จะเป็นปัญหาความเร็วเพิ่มขึ้นเรื่อยๆ ตามอายุร้าน
ถ้าจะทำต้องคุยก่อนว่าฟีเจอร์ "ภาพรวม/กราฟยอดขาย" (`renderAdminOverview`) ต้องดูย้อนหลังกี่เดือน ไม่งั้นจะพังฟีเจอร์นั้น
**(อัปเดต 7 ก.ย. 69: ทำเสร็จแล้ว ดูหัวข้อด้านล่าง — backlog นี้ปิดแล้ว)**

### สถานะล่าสุด (อัปเดต 7 ก.ย. 69) — ตรวจสอบ performance/security ทั้งไฟล์ + แยก endpoint โหลดข้อมูลแอดมิน

**Telegram ปิดเคสแล้ว** (รายละเอียดอยู่ด้านบนแล้ว หัวข้อ "เรื่อง Telegram ปิดถาวรแล้ว")

**ตรวจโค้ดทั้ง `index.html` (3,008 บรรทัด) และ `Code.gs` จริงที่ผู้ใช้อัปโหลดมา** ตามคำขอ "เช็คว่ามีจุดไหนทำให้แอปเร็วขึ้นได้อีก มีบั๊กไหม มีจุดเสี่ยงตรงไหนอีก" สรุปที่ยืนยันแล้วว่าเป็นเรื่องจริง (ไม่ใช่แค่สงสัย):

⚠️ **[ยังไม่แก้] ช่องโหว่ความปลอดภัยสำคัญที่สุด**: `doGet`/`doPost` ใน `Code.gs` ไม่มีการเช็คสิทธิ์/key ใดๆ เลยแม้แต่จุดเดียว
(`ADMIN_KEY='shop123'` ใน `index.html` เป็นแค่ตัวกรอง UI ฝั่ง browser เท่านั้น ไม่เคยถูกส่งไปเช็คที่ backend) — ใครก็ตามที่รู้ URL
ของ `API` (เปิดเผยอยู่ใน `index.html` บน GitHub) ยิง `getAdminOrders`/`getAdminOrdersFull` ตรงๆ จะดึงชื่อ+ออเดอร์ลูกค้าทั้งร้าน
ออกมาได้หมด และยิง `updateOrder`/`updateDelivery`/`cancelOrder`/`addBusinessNote`/`deleteBusinessNote` แก้ไขข้อมูลได้อิสระ
โดยไม่ต้องผ่านหน้าเว็บเลย — pattern เดียวกับที่แอปเช็คสต๊อกเป็น แต่แอปนี้เก็บข้อมูลลูกค้าจริงจึงเสี่ยงกว่า

⚠️ **[ยังไม่แก้] Stored XSS ผ่านช่องโน้ต**: `note` (ลูกค้าพิมพ์ตอนสั่งของ) และ `text` (โน้ตแอดมิน) ถูกเขียนลง Sheet ตรงๆ
ไม่มีการ sanitize ทั้งฝั่ง frontend/backend แล้ว render ผ่าน `innerHTML` แบบไม่ escape ในหลายจุด (`buildHistoryCard`,
`buildPendingAdminCard`, `buildDoneAdminCard`, `renderOverviewNotePanel`) — รวมกับข้อบนที่ backend ไม่เช็คสิทธิ์เลย
ทำให้คนนอกยิง `createOrder`/`addBusinessNote` พร้อม payload สคริปต์ตรงๆ แล้วไปรันในเบราว์เซอร์แอดมินได้ทันที
ไม่ต้องผ่านฟอร์มสั่งของด้วยซ้ำ

⚠️ **[ยังไม่แก้] `total` ไม่ถูกคำนวณใหม่ที่ backend**: `createOrder`/`updateOrder` เชื่อค่า `data.total` จาก client 100%
ไม่มีการ recompute จากราคาสินค้าจริงใน Sheet เลย

⚠️ **[ยังไม่แก้] `parseItems` เปราะบาง**: เก็บรายการสินค้าเป็น string `"ชื่อ xจำนวน, ชื่อ xจำนวน"` แล้ว `split(', ')` กลับ
— ถ้าตั้งชื่อสินค้าที่มี comma อยู่ในชื่อ การ parse จะพังแบบเงียบๆ (ยอด/รายการผิดโดยไม่มี error ให้เห็น)

✅ **[ทำเสร็จแล้ว] แยก endpoint โหลดข้อมูลแอดมิน** (ปิด backlog เดิมจาก 4 ก.ย.) — ก่อนแก้ทำเดโม่เทียบ performance ด้วยข้อมูลจำลอง
เป็น Claude Artifact ให้ดูก่อน (ตามที่ผู้ใช้ขอ "ต้องแก้เรื่องมีผลต่อความหน่วงของแอปก่อน") ได้รับ approve แล้วค่อย implement จริง:
- `Code.gs`: เพิ่ม `filterAdminOrdersWindow()` + ค่าคงที่ `ADMIN_ORDERS_WINDOW_DAYS=60` — `getAdminOrders()` (เดิม) ตอนนี้ส่งแค่
  pending/packing (ทุกอายุ) + done/cancelled ย้อนหลัง 60 วัน ใช้กับแท็บหลัก (รอจัด/กำลังจัด/เสร็จวันนี้) และ auto-refresh ทุก 30 วิ
- `Code.gs`: เพิ่ม action ใหม่ `getAdminOrdersFull()` ส่งข้อมูลเต็มทุกปีแบบเดิม ใช้เฉพาะตอนแอดมินเปิดแท็บ "ภาพรวม"/แดชบอร์ดเท่านั้น
- `index.html`: เพิ่มตัวแปร `allAdminOrdersFull` + ฟังก์ชัน `fetchFullAdminOrders()` — `switchAdminTab('overview')` และ auto-refresh
  ตอนอยู่แท็บภาพรวม จะเรียก `getAdminOrdersFull` แยกต่างหาก ส่วนจุดที่ใช้ `allAdminOrders` (windowed) เดิมทั้งหมด
  (`renderAdminOrders`, การหา order ด้วย id ในแท็บหลัก) ไม่ต้องแก้เพราะข้อมูลที่ต้องใช้อยู่ในหน้าต่าง 60 วันอยู่แล้วเสมอ
  ส่วนที่ต้องดูย้อนหลังได้ไกลกว่านั้น (`getAvailableOverviewMonths`, `ov_doneOrdersInRange`, `renderAdminOverview`'s
  `activeOrders`, `db_doneOrders`) เปลี่ยนไปอ่านจาก `allAdminOrdersFull` แทน
- ทดสอบผ่าน Playwright E2E จริง (mock API ที่ `page.route()`) ยืนยันว่า: เปิดแอดมินครั้งแรกยิงแค่ `getAdminOrders` ไม่ยิง Full
  โดยไม่จำเป็น, เปิดแท็บภาพรวมยิง `getAdminOrdersFull` แค่ 1 ครั้งและเห็นออเดอร์เก่ากว่า 60 วันได้ถูกต้องเมื่อเลือกฟิลเตอร์ "ทั้งหมด",
  สลับกลับแท็บหลักใช้งานปกติไม่มี JS error
- Push แล้ว (commit `17e7ef1` บน `main`) และผู้ใช้ deploy `Code.gs` ใหม่ผ่าน Apps Script Editor (Manage deployments → New version) แล้ว

**Backlog ที่ยังไม่ทำ (เรียงตามความสำคัญ ตกลงกันไว้ว่าจะทำต่อ)**:
1. เพิ่ม key check ที่ backend สำหรับ action ฝั่งแอดมินทั้งหมด (`getAdminOrders`, `getAdminOrdersFull`, `updateOrder`,
   `updateDelivery`, `cancelOrder`, `addBusinessNote`, `deleteBusinessNote`) — เก็บ key ไว้ใน Script Properties เหมือน
   `TELEGRAM_BOT_TOKEN`, ฝั่ง action ของลูกค้า (`getCustomer`/`getProducts`/`getOrders` เฉพาะ id ตัวเอง/`createOrder`) ปล่อยผ่านได้ตามเดิม
2. Escape user input (`note`, business note `text`) ก่อน render ด้วย `innerHTML` ทุกจุดที่ระบุไว้ด้านบน
3. คำนวณ `total` ใหม่ที่ backend จากราคาสินค้าจริงใน Sheet แทนเชื่อค่าจาก client
4. กันชื่อสินค้ามี comma ปนใน `parseItems`/รูปแบบเก็บ `items`
ข้อ 1-2 ควรทำพร้อมกันก่อนเป็นอันดับแรก เพราะเป็นช่องโหว่จริงที่ยืนยันแล้วทั้งสองฝั่ง

### สถานะล่าสุด (อัปเดต 8 ก.ย. 69) — เก็บ backend เข้า repo + แก้บั๊กแจ้งเตือนซ้ำ

**`order-app-Code.gs` ถูกเก็บเข้า repo แล้ว** (ก่อนหน้านี้ไม่เคยอยู่ใน repo เลย ต้องขอผู้ใช้อัปโหลดทุกครั้ง) สแกนหา
secret/hardcoded key ก่อนแล้ว ไม่พบ (Telegram token/chat id ดึงผ่าน `PropertiesService` ทั้งคู่) —ดูคำเตือนเรื่อง sync
สองทางที่ย่อหน้า Production ด้านบน

✅ **บั๊กแจ้งเตือน Telegram ซ้ำตอน "จัดเสร็จ" — แก้ในโค้ดแล้ว ผู้ใช้ deploy แล้ว**: พบจากภาพแชท (ออเดอร์เดียวกัน แจ้ง
"จัดเสร็จแล้ว" 2 ครั้งเวลาเดียวกัน) ต้นตอคือ `updateDelivery` มี guard กันเรียกซ้ำอยู่แล้ว (skip ถ้า `delivery_status`
ที่ส่งมาตรงกับค่าปัจจุบันในชีต) แต่ guard เช็คก่อนเขียนเท่านั้น ไม่ได้ล็อกอะไรเลย (ไฟล์ไม่เคยมี `LockService`) — ถ้า 2
request มาถึงพร้อมกัน (เช่น `postAction` auto-retry ยิงซ้ำตอน execution แรกยังเขียนไม่เสร็จ หรือแตะปุ่มรัว) ทั้งคู่อ่าน
`currentStatus` เดิมพร้อมกันได้ก่อนฝ่ายใดเขียนทัน → ผ่าน guard ได้ทั้งคู่ → เขียน `done` ซ้ำ + ยิง Telegram ซ้ำ
**แก้แล้ว**: ห่อทั้งฟังก์ชัน `updateDelivery` ด้วย `LockService.getScriptLock()` (`lock.waitLock(10000)` ก่อนเริ่ม,
`lock.releaseLock()` ใน `finally`) ทำให้ทุกคำขอ `updateDelivery` ประมวลผลทีละคำขอเท่านั้น ปิด race นี้ที่ต้นตอ — syntax
check ผ่านแล้ว (`node --check`), ส่งไฟล์เต็มให้ผู้ใช้แปะ Apps Script Editor แล้ว และผู้ใช้ยืนยันว่า deploy แล้ว
⚠️ หมายเหตุ: fix นี้ยังไม่ได้ทำกับ `cancelOrder` (มี pattern คล้ายกันแต่ยังไม่มีรายงานบั๊กจริงที่จุดนั้น — ตั้งใจพักไว้
ตามที่ผู้ใช้ขอให้ไล่แก้ทีละเรื่อง)

✅ **[ทำเสร็จแล้ว] ข้อ 1+2 ของ backlog — auth check ฝั่งแอดมิน + escape note กัน stored XSS (8 ก.ย. 69)**:

**ออกแบบ** (คุยผ่าน `AskUserQuestion` ก่อน implement เพราะพบว่า backlog เดิมสโคปผิด — `updateOrder`/`cancelOrder`
ไม่ใช่ action ฝั่งแอดมินล้วนอย่างที่บันทึกไว้ แต่ถูกเรียกจากฝั่งลูกค้าด้วย เช่น หน้า track order ของลูกค้ามีปุ่ม
"แก้ไข"/"ยกเลิก" ออเดอร์ตัวเอง — ถ้าบังคับ `ADMIN_KEY` ตรงๆ ตาม list เดิมจะพังฟีเจอร์ลูกค้า และเจอเพิ่มว่าเดิมทั้งคู่
ไม่เช็ค ownership เลยด้วยซ้ำ ไม่ใช่แค่ไม่มี key):
- **Action แอดมินล้วน** (`getAdminOrders`, `getAdminOrdersFull`, `updateDelivery`, `addBusinessNote`,
  `deleteBusinessNote`) — ต้องมี `ADMIN_KEY` ที่ถูกต้องเท่านั้น เก็บ key ไว้ใน Script Properties (ฟังก์ชันใหม่
  `isValidAdminKey(key)` อ่านจาก `PropertiesService`) ไม่ hardcode ในไฟล์
- **`updateOrder`/`cancelOrder`** (ใช้ร่วมกันทั้งแอดมินและลูกค้า) — ยอมผ่านถ้ามี `ADMIN_KEY` ถูกต้อง **หรือ**
  `customer_id` ที่ส่งมาตรงกับเจ้าของออเดอร์จริงในชีต (เทียบจาก `findOrderLocation` ที่อ่านมาแล้ว) — ปิดทั้ง 2 ช่องโหว่
  พร้อมกัน (ไม่มี key เลย + ไม่เช็ค ownership เลย)

**Backend (`order-app-Code.gs`)**: เพิ่ม `isValidAdminKey(key)`, `doGet` ส่ง `e.parameter.key` ให้
`getAdminOrders`/`getAdminOrdersFull`, ทั้ง 7 ฟังก์ชันข้างต้นเช็คสิทธิ์ก่อนทำงานจริงทุกตัว

**Frontend (`index.html`)**:
- `postAction()` แนบ `key:ADMIN_KEY` อัตโนมัติให้ทุกคำขอที่ยิงตอน `isAdmin===true` (ครอบคลุม `updateDelivery`,
  `addBusinessNote`, `deleteBusinessNote`, และ `updateOrder` ที่แอดมินเรียก — ไม่ต้องแก้ทีละจุดเรียก)
- 4 จุดเรียก GET (`getAdminOrders`×3, `getAdminOrdersFull`×1) เติม `&key=${ADMIN_KEY}` ในสตริง URL ตรงๆ
- 2 จุดเรียกฝั่งลูกค้า (`saveEdit`→`updateOrder`, `cancelOrder()`) เพิ่ม `customer_id:customer.customer_id`
- เพิ่ม `escapeHtml()` helper แล้ว escape ทุกจุดที่ render `note`/business-note `text` ผ่าน `innerHTML` — **จริงๆ มี
  5 จุด ไม่ใช่ 4 จุดตามที่ backlog เดิมระบุ** (`buildHistoryCard`, `buildPendingAdminCard` ที่ backlog เดิมไม่ได้แยก
  จากจุดเรียกใน `renderAdminOrders` ที่ใช้กับแท็บ packing ให้ถูก, `buildDoneAdminCard`, `renderOverviewNotePanel`,
  และจุดที่ backlog เดิมไม่เคยพูดถึงเลยคือ textarea `editNote` ใน `renderEditModal` ที่ pre-fill note เดิมของลูกค้า
  ตอนเปิดหน้าแก้ไข) — **เจอจุดที่ 5 (`buildPendingAdminCard`) จากการรัน Playwright test จริงเท่านั้น** ตอนแรกไล่ด้วยตาเปล่า
  แล้วคิดว่าครบ 4 จุดตาม backlog แต่เทสจริงจับได้ว่า XSS payload ยังทำงานได้จากแท็บ "รอจัด" (default tab ของแอดมิน)

**ทดสอบแล้ว**:
- `node --check` ผ่านทั้ง 2 ไฟล์
- ทดสอบ logic `isValidAdminKey`/authorization ของ `updateOrder`/`cancelOrder` แยกใน Node (mock `PropertiesService`)
  ครบ 10 เคส ผ่านหมด
- Playwright E2E จริง (mock `page.route()`): ยืนยันว่า GET แอดมินแนบ `key=` ใน URL, POST แอดมินแนบ `key` ใน body,
  POST ลูกค้าแนบ `customer_id` ไม่มี `key`, และ XSS payload (`<img onerror>`) ไม่ทำงานในการ์ดแอดมิน render เป็น
  escaped text แทน — **เจอบั๊กจริงระหว่างเทส (จุดที่ 5 ด้านบน) แก้แล้วรันซ้ำผ่านหมด**

**พักไว้ไม่ทำ (ผู้ใช้ตัดสินใจแล้ว 8 ก.ย. 69)**: backlog ข้อ 4 (กัน comma ใน `parseItems`) — ผู้ใช้บอกว่าไม่สำคัญ
ไม่ต้องหยิบขึ้นมาเสนออีกโดยไม่ถูกถาม

✅ **[ทำเสร็จแล้ว] ข้อ 3 — คำนวณ `total` ใหม่ที่ backend (8 ก.ย. 69)**: เดิม `createOrder`/`updateOrder` เชื่อค่า
`data.total` จาก client 100% (คำนวณจาก `items.reduce` ฝั่ง browser แล้วส่งมาตรงๆ) ใครก็เปิด DevTools/ยิง API ตรงๆ
แก้ยอดก่อนส่งได้ — เพิ่ม `computeOrderTotal(itemsText, customerGroup)` คำนวณจากราคาสินค้าจริงใน Sheet "Product"

⚠️ **สำคัญที่พบระหว่างคุย**: ระบบราคาผูกกับ `customer_group` (per-customer-segment price list — สินค้าชื่อเดียวกันมี
หลายแถวคนละราคาต่อกลุ่ม) ถ้าคำนวณ total ใหม่แต่ยังเชื่อ `customer_group` ที่ client ส่งมา ช่องโหว่จะไม่ปิดจริง (แค่
เปลี่ยนจาก "โกหกยอดรวม" เป็น "โกหกกลุ่มราคา" แทน) แก้โดย:
- `createOrder`: เพิ่ม `findCustomerRow(id)` ดึง `product_group` จริงจาก Sheet "Customer" ตรงๆ ไม่เชื่อ
  `data.customer_group` เลย (fallback ไปใช้ `data.customer_group` เฉพาะกรณี `customer_id` หาไม่เจอในชีต)
- `updateOrder`: ใช้ `customer_group` ที่บันทึกไว้แล้วในแถวออเดอร์นั้น (ยืนยันความถูกต้องไปแล้วตอน `createOrder`)
  ไม่รับค่าจาก `data` เลย
- `computeOrderTotal` group-aware ตาม pattern เดียวกับ `getOrderProdMap()`/`getOrderProdFullMap()` ฝั่ง
  `index.html` เป๊ะ (filter ตาม group ก่อน, fallback ไปสินค้าทั้งหมดถ้า group นั้นไม่มีสินค้าเลย)

**ทดสอบแล้ว**: `node --check` ผ่าน, unit test แยกใน Node ครอบคลุมกรณี group-aware pricing ตรงๆ (สินค้าชื่อเดียวกัน
คนละราคาคนละกลุ่ม ต้องไม่ leak ราคากลุ่มอื่น), fallback เมื่อ group ไม่มีสินค้า, และสินค้าที่หาชื่อไม่เจอ (ราคา 0 ไม่ throw)
— ผ่านหมดทุกเคส **ไม่ได้แก้ frontend เลยรอบนี้** (`index.html` ยังส่ง `total`/`customer_group` แบบเดิม แค่ backend
เพิกเฉยแล้วคำนวณเองแทน) — จุดนี้ไม่กระทบ UX ปกติเลย

⚠️ **Trade-off ที่ยังไม่ได้แก้ (ความเสี่ยงต่ำ ไม่ได้ทำอะไรเพิ่ม)**: `findRecentMatchingOrder()` ฝั่ง `index.html`
(กันออเดอร์ซ้ำตอน retry) เทียบ `total` ที่ client คำนวณเองกับ `o.total` ที่ตอนนี้เป็นค่าที่ backend คำนวณใหม่แล้ว —
ปกติสองค่านี้ตรงกันเสมอเพราะข้อมูลราคามาจากแหล่งเดียวกัน มีโอกาสไม่ตรงกันเฉพาะกรณีที่ราคาสินค้าถูกแก้ในชีตระหว่าง
ที่ลูกค้ากำลังสั่งของพอดี (หน้าต่างเวลาแคบมาก) ถ้าเกิดขึ้นจริง dedup check จะไม่เจอออเดอร์เดิม ทำให้เกิดออเดอร์ซ้ำได้ตอน
retry — ยังไม่ได้แก้เพราะเป็น edge case ที่โอกาสเกิดต่ำมาก ถ้าเจอปัญหาออเดอร์ซ้ำอีกให้กลับมาดูจุดนี้ก่อน

✅ **Script Property `ADMIN_KEY` ตั้งค่าแล้ว + deploy แล้ว + frontend push แล้ว (ยืนยันจากผู้ใช้ 8 ก.ย. 69)** —
ลำดับ deploy ที่ใช้จริง (ปลอดภัย ไม่มีช่วงพัง): push frontend เข้า `main` ก่อน (Vercel auto-deploy, ส่ง key แต่
backend เก่ายังไม่เช็คเลยไม่กระทบอะไร) รอ deploy เสร็จ แล้วค่อยตั้ง Script Property + แปะ `order-app-Code.gs` +
deploy backend ทีหลัง — **ถ้าแก้ auth/key เรื่องนี้ต่อในอนาคต ให้แนะนำลำดับเดียวกันนี้เสมอ (frontend ก่อน backend
เสมอเมื่อ backend กำลังจะเริ่มบังคับ requirement ใหม่ที่ frontend เก่ายังไม่รู้จัก)**

### สถานะล่าสุด (อัปเดต 8 ก.ย. 69) — แก้การเทียบยอดขาย MoM/YoY ให้ตัดช่วงวันเท่ากัน

**ปัญหาที่พบ**: หน้าแดชบอร์ด "ภาพรวม → ยอดขาย" (`renderDashboardSales`) เทียบยอดขายเดือนที่ยังไม่จบ (เช่นวันนี้อยู่
วันที่ 8-9 ของเดือน) กับยอดขาย**เต็มเดือน**ก่อนหน้าตรงๆ — ทำให้ % เปลี่ยนแปลงดูแย่เกินจริงเสมอ (เทียบข้อมูล ~9 วัน
กับ ~30 วัน) ผู้ใช้เจอจากภาพจริง (เดือนนี้ 9 วันแรก ฿113,236 เทียบกับเดือนก่อนเต็มเดือน ฿448,070 → โชว์ "ลดลง 75%"
ทั้งที่ไม่ได้แย่ขนาดนั้นจริง)

**แก้แล้ว**: เพิ่ม `isInProgressMonth(y,m)` เช็คว่าเดือนที่กำลังดูอยู่คือเดือนปัจจุบันจริงๆ (ยังไม่จบ) หรือไม่ — ถ้าใช่
ตัดช่วงเดือนที่เอามาเทียบ (`prevE`) ให้เหลือแค่ "วันที่ 1 ถึงวันเดียวกับวันนี้" เท่านั้น (cap ไม่ให้เกินจำนวนวันจริงของ
เดือนที่เทียบ เผื่อกรณีเทียบกับเดือน ก.พ.) แทนที่จะเอาทั้งเดือนมาเทียบ — ใช้ได้ทั้งโหมด "เทียบเดือนก่อน" และ
"เทียบปีก่อน" (logic เดียวกัน ไม่ต้องแยก) เพิ่มข้อความ periodNote ต่อท้าย insight บอกช่วงวันที่กำลังเทียบให้ชัดเจน
เช่น "(เทียบเฉพาะวันที่ 1-8 เท่ากันทั้งสองช่วง เพราะเดือนนี้ยังไม่จบ)"

**เดือนที่จบแล้วไม่กระทบเลย** — เทียบเต็มเดือนต่อเต็มเดือนเหมือนเดิมทุกอย่าง (`isInProgressMonth` return false)
ปัญหานี้เกิดเฉพาะตอนดูเดือนที่ยังไม่จบเท่านั้น

**ทำไมไม่ใช้การหารเฉลี่ยต่อวันแทน** (ผู้ใช้เสนอมาเองว่าอาจต้องหารเฉลี่ยเพราะแต่ละเดือนวันไม่เท่ากัน) — ตัดช่วงให้
จำนวนวันเท่ากัน (day-range capped) แก้ปัญหา "จำนวนวันไม่เท่ากัน" ได้ในตัวอยู่แล้ว โดยไม่ต้องหารเฉลี่ยเพิ่ม การหารเฉลี่ย
จะจำเป็นก็ต่อเมื่อยืนกรานเทียบกับเต็มเดือนก่อน (จำนวนวันไม่เท่ากันจริง) ซึ่งไม่ใช่สิ่งที่ต้องการ

**ทำเดโม่ก่อน implement ตาม workflow**: ใช้ไฟล์ `index.html` จริงทั้งไฟล์ทับ (ไม่ใช่ mockup) override
`safeFetchJSON`/`postAction` ด้วยข้อมูลจำลอง (เดือนก่อนเต็มเดือน + เดือนนี้ถึงแค่วันนี้จริงตาม `new Date()`) แล้ว
override `renderDashboardSales` ด้วย logic ใหม่ ให้ดูผลจริงก่อน ได้ approve แล้วค่อย implement ใน `index.html` จริง

**ทดสอบแล้ว**: `node --check` ผ่าน + Playwright E2E จริง (mock `page.route()`) ยืนยัน 2 เคส — (1) เดือนที่ยังไม่จบ
โชว์ periodNote ถูกต้อง และ prevTotal คำนวณจากแค่ N วันแรกจริง ไม่ใช่เต็มเดือน (2) เดือนที่จบแล้ว **ไม่**โชว์
periodNote และยังเทียบกับเต็มเดือนก่อนเหมือนเดิม (regression check) — ผ่านหมดทุกเคส

### สถานะล่าสุด (อัปเดต 8 ก.ย. 69 — ต่อ) — ตรวจสอบฝั่งลูกค้า: ลบฟีเจอร์รีวิวปลอม + แก้ประสิทธิภาพ `getOrders`

**ตามคำขอ "ตรวจสอบแอปฝั่งลูกค้าว่ามีช่องโหว่/บั๊ก/หน่วงตรงไหนอีก"** ไล่โค้ดฝั่งลูกค้าทั้งหมด (ไม่ใช่แค่อ่านผ่านๆ —
ไล่ทุกจุดที่ยิง network call จริง) เจอ 3 จุด แก้ครบทั้ง 3 แล้ว:

✅ **[ลบแล้ว] ฟีเจอร์ "ให้คะแนน/ติชม" เป็นของปลอม ไม่เคยถูกต่อเข้าเมนูเลย** — `feedbackSection`/`reviewSection`
(รวม `setupReview`/`rateReview`/`submitReview`/`currentRating`) มีโค้ดสมบูรณ์ (ให้ดาว, คอมเมนต์, แยกร้าน/พนักงานคนที่
1/2) แต่ **ไม่มีทางเข้าถึงจากเมนูไหนในแอปเลยสักจุด** (เช็คแล้ว: ไม่มี `goSection('feedback')` เรียกจากที่ไหนนอกจาก
ปุ่มย้อนกลับที่วนกลับมาหน้าเดียวกันเอง) — ต่อให้เข้าถึงได้ก็เป็นของปลอมอยู่ดี (`submitReview()` แค่โชว์ "ขอบคุณ" ไม่เคย
ส่งข้อมูลไปไหนเลย ไม่มี action/sheet เก็บรีวิวใน backend เลยสักตัว) ผู้ใช้ยืนยันให้ลบทิ้งทั้งหมด (ไม่ต่อให้ใช้งานได้จริง)
— ลบ HTML 2 section + ฟังก์ชัน JS 3 ตัว + ตัวแปร 1 ตัว + จุดอ้างอิงใน `goHome()`/`goSection()` ครบ เช็คแล้วไม่มี
ที่ไหนอ้างอิงเหลือค้าง (`grep` ทั้งไฟล์ = 0 ผลลัพธ์)

✅ **[แก้แล้ว] `getOrders` ฝั่งลูกค้าไม่มี limit เลย — เหมือน `getAdminOrders` ก่อนแก้เป๊ะ แต่หนักกว่า**: เรียกจาก
6 จุดในแอป ส่งประวัติ**ทั้งหมดตลอดชีพ**ของลูกค้าทุกครั้ง จุดที่หนักสุดคือหน้า track ที่ auto-refresh ทุก **10 วิ**
(เร็วกว่า `getAdminOrders` เดิมที่เป็น 30 วิ) — ยิ่งลูกค้าสั่งสะสมมานาน ยิ่งช้าลงเรื่อยๆ เฉพาะคนนั้น เพิ่ม action ใหม่
`getTrackOrders(customer_id)` ใน `order-app-Code.gs` ทำ filter ที่ backend ให้ตรงกับที่ frontend กรองทิ้งอยู่แล้ว
(active/pending/packing ทุกอันไม่ว่าจะเก่าแค่ไหน + เสร็จ/ยกเลิกล่าสุดอย่างละ 5 อัน — ค่าคงที่
`TRACK_ORDERS_RECENT_LIMIT`) — สลับไปใช้ตัวใหม่ 4 จาก 6 จุด (`loadTrackSection`, auto-refresh, dedup-check ใน
`findRecentMatchingOrder`, `openReorderModal`) **`loadHistory()` (แท็บ "ประวัติทั้งหมด") ยังคงใช้ `getOrders` เดิม
ตั้งใจไม่แตะ** เพราะจุดนั้นต้องการเห็นทุกออเดอร์จริงๆ ตามชื่อฟีเจอร์ ไม่ได้ auto-refresh ถี่เหมือนหน้า track เลยความเสี่ยง
ต่ำกว่ามาก — **หน้าตาที่ลูกค้าเห็นเหมือนเดิมทุกอย่าง ไม่เปลี่ยนอะไรที่มองเห็นได้เลย** เปลี่ยนแค่ปริมาณข้อมูลที่ส่งมา

✅ **[แก้แล้ว] `placeOrder()` ยิง `getOrders` ซ้ำโดยไม่จำเป็นหลังสั่งของสำเร็จ**: `createOrder` คืน `order_id` จริง
มาให้ในตัวอยู่แล้วตอนสำเร็จ (`result.order_id`) แต่โค้ดเดิมไม่ใช้ค่านั้น กลับไปยิง `getOrders` (ตอนนี้คือ
`getTrackOrders` แล้ว) ซ้ำเพื่อ "เดา" เอาจาก `orders[0]` แทน — ตัดการยิงซ้ำทิ้งทั้งก้อน ใช้ `result.order_id` ตรงๆ
(fallback เป็น `pendingOid` เดิมเฉพาะกรณี `result.order_id` หายไปจริงๆ) ปิดทั้งการยิง request เปล่าประโยชน์ในจุดที่
ลูกค้าใช้บ่อยที่สุดของแอป และปิดความเสี่ยงที่เคยมีว่า `orders[0]` อาจไม่ใช่ออเดอร์ที่เพิ่งสั่งจริงถ้ามีออเดอร์อื่นแทรกมา

**ทดสอบแล้ว**: `node --check` ผ่านทั้ง 2 ไฟล์ + unit test แยกของ `getTrackOrders()` ใน Node (mock ข้อมูล 8 done/
8 cancelled/2 active คนละอายุ ยืนยันว่ากรองเหลือแค่ active ทั้งหมด+ล่าสุดอย่างละ 5 ถูกต้อง และไม่หลุดข้อมูลลูกค้าอื่นมา
ปนด้วย) + Playwright E2E จริงยืนยัน 12 เคสครบทั้ง 3 เรื่อง (ฟีเจอร์รีวิวหายสนิท, 4 จุดใหม่เรียก `getTrackOrders` ไม่ใช่
`getOrders`, `loadHistory` ยัง unaffected เหมือนเดิม, `placeOrder` เรียกแค่ `createOrder` ไม่มี fetch ซ้ำ และโชว์
order_id ที่ถูกต้องจาก response จริง) — ผ่านหมดทุกเคส

### สถานะล่าสุด (อัปเดต 8 ก.ย. 69 — ต่อ) — แก้เข้าแอปแบบไม่มี `?ref=` โชว์ error ผิดเรื่อง

**พบระหว่างไล่หา "จุดที่ enhance ได้อีก" ตามคำขอผู้ใช้**: เปิด `index.html` แบบไม่มี query string เลย (เช่น
`https://samurai-murex.vercel.app/` เฉยๆ) จะเห็นหน้า home ที่ดูใช้งานได้ปกติ (โชว์ "สวัสดีครับ" เฉยๆ ไม่มีชื่อ) แต่
`customer` เป็น `null` อยู่เบื้องหลัง — กดปุ่ม "สั่งสินค้า" จะเห็นร้านว่างเปล่า (ไม่มี error) และกดปุ่ม "ติดตามสถานะ"
**ไม่ได้พัง/ไม่มี JS error เต็มจอ** (แก้คำพูดตัวเองจากตอนแรกที่บอกว่า "พัง" — เกินจริง) เพราะ `loadTrackSection()`
อ่าน `customer.customer_id` อยู่ใน `try{}` block ที่ดักไว้อยู่แล้ว — สิ่งที่เกิดขึ้นจริงคือ**โชว์ข้อความ "โหลดข้อมูลไม่ได้"**
ซึ่งหลอกลูกค้าว่าเน็ตมีปัญหา ทั้งที่จริงๆ ไม่เกี่ยวเน็ตเลย (สาเหตุจริงคือไม่มีข้อมูลลูกค้าอยู่เบื้องหลังต่างหาก) — ยืนยันด้วย
เทส before/after เทียบโค้ดจริง 2 เวอร์ชันแล้ว (ดูหัวข้อทดสอบด้านล่าง) ไม่ใช่แค่เดา

**ทำไมเกิดขึ้นได้ทั้งที่ลิงก์ที่ให้ลูกค้ามี `?ref=` เสมอ**: `manifest.json` (ผู้ใช้ตั้งใจทำไอคอน PWA ไว้เอง — มี
`icon-192.png`/`icon-512.png`/เวอร์ชัน black ครบ) ตั้ง `start_url: "/"` — ไม่มี `?ref=` ติดไปด้วย เพราะเป็นไฟล์เดียว
ใช้ร่วมกันทุกคน ใส่ค่าเฉพาะคนไม่ได้ — พฤติกรรมมาตรฐานของ PWA คือตอนลูกค้ากด "ติดตั้งแอป"/"เพิ่มหน้าจอโฮม"
เบราว์เซอร์จะจำแค่ `start_url` จาก manifest ไปใช้เปิดทุกครั้งในอนาคต **ไม่ได้จำ URL ที่กดติดตั้งตอนนั้น** — แปลว่า
ลูกค้าคนไหนก็ตามที่กด "ติดตั้งแอป" จากลิงก์ส่วนตัวของตัวเอง ไอคอนที่ได้จะพาไปหน้าที่พังแบบนี้ทุกครั้งที่เปิดในอนาคต

✅ **แก้แล้ว**: `initCustomer()` จำ `customerRef` ที่เพิ่งเข้าสำเร็จไว้ใน `localStorage`
(`samurai_customer_ref`) — `window.onload` เมื่อเจอ query string ว่างเปล่าเลย จะลองอ่านค่านี้ก่อน ถ้ามีจะ
`location.replace` เด้งกลับไปที่ `?ref=<ค่าที่จำไว้>` อัตโนมัติ (โหลดร้านของลูกค้าคนนั้นได้ปกติ ไม่ต้องเจอข้อความ error
ผิดเรื่องอีกต่อไป) ถ้าไม่มีเลย (เครื่องใหม่/ล้าง storage) แสดง `showError(false)` (หน้า "ลิงค์นี้อาจไม่ถูกต้อง กรุณาติดต่อ
ร้านค้าครับ" — ข้อความตรงประเด็นจริง) แทนที่จะปล่อยให้ไปเจอหน้า home ที่ดูปกติแต่กดอะไรก็ได้ข้อความหลอก —
ไม่ได้แตะ backend เลย แก้แค่ `index.html`

**ทดสอบแล้ว 2 รอบ**:
1. Playwright E2E จริง (ใช้ browser context เดียวกันเพื่อให้ localStorage ข้ามหน้าได้เหมือนอุปกรณ์จริง) ยืนยัน 3 เคส:
   เข้าด้วย `?ref=` ปกติแล้ว ref ถูกจำไว้ / เข้าแบบไม่มี query string ทีหลัง (จำลองเปิดผ่านไอคอน PWA) เด้งกลับไปโหลด
   ร้านถูกต้องอัตโนมัติ / browser context ใหม่ที่ไม่เคยมี ref จำไว้เลยโชว์หน้า error ที่ตรงประเด็น
2. **ผู้ใช้ขอให้ทดสอบเทียบก่อน-หลังให้เห็นผลต่างจริงก่อนอนุญาตไปต่อ** — ทำเทส before/after รันโค้ดจากคอมมิตล่าสุด
   (ยังไม่มี fix) เทียบกับโค้ดที่แก้ในเครื่อง (มี fix) ด้วยสถานการณ์เดียวกันเป๊ะ ผลคือก่อนแก้: ลูกค้าค้างอยู่หน้า home
   ที่ไม่มีข้อมูล กด "ติดตามสถานะ" โชว์ "โหลดข้อมูลไม่ได้" (ข้อความหลอก) — หลังแก้: เด้งกลับไปโหลดร้านสำเร็จอัตโนมัติ
   ไม่มี JS error ทั้งสองเวอร์ชัน (ยืนยันว่าไม่ใช่ "crash" แบบที่เคยพูดผิดไปตอนแรก) — ผ่านหมดทุกเคสทั้ง 2 รอบ

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69) — ออเดอร์ซ้ำจริงจาก cold-connection timeout + แก้ด้วย idempotency key

**บริบท**: ลูกค้ารายงานเจอ alert "บันทึกคำสั่งซื้อไม่สำเร็จ" ตอนสั่งของ (~07:39) ตรวจสอบแล้วพบว่า `createOrder`
บันทึกสำเร็จจริง (ร้านเห็นออเดอร์) แต่ response หลุด/timeout กลับมาหา client (cold connection ปัญหาเดิมที่เคย
บันทึกไว้แล้วในไฟล์นี้) — ลูกค้าเห็น alert เลยกดสั่งซ้ำเอง ยืนยันจาก Telegram ที่แจ้งเตือน "ออเดอร์ใหม่" 2 รอบ ->
**เกิดออเดอร์ซ้ำจริงในชีต** ไม่ใช่แค่ false-alarm ทาง UX เท่านั้น (ตรงกับ trade-off ที่เคย flag ไว้ตอนแก้เรื่อง
คำนวณ `total` ใหม่ที่ backend เมื่อ 8 ก.ย. 69 — แต่ครั้งนี้ต้นตอเป็น connection ล้มเหลวทั้ง `createOrder` และ
`findRecentMatchingOrder`'s dedup check พร้อมกัน ไม่ใช่กรณี `total` ไม่ตรงกันตามที่เคยคาดไว้)

**Vercel logs ไม่เกี่ยวข้องกับบั๊กประเภทนี้เลย**: แอปนี้เป็น static site ล้วน ยิง request ตรงจาก browser ไปหา
Google Apps Script โดยไม่ผ่าน Vercel backend ใดๆ — ถ้าเจอรายงาน "สั่งของ/บันทึกไม่สำเร็จ" ในอนาคต ไม่ต้องเสียเวลา
เช็ค Vercel runtime logs/errors เลย ให้เช็ค Apps Script Executions log แทน (ตามธรรมเนียมเดิมของแอปเช็คสต๊อก)

**แก้แล้ว (ยังไม่ deploy backend — รอผู้ใช้แปะ `order-app-Code.gs` ใหม่)**: เพิ่ม idempotency key กัน
`createOrder` สร้างแถวซ้ำ:
- `index.html`: `showConfirm()` สร้าง `orderIdemKey` ใหม่ทุกครั้งที่เข้าหน้ายืนยันออเดอร์ (คู่กับ `pendingOid` เดิม)
  ส่งไปกับ payload ของ `placeOrder()` — key เดิมนี้ถูกใช้ซ้ำทั้งตอน auto-retry ของ `postAction` และตอนลูกค้ากดปุ่ม
  "ยืนยันสั่งซื้อ" ซ้ำเองด้วยมือ (เพราะไม่ได้เรียก `showConfirm()` ใหม่) จนกว่าจะสำเร็จจริงหรือกลับไปเริ่มออเดอร์ใหม่
  (`showOrderSuccessScreen()` reset เป็น `''`) — เปิด auto-retry ให้ `createOrder` แล้ว (`postAction(...,15000,1)`
  จากเดิม `retries=0`) เพราะตอนนี้ปลอดภัยแล้วที่ backend
- `order-app-Code.gs`: `createOrder` ห่อด้วย `LockService.getScriptLock()` (pattern เดียวกับ `updateDelivery`)
  เช็ค `CacheService` ก่อนว่าเคยสร้างออเดอร์ด้วย `idempotency_key` นี้แล้วหรือยัง (TTL 5 นาที) ถ้าเจอคืน `order_id`
  เดิมกลับไปเลยไม่ `appendRow` ซ้ำ — **ไม่บังคับว่าต้องมี key** (frontend เก่าที่ cache ค้างยังทำงานได้ปกติ แค่ไม่มี
  dedup protection ให้ เหมือนพฤติกรรมเดิมก่อนแก้)

**ทดสอบแล้ว**:
- `node --check` ผ่านทั้ง 2 ไฟล์
- Unit test แยกใน Node (mock `LockService`/`CacheService`/`SpreadsheetApp`) ครอบคลุม 4 เคส: key เดิมซ้ำ ->
  ได้ `order_id` เดิม + `appendRow` แค่ 1 ครั้ง, key ต่างกัน -> คนละออเดอร์, ไม่มี key เลย -> พฤติกรรมเดิม (ไม่ dedup,
  backward compat), lock ชนกัน -> คืน error สุภาพไม่เขียนแถว — ผ่านหมด
- Playwright E2E จริง (mock `page.route()`) 3 เคส: (1) attempt แรกพัง auto-retry สำเร็จ -> ไม่โชว์ alert เลย
  ทั้ง 2 ครั้งส่ง key เดียวกัน (2) ทั้ง 2 attempt พังหมด -> โชว์ alert ไม่สำเร็จ แล้วกดปุ่มซ้ำเอง (manual retry) ->
  ยังใช้ key เดิมทุกครั้งทั้ง 4 คำขอ (3) สั่งสำเร็จแล้วกลับไปสั่งออเดอร์ใหม่รอบสอง -> ได้ key ใหม่ไม่ซ้ำของเดิม —
  ผ่านหมดทุกเคส

**ขั้นต่อไป**: ผู้ใช้ต้องแปะ `order-app-Code.gs` เวอร์ชันใหม่ใน Apps Script Editor (Manage deployments -> New
version) — ยังไม่ได้ push ขึ้น `main` (รอ approve ตามธรรมเนียมเดิม) frontend/backend รอบนี้ compatible กันทั้ง
2 ทิศทาง (deploy backend ก่อนหรือหลัง push frontend ก็ได้ ไม่มีช่วงพัง เพราะ backend ไม่บังคับว่าต้องมี key)

⚠️ **[พักไว้ 10 ก.ย. 69] ผู้ใช้ขอเก็บ fix นี้ไว้ก่อน** — ยังไม่ deploy/push ทั้งคู่ รอแก้อีกเรื่องหนึ่งให้เสร็จก่อน
แล้วจะขึ้นระบบ (deploy backend + push frontend) พร้อมกันทีเดียว โค้ดทั้ง 2 ไฟล์ในเครื่อง/ใน repo นี้แก้เสร็จพร้อม
ใช้แล้ว แค่รอคำสั่งให้ deploy จริง — **ห้าม push/เตือนให้ deploy fix นี้เองโดยไม่ถูกถาม**

### สถานะล่าสุด (อัปเดต 10 ก.ย. 69 — ต่อ) — "อีกเรื่อง" ที่รออยู่คือบั๊ก lock ค้างเพราะ Telegram (พบ+แก้แล้ว)

**บริบท**: ผู้ใช้ส่งภาพจริงจากฝั่งแอดมิน — กด "จัดเสร็จแล้ว" (`markDone`) แล้วเจอ error
`updateDelivery: ระบบกำลังประมวลผลคำขออื่นอยู่ กรุณาลองใหม่อีกครั้ง` (ข้อความจาก `LockService` ที่เพิ่มไป 8 ก.ย. 69)
นี่คือ "อีกเรื่อง" ที่ผู้ใช้บอกว่าจะรอแก้ก่อนขึ้นระบบพร้อมกับ idempotency key fix ด้านบน

**Root cause**: `sendTelegramNotify()` (network call ใช้เวลาไม่แน่นอน) เดิมถูกเรียก**ข้างใน**
`try{...}finally{lock.releaseLock()}` ของ `updateDelivery` — ถ้า Telegram ช้า lock จะถูกถือค้างนานเกิน 10 วิได้
คำขอ `updateDelivery` อื่นที่เข้ามาพร้อมกัน (เช่น auto-retry ของ `postAction` ที่ `markDone()`/`markPacking()`
ไม่ได้ปิด retry ไว้ ใช้ default `retries=1`) จะ `waitLock` timeout เห็น error นี้ ทั้งที่คำขอแรกอาจสำเร็จจริงอยู่ดี
— รูปแบบบั๊กเดียวกับเคสออเดอร์ซ้ำตอนเช้า (10 ก.ย. 69) แค่คนละฟังก์ชัน

⚠️ **เจอบั๊กแฝงตัวเดียวกันใน `createOrder` ที่เพิ่งแก้ไปก่อนหน้านี้ในเซสชันเดียวกัน (ยังไม่ deploy)** — ตอนเขียน
lock+idempotency ให้ `createOrder` ผมคัดลอกโครงเดิมมาโดยไม่ทันสังเกตว่า `sendTelegramNotify()` อยู่ในลูปก่อน
`lock.releaseLock()` เหมือนกัน ถ้า deploy ไปตอนนั้นจะเจอปัญหาเดียวกันกับฝั่งลูกค้าได้ — แก้พร้อมกันในรอบนี้เลย

**แก้แล้วทั้ง 2 จุด**: ย้าย `sendTelegramNotify()` ไปเรียกใน `finally` **ต่อจาก** `lock.releaseLock()` (เก็บแค่
message string ไว้ในตัวแปร `notifyMessage` ระหว่างอยู่ใน critical section) — lock จะถือแค่ช่วงอ่าน/เขียนชีต
(เร็ว) เท่านั้น ไม่ถือระหว่างรอ Telegram ตอบกลับอีกต่อไป ไม่ต้องปรับ timeout 10 วิหรือ logic อื่นเลย

**ทดสอบแล้ว**:
- `node --check` ผ่าน
- Unit test ใหม่ 3 เคส (mock เก็บลำดับการเรียก call log): `createOrder` และ `updateDelivery` (status `done`)
  ยืนยันว่า `releaseLock` เกิดก่อน `sendTelegramNotify` เสมอ, `updateDelivery` (status `packing`) ยืนยันว่ายังไม่
  แจ้ง Telegram เหมือนเดิม (ไม่ใช่ regression) — ผ่านหมด
- รัน unit test ชุด idempotency key เดิม (4 เคส) ซ้ำอีกรอบ ยืนยันว่าไม่มี regression จากการย้าย Telegram —
  ผ่านหมดเหมือนเดิม

✅ **[อัปเดตสถานะ deploy — เสร็จแล้วทั้งคู่ 10 ก.ย. 69]** ผู้ใช้สั่ง "Push" ให้ทั้ง 2 fix ขึ้นพร้อมกันตามแผนเดิม:
- Push ขึ้น `main` แล้ว (commit `50fc597`) — ต้องใช้ `mcp__Claude_Code_Remote__add_repo` (access: push) ขอสิทธิ์
  push ให้ session นี้ก่อนถึงจะ push ผ่าน (รอบแรกที่ลองถูก auto-mode classifier บล็อกไว้เพราะเป็นการขอสิทธิ์
  ยกระดับ — ต้องให้ผู้ใช้ยืนยันชัดเจนก่อนถึงลองซ้ำแล้วผ่าน) — ถ้าเจอ push ไม่ผ่านแบบ "access denied by git proxy...
  not in this session's authorized repository set" อีกในอนาคต ให้นึกถึงขั้นตอนนี้ก่อน ไม่ต้องแปลกใจ
- Vercel auto-deploy frontend จาก push นี้ให้แล้ว
- ผู้ใช้แปะ `order-app-Code.gs` เข้า Apps Script Editor เองแล้ว (New version) ยืนยันแล้วว่า "แปะ backend เสร็จแล้ว"

⚠️ **[รอผลทดสอบจริง — ยังไม่ได้รับการยืนยัน]** ผู้ใช้บอกว่า "รอทดสอบสั่งของจริงอีกรอบ" (12 ก.ย. 69 ยังไม่มีรายงาน
ผลกลับมาในเซสชันนี้) — session ถัดไปควรถามผลทดสอบก่อนเป็นอันดับแรกถ้ายังไม่ถูกบอก โดยเฉพาะ 2 จุดที่ fix รอบนี้
แก้ตรงๆ: (1) ฝั่งลูกค้า สั่งของตอนเน็ตสะดุด/ช้าไม่ควรเจอ "บันทึกคำสั่งซื้อไม่สำเร็จ" ง่ายๆ อีก แม้กดซ้ำก็ไม่ควรสร้าง
ออเดอร์ซ้ำ (2) ฝั่งแอดมิน กด "จัดเสร็จแล้ว"/"เริ่มจัดสินค้า" ไม่ควรเจอ "ระบบกำลังประมวลผลคำขออื่นอยู่" อีก — ถ้ายังเจอ
ปัญหาใดปัญหาหนึ่งซ้ำ แปลว่า fix รอบนี้ไม่ครอบคลุมพอ ต้องไล่หาสาเหตุเพิ่ม อย่าเดาว่าแก้ครบแล้ว

**เคสนี้ยังต้องติดตามต่อ**: แนะนำให้ร้านเช็คว่าออเดอร์ซ้ำที่เกิดจริงเมื่อเช้า 10 ก.ย. 69 (ก่อน fix นี้ขึ้นระบบ) ถูก
ยกเลิก/จัดการแล้วหรือยัง (ไม่ใช่ปัญหาโค้ด แต่เป็นข้อมูลที่ค้างอยู่ในชีตจากก่อนแก้ — ยังไม่ได้รับการยืนยันจากผู้ใช้)

### สถานะล่าสุด (อัปเดต 12 ก.ย. 69) — ออเดอร์เดียวกันมี 2 ยอดต่างกัน (`ORD-120926-003820`) + เพิ่ม lock/audit log ให้ `updateOrder`

**บริบท**: ผู้ใช้พบว่าออเดอร์ `ORD-120926-003820` (ลูกค้าคุณมาร์ค) มี 2 ยอดปรากฏต่างกัน — บิลตอนสั่ง/ตอนจัดของจริง
(00:38 น.) แสดง 2,527 บาท แต่ Telegram แจ้ง "✅ จัดเสร็จแล้ว" ตอน 05:37 น. แสดง 2,917 บาท (รายการสินค้าต่างกันจริง
ไม่ใช่แค่ปัดเศษ) พอเปิดชีต `Order_2569` ดูภายหลังกลับเจอ 2,527 — ผู้ใช้ยืนยันหนักแน่นว่า **ไม่มีใครแตะ +/- หรือแก้ไข
ออเดอร์ด้วยมือเลย** และยืนยันว่า **2,917 คือยอดที่ถูกต้อง** (ตรงกับสิ่งที่จัดให้ลูกค้าจริง)

**ไล่ตรวจแล้วพบข้อเท็จจริงจากโค้ด** (ไม่ใช่การเดา):
- `order_id` ถูกสร้างจากเวลาจริงฝั่ง server (`new Date()` ตอน `createOrder` รัน) ไม่ใช่ค่าที่ client ส่งมา — ดังนั้น
  ยืนยันได้ว่าออเดอร์นี้ถูกสร้างครั้งเดียวจริงตอน 00:38:20 (ตรงกับหลัง 6 หลักท้ายของ order_id เป๊ะ) ไม่ใช่ 2 ออเดอร์
  ที่บังเอิญได้เลขซ้ำกัน
- มีจุดเดียวในระบบทั้งหมดที่แก้ `items`/`total` ของออเดอร์ที่มีอยู่แล้วได้ คือ `updateOrder` — เรียกได้ทั้งจากลูกค้า
  เอง (ปุ่ม "แก้ไข" ที่หน้าติดตามสถานะ, `saveEdit()`), แอดมิน (`saveAdminEdit()`), หรือระบบ auto-recalc ตอนกด
  "จัดเสร็จแล้ว" ถ้าจำนวนต่างจากเดิม (`markDone()`) — **เดิม `updateOrder` ไม่มี `LockService` และไม่มี log การแก้ไข
  เก็บไว้เลยแม้แต่นิดเดียว** ต่างจาก `createOrder`/`updateDelivery` ที่มี lock แล้ว
- เช็ค Apps Script Executions log ช่วง 5:37:49–5:38:05 น. (วันเกิดเหตุ) พบ `doPost` ยิงรัวถึง **8 ครั้งในเวลาไม่ถึง
  20 วินาที** (ปกติกด "จัดเสร็จแล้ว" ควรมีแค่ 1-2 request) แต่ log ไม่บอกว่าแต่ละคำขอเป็น action ไหน/พารามิเตอร์อะไร
  เพราะทุก action วิ่งผ่าน `doPost` เดียวกันหมดและไม่เคย log ไว้ — **สรุปว่าสาเหตุที่แท้จริงระบุชี้ชัดไม่ได้ 100%**
  (ไม่มีหลักฐานเพียงพอจะฟันธงว่าเป็น retry ซ้อนกัน/สองอุปกรณ์แก้พร้อมกัน/อื่นๆ) แต่ยืนยันได้แน่ชัดว่า `updateOrder`
  ถูกเรียกอย่างน้อย 1 ครั้งหลัง 05:37 เพื่อเปลี่ยนยอดกลับเป็น 2,527 — ไม่มีทางอื่นในโค้ดที่ทำแบบนี้ได้
- ผู้ใช้เสนอเพิ่มว่าช่วงนั้นเคยลบแถวออเดอร์ซ้ำ 2-3 ใบด้วยมือ (ลูกค้ากดสั่งซ้ำเพราะแอปค้าง) — ตรวจแล้วว่า **การลบแถว
  ไม่กระทบ order_id/total ของแถวอื่นที่เหลืออยู่เลย** (order_id คำนวณจากเวลาสร้างจริง คนละแถวคนละเลขเสมอ) จึงน่าจะ
  ไม่ใช่สาเหตุโดยตรง แต่ชี้ให้สงสัยว่ายอด "2,527" ที่ผู้ใช้จดไว้ในสมุด/ที่จำได้ อาจเป็นยอดของออเดอร์ซ้ำอีกใบที่ถูกลบไป
  แล้ว (จำสลับแถวกันตอนรีบไล่ลบ) มากกว่าจะเป็นยอดจริงของ `ORD-120926-003820` — ยังไม่ได้รับการยืนยัน/ปฏิเสธจากผู้ใช้

**แก้ไปแล้ว 2 อย่าง (deploy จริงแล้ว, push เข้า `main` แล้ว — commit `ba977d1`)**:
1. **แก้ข้อมูลย้อนหลัง**: ผู้ใช้แก้ `items`/`total` ของแถว `ORD-120926-003820` ในชีตด้วยมือตรงๆ ให้กลับไปเป็น 2,917
   ตามรายการในบิลที่ถูกต้อง (ไม่ได้ยิงผ่าน `updateOrder` เพราะตอนนั้นยังไม่มี lock/log ป้องกัน)
2. **`updateOrder` ใน `order-app-Code.gs` เพิ่ม**:
   - `LockService.getScriptLock()` ห่อทั้งฟังก์ชัน (แพทเทิร์นเดียวกับ `updateDelivery`/`createOrder`) — กัน request
     ชนกันจนเขียนทับแบบสุ่มอีก
   - Log ทุกครั้งที่แก้ไขสำเร็จลงชีตใหม่ `OrderEditLog` (คอลัมน์: `log_id, order_id, timestamp, edited_by,
     items_before, total_before, items_after, total_after`) — `edited_by` เป็น `'admin'` หรือ `'customer:<id>'`
     แยกได้ว่าใครแก้ — ชีตนี้**ต้องสร้างเองก่อนใช้งาน** (สร้างแล้ว ณ วันที่แก้) ถ้าไม่มีชีตนี้ `updateOrder` จะยังทำงาน
     ปกติแค่ข้ามการ log เงียบๆ ไม่พังทั้งฟังก์ชัน

⚠️ **สำคัญมาก — ครั้งหน้าถ้าเจอเคส "ยอดออเดอร์ไม่ตรงกัน" อีก ให้เปิดชีต `OrderEditLog` ดูก่อนเป็นอันดับแรกเลย**
(กรอง `order_id` ที่มีปัญหา) จะเห็นค่าก่อน/หลังทุกครั้งที่มีการแก้ไข พร้อมเวลาและคนแก้ — ไม่ต้องไปงมใน Apps Script
Executions log แบบรอบนี้อีกแล้ว (log นั้นเก็บแค่ไม่กี่วันและไม่มีรายละเอียดพารามิเตอร์ให้ดูเลย)

**ยังไม่ได้ทดสอบ**: ยังไม่มีการยืนยันว่าหลัง deploy แล้ว แก้ไขออเดอร์จริง 1 ครั้งแล้วชีต `OrderEditLog` มีแถวขึ้นมาถูกต้อง
จริงหรือไม่ — session ถัดไปควรถามผลตรงนี้ก่อนถ้ายังไม่ถูกบอก

### สถานะล่าสุด (อัปเดต 13 ก.ย. 69) — เพิ่ม `keepWarm()` กัน cold start ของ Apps Script

**บริบท**: ผู้ใช้เจอ `initAdmin:getAdminOrders: หมดเวลาเชื่อมต่อ (timeout)` ตอนเปิดหน้าแอดมิน — reload อีกรอบเดียวก็หาย
สรุปว่าเป็น **Apps Script cold start** (สคริปต์ "หลับ" เมื่อไม่มีคนเรียกสักพัก คำขอแรกหลังจากนั้นเลยช้าจนชน timeout
15 วิ × 2 ครั้ง = ~30 วิ ที่ `initAdmin` ตั้งไว้) ไม่เกี่ยวกับ `updateOrder`/fix อื่นๆ ที่เพิ่งแก้ไปก่อนหน้านี้เลย

**แก้แล้ว (deploy จริงแล้ว, push เข้า `main` แล้ว — commit `d2dbc0a`)**: เพิ่มฟังก์ชัน `keepWarm()` ใน
`order-app-Code.gs` — ฟังก์ชันเปล่าๆ แค่ log เวลา ไม่แตะ Sheet/Cache/Lock ใดๆ เลย (กินโควต้า execution time
แทบเป็น 0 วิ/ครั้ง) ผู้ใช้ตั้ง **time-driven trigger เรียกทุก 5 นาที** ใน Apps Script Editor เองแล้ว (Triggers →
Add Trigger → keepWarm → Time-driven → Minutes timer → Every 5 minutes → Save) — เป้าหมายคือกันไม่ให้สคริปต์
มีโอกาส "หลับ" ตั้งแต่แรก แทนที่จะให้ผู้ใช้จริงเป็นคนจ่าย cost ของ cold start ตอนเปิดหน้า

⚠️ **[รอผลทดสอบจริง — ยังไม่ได้รับการยืนยัน]** เพิ่งตั้ง trigger เสร็จ ยังไม่มีทางเห็นผลทันที (ทดสอบตอนสคริปต์ยัง
"ตื่น" อยู่จากการใช้งานเมื่อครู่ ไม่มีทางต่างจากก่อนตั้ง trigger เลย) — จุดที่จะพิสูจน์ได้จริงคือ **ตอนเปิดหน้าแอดมิน
ครั้งแรกหลังไม่มีคนใช้แอปมาข้ามคืน/หลายชั่วโมง** ถ้าเปิดมาเร็วปกติไม่เจอ timeout อีก แปลว่าได้ผล — session ถัดไป
ควรถามผลตรงนี้ก่อนถ้ายังไม่ถูกบอก ถ้ายังเจอ timeout อยู่ ให้เช็คก่อนว่า trigger ยังทำงานอยู่จริงไหม (ดู "Last run"
ในหน้า Triggers หรือกรอง `keepWarm` ใน Executions log ว่ารันสม่ำเสมอทุก 5 นาทีจริงหรือเปล่า)

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69) — เหตุผลแก้ไขรายรายการ + สอบสวนเหตุการณ์เชื่อมต่อไม่นิ่งช่วงเช้า + UX โหลด/error เงียบขึ้น

**บริบทกว้าง**: session ยาวมาก เริ่มจากขยายฟีเจอร์ "เหตุผลแก้ไขออเดอร์ตอนจัดของ" (มีมาจาก session ก่อนหน้า) ต่อด้วยเจอ
incident เชื่อมต่อไม่นิ่งช่วงเช้าที่กระทบธุรกิจจริง (ส่งยอดลูกค้าไม่ได้หลายเจ้า) แล้วปิดท้ายด้วยงาน UX ปรับปรุง
ประสบการณ์ตอนเน็ต/backend ไม่นิ่ง — **ทุกอย่างด้านล่างนี้ push + deploy ครบทั้ง 2 ฝั่งแล้ว (ยืนยันจากผู้ใช้)**

**1. เหตุผลแก้ไขออเดอร์ — เปลี่ยนจากเหตุผลเดียวรวมเป็นแยกรายรายการ**: ผู้ใช้ชี้ว่าถ้าแก้หลายสินค้าพร้อมกัน แต่ละ
รายการอาจคนละเหตุผล (ตัวนึงหมด อีกตัวลูกค้าขอเปลี่ยน) รวมเป็นเหตุผลเดียวผิด — แก้ทั้ง 2 ฝั่ง:
- **Frontend**: modal เหตุผลตอนนี้แสดง 1 การ์ดต่อ 1 รายการที่เปลี่ยนจริง แต่ละการ์ดมีปุ่ม "หมด"/"อื่นๆ" + ช่องพิมพ์
  ของตัวเอง ส่งเป็น `itemReasons` object `{ชื่อสินค้า: เหตุผล}` แทนสตริงเดียว — modal ปรับให้ลอยกึ่งกลางจอมีขอบ
  รอบด้าน (เดิมเต็มจอ) ตามที่ผู้ใช้ขอ
- **Backend**: `formatItemsDiffWithReasons()` คืน array แยกทีละรายการ (ไม่ join คอมมา) เก็บ `change_summary` เป็น
  หลายบรรทัดคั่น `\n` ต่อรายการ ให้ `getOrderEditSummary()` แยกกลับเป็นคนละบูลเล็ตได้ตอนสรุปใส่ Telegram — **เลิก
  เขียนคอลัมน์ `reason` แยก** (ซ้ำซ้อนกับ change_summary ที่ผูกเหตุผลไว้ต่อรายการแล้ว) **เปลี่ยนไปเขียน
  `total_diff`** (ยอดหลัง−ก่อน) แทน — ต้องเพิ่มหัวคอลัมน์ `total_diff` ใน sheet OrderEditLog เอง (ทำแล้ว)
- ข้อความ Telegram ตอนจัดเสร็จตอนนี้บอกส่วนต่างยอดชัดเจน: "ยอดเดิม ฿X → ยอดนี้ ฿Y, ต่างกัน ±Z บาท" ตามด้วย
  บูลเล็ตแยกรายการ

**2. บั๊กจำนวนย้อนกลับตอนแก้ไข+จัดเสร็จ — แก้แล้ว**: เจอจากภาพจริง (แก้ 99→100 แล้วโชว์ยอดกลับไป 99) ต้นตอคือ
auto-refresh พื้นหลัง (ทุก 30 วิ) ไม่เช็คว่า reason modal เปิดอยู่/กำลังแก้ไขอยู่ เลยดึงข้อมูลเก่ามาทับกลางทาง —
เพิ่ม `adminEditBusy` flag (ตั้ง true ตลอดช่วง `markDone`/`saveAdminEdit`) ให้ auto-refresh ข้ามทั้งตอนเริ่ม fetch
และตอน apply ผล (เผื่อ request ค้างอยู่ก่อนเริ่มแก้)

**3. บั๊ก modal ไม่โผล่ในหน้าแอดมิน — แก้แล้ว**: reason modal เดิมซ้อนอยู่ใน `#app` (กล่องฝั่งลูกค้า) ซึ่ง
`show('adminApp')` ตั้งเป็น `display:none` ทับ ทำให้ modal ไม่มีวันโผล่ตอนอยู่หน้าแอดมิน (กด "จัดเสร็จแล้ว" แล้ว
เหมือนไม่มีอะไรเกิดขึ้น) — ย้าย modal ออกมาเป็น sibling ของ `#app`/`#adminApp` แทน

**4. Two-step confirm เวลาแก้จำนวนก่อนจัดเสร็จ**: เดิมกด "จัดเสร็จแล้ว" ตอนมีแก้จำนวน จะบันทึกการแก้ไข+จัดเสร็จ
รวดเดียวจบ ไม่มีจังหวะทวนดู — ผู้ใช้ขอแยกเป็น 2 คลิก: **คลิกแรก** (มี diff) บันทึกแค่การแก้ไข ยังอยู่แท็บ "กำลังจัด"
พร้อมโน้ตสีเทาใต้ราคาบอก "แก้จาก x99 → x100 — เหตุผล" ของแต่ละรายการ (เก็บใน memory เท่านั้น หายเองตอน
auto-refresh ครั้งถัดไป ไม่ใช่ประวัติถาวร) **คลิกที่สอง** (ไม่มี diff แล้ว) ถึงจะจัดเสร็จจริง ไม่ถามเหตุผลซ้ำ

**5. ช่องโหว่ log/Telegram รั่วไปทางฝั่งลูกค้า — พบเพราะผู้ใช้ถามให้ทวนโค้ด แก้แล้ว**: `updateOrder()` เดิม log
ทุกครั้งไม่ว่าใครแก้ (ตั้งใจไว้ตั้งแต่ 12 ก.ย. เพื่อ audit ทั่วไป) ทำให้ลูกค้าแก้ไขออเดอร์ตัวเองก่อนแอดมินเริ่มจัด
(สิทธิ์ปกติ) ถูกดึงไปโผล่ในสรุป "มีการแก้ไขก่อนจัดเสร็จ" ผิดที่ผิดทาง — **นี่คือจุดที่ควรถูกจับได้ตั้งแต่ตอนสร้าง
ฟีเจอร์เหตุผล/Telegram (14 ก.ย.) แต่พลาดไป ผู้ใช้ต้องถามเองถึงเจอ** แก้โดย log เฉพาะตอน `isAdminCall` เท่านั้น
ลูกค้าแก้เองยังบันทึก items/total ปกติ แค่ไม่ต่อ log/Telegram อีกต่อไป

**6. ปิดช่องโหว่ backend ไม่เช็คสถานะออเดอร์ตอนลูกค้าแก้ไข/ยกเลิก**: เดิมบล็อกแค่ที่ UI (ซ่อนปุ่มตอน
packing/done) ไม่เคยเช็คที่ backend เลย — ถ้าลูกค้าเปิดหน้าค้างไว้ตั้งแต่ก่อนแอดมินเริ่มจัด (ปุ่มยังโชว์) แล้ว
กดตอนแอดมินเพิ่งกด "เริ่มจัดสินค้า" พอดี จะยังแก้ไข/ยกเลิกได้อยู่ดี — เพิ่มเช็คที่ `updateOrder()`/`cancelOrder()`:
ฝั่งลูกค้า (ไม่ใช่ admin) ทำได้เฉพาะตอน `delivery_status==='pending'` เท่านั้น แอดมินไม่ถูกบล็อก (แก้ระหว่างจัดของ
เป็น flow ปกติที่ตั้งใจไว้)

**7. Incident เช้า 15 ก.ย. 69 — เชื่อมต่อไม่นิ่ง กระทบธุรกิจจริง**: ผู้ใช้รายงานว่ากด "จัดเสร็จแล้ว" ไม่ติด/โหลด
ไม่ขึ้นหลายจุด (`loadAdminOrders`/`updateDelivery`/`getCustomer` timeout) เป็นๆ หายๆ ไม่เสถียร ส่งยอดลูกค้าไม่ได้
หลายเจ้า — **ยืนยันแล้วว่าไม่เกี่ยวกับฟีเจอร์เหตุผลที่เพิ่งทำ** (error เป็น "timeout" ไม่ใช่ error message เฉพาะของ
ฟีเจอร์) ลักษณะเป็นๆ หายๆ ตรงกับ pattern cold-start/เน็ตไม่นิ่งที่เคยเจอมาก่อน — **ยังไม่ได้ยืนยันสาเหตุแท้จริงจาก
Executions log** (ผู้ใช้ไม่สะดวกเช็คตอนนั้น) session ถัดไปถ้าเจอรายงานแบบนี้อีก ให้ขอเช็ค log ก่อนเป็นอันดับแรก
ตามธรรมเนียมเดิม — **สิ่งที่ทำแทนคือปรับ UX ให้ทนต่อเหตุการณ์นี้ได้ดีขึ้น** (ดูข้อ 8-9 ด้านล่าง) ไม่ใช่แก้ต้นตอ
เพราะยังไม่รู้ต้นตอแน่ชัด — ⚠️ **ข้อควรระวังสำคัญ**: ระหว่างสอบถามอาการ ผู้ใช้ตำหนิว่าผมไม่แจ้งเชิงรุกตอนสร้าง
ฟีเจอร์เหตุผลว่า log จะรั่วไปทางลูกค้า (ดูข้อ 5) — บทเรียนคือ **เวลาสร้างฟีเจอร์ที่พึ่งพา/อ่านข้อมูลที่มีอยู่แล้ว
ต้องเช็คว่าขอบเขตเดิมของมันตรงกับที่ฟีเจอร์ใหม่ต้องการจริงไหม ไม่ใช่เอามาใช้ต่อเฉยๆ แล้วรอให้ผู้ใช้ถามเองถึงเจอ**

**8. ปุ่ม action ไม่มี loading state — พนักงานกดซ้ำจนระบบรวน**: ผู้ใช้ชี้ว่าตอนกดปุ่ม "เริ่มจัดสินค้า"/"จัดเสร็จแล้ว"
ไม่มีสัญญาณอะไรระหว่างรอ backend ตอบ (ช้าได้หลายวินาทีเวลาเน็ต/backend ไม่นิ่งแบบ incident ข้อ 7) พนักงานคิดว่า
กดไม่ติดเลยกดซ้ำ กลายเป็นยิง request ซ้อนกันจริง — แก้โดยปุ่ม disable + เปลี่ยนข้อความเป็น "กำลังบันทึก..." ทันที
ที่กด กันกดซ้ำระหว่างรอ คืนสภาพปุ่มถ้ายกเลิก/error (ถ้าสำเร็จจะโดน re-render ทับไปเองอยู่แล้ว)

**9. ลด noise ของ error message ตอนเชื่อมต่อไม่นิ่ง — ผู้ใช้ขอให้เงียบและลองใหม่เองแทน**: เดิมมี 2 ชั้นที่กวนใจ —
(ก) แบนเนอร์แดงลอยจออัตโนมัติทุกครั้งที่ fetch/postAction พลาด (เจอซ้อนกับ alert() ของ action นั้นเองด้วย ข้อความ
ซ้ำกัน) (ข) หน้าจอ "โหลดข้อมูลไม่สำเร็จ"/"เชื่อมต่อไม่ได้" พร้อมปุ่ม "ลองใหม่" ให้กดเอง — ผู้ใช้บอกตรงๆ ว่า "ถ้ารู้ว่า
โหลดไม่ได้ก็โหลดซ้ำเลย ไม่ต้องแจ้งเตือนให้เห็น" แก้โดย:
- `logError()` ที่เรียกจากใน `safeFetchJSON`/`postAction` บังคับ `silent=true` เสมอ (ไม่โชว์แบนเนอร์แดงอีกต่อไป
  แต่ยัง log ลง console/localStorage เผื่อ debug) — banner ยังโชว์ปกติเฉพาะ JS error จริง (window.onerror/
  unhandledrejection) ที่ต้องการความสนใจจริงๆ
- จุดโหลดเริ่มต้นทั้งหมด (`initCustomer`, `loadAdminOrders`/`initAdmin`, `fetchFullAdminOrders`/แท็บภาพรวม,
  `loadTrackSection`) เปลี่ยนจากโชว์ error+ปุ่มกดเอง เป็น **วนลองใหม่อัตโนมัติทุก 3 วิเงียบๆ** จนกว่าจะสำเร็จ
  ไม่มีข้อความ error ให้เห็นเลย ค้างแค่สถานะ "กำลังโหลด" ไว้ — ยกเว้น `initCustomer` กรณี `data.error` จริง
  (เช่นลิงก์ผิด ไม่ใช่ปัญหาเน็ต) ยังโชว์ error ตามเดิม เพราะลองใหม่ไปก็ไม่มีทางสำเร็จ
- แบนเนอร์ "พลาด 3 ครั้งติด" ของ auto-refresh พื้นหลัง (ทุก 10-30 วิ) — เอาออกแล้วเช่นกัน ปล่อยให้ interval
  รอบถัดไปลองเองต่อไปเรื่อยๆ

**10. ไอคอน "กำลังโหลด" — เปลี่ยนเป็นจุดกระโดดสามจุด**: ผู้ใช้ขอเดโม่ตัวเลือกก่อน (ทำ Claude Artifact โชว์ 6 แบบ
ให้เลือก) เลือก **จุดกระโดดสามจุดสีส้มตามธีมแอป** ใช้แบบเดียวกันทุกจุดที่เคยขึ้น "กำลังโหลด..." เฉยๆ (หน้าแอดมิน/
ติดตามสถานะ/สั่งซื้อ/สั่งซ้ำ) — **ไม่แตะหน้าแรกตอนเปิดแอป** (โลโก้ + แถบไฟวิ่งเดิม คนละจุดกัน มีแอนิเมชันของตัวเอง
อยู่แล้ว) และไม่แตะ label เล็กๆ ใต้หัวข้อหน้าแอดมิน (`#adminSubtitle` — ข้อความชั่วคราวเล็กเกินไปไม่คุ้มใส่)

**11. ยกเว้นลูกค้า "ซูโม่ Sausage" ออกจากภาพรวม/แดชบอร์ด**: เป็นร้านของเจ้าของเองอีกร้าน ใช้ order web app นี้แค่
ทำบิลระหว่างร้าน ไม่ใช่ลูกค้าจริง — เพิ่ม `OVERVIEW_EXCLUDED_CUSTOMER_NAMES` array กรองเฉพาะตอนดึง
`allAdminOrdersFull` (แหล่งข้อมูลเดียวของทุกฟังก์ชันในหน้าภาพรวม) ไม่แตะ `allAdminOrders` ที่ใช้ในแท็บ
รอจัด/กำลังจัด/เสร็จสมบูรณ์เลย — ยังจัดของ/ติดตามสถานะออเดอร์นี้ได้ปกติทุกอย่าง แค่ไม่นับยอดในภาพรวม ถ้ามีลูกค้า
ลักษณะนี้เพิ่มในอนาคต เพิ่มชื่อเข้า array เดิมได้เลยไม่ต้องแก้โค้ดใหม่

**Deploy/push**: ทุกข้อด้านบน push เข้า `main` แล้วครบ (แยก push เป็น 3 รอบตามที่ผู้ใช้ขอ: frontend-only ก่อน
ที่ไม่ต้องรอ backend, แล้วค่อย push backend source ทีหลังเมื่อพร้อม deploy) **ผู้ใช้ยืนยันแล้วว่า deploy
`order-app-Code.gs` ล่าสุดเรียบร้อย** — ระบบซิงค์กันครบทั้ง 2 ฝั่งแล้ว ณ ตอนจบ session นี้

**ค้างไว้/ยังไม่ยืนยัน**:
- สาเหตุแท้จริงของ incident ข้อ 7 (เชื่อมต่อไม่นิ่งช่วงเช้า) ยังไม่ได้เช็ค Executions log ยืนยัน — ถ้าเกิดซ้ำอีก
  ให้ขอ log ก่อนเป็นอันดับแรก อย่าเดาว่าเป็นโค้ดใหม่ที่เพิ่งทำ (ตรวจสอบแล้วว่าไม่ใช่)
- ยังไม่ได้ทดสอบ `keepWarm` trigger (จากอัปเดต 13 ก.ย.) ว่าช่วยจริงไหมกับ incident รอบนี้ — ควรถามผลถ้ายังไม่ถูก
  บอกตอนเริ่ม session ถัดไป

### สถานะล่าสุด (อัปเดต 15 ก.ย. 69 — ต่อ 2) — cache `getAdminOrdersFull` + ขยาย timeout กัน error หน้าภาพรวม

**บริบท**: ผู้ใช้รายงาน error `getAdminOrdersFull: หมดเวลาเชื่อมต่อ (timeout)` ตอนเปิดแท็บ "ภาพรวม" ของหน้าแอดมิน
(สกรีนช็อตจาก iPad, production `samurai-murex.vercel.app`) — ระบุสาเหตุ: `getAdminOrdersFull()` query ทั้งชีต
ทุกปีไม่กรองช่วงวันที่เลยทุกครั้งที่เปิดแท็บนี้ (ต่างจาก `getAdminOrders` ที่กรอง 60 วันแล้วตั้งแต่ 7 ก.ย. 69) บวกกับ
cold-start ของ Apps Script (ปัญหาเดิมที่บันทึกไว้ซ้ำหลายรอบในไฟล์นี้) ทำให้ client timeout เดิม (20 วิ) หลุดบ่อย

**แก้แล้ว push+deploy แล้วทั้งคู่**:
- `order-app-Code.gs`/`getAdminOrdersFull()`: เพิ่ม `CacheService.getScriptCache()` cache ผลลัพธ์ไว้ 120 วิ
  (`ADMIN_ORDERS_FULL_CACHE_KEY`/`ADMIN_ORDERS_FULL_CACHE_TTL_SEC`) — ข้ามไปเงียบๆ ไม่ cache ถ้า JSON ยาวเกิน
  100KB (ข้อจำกัดของ `CacheService` เอง) ไม่ error แค่เสียประโยชน์แคชไปสำหรับร้านที่ข้อมูลใหญ่มากๆ
- `index.html`/`fetchFullAdminOrders()`: ขยาย timeout จาก 20 วิ → 35 วิ เผื่อ cold start
- **ตอน push เจอ remote มีคอมมิตใหม่จากอีก session** (งานฝั่งแอปเช็คสต๊อก 5 คอมมิต — `syncProductUnits` trigger,
  `keepWarm()`, perf หน้าสั่งของ) merge อัตโนมัติสำเร็จไม่มี conflict เพราะคนละไฟล์กัน (ตรวจสอบแล้วว่าโค้ดที่แก้ยัง
  อยู่ครบถูกต้องหลัง merge)
- **เจอปัญหาระหว่าง deploy**: ไฟล์ `order-app-Code.gs` ที่ส่งให้ผู้ใช้รอบแรกทาง SendUserFile ถูกส่ง**ก่อน** merge
  ข้างต้น ทำให้เป็นเวอร์ชันเก่าที่ไม่มีการเปลี่ยนแปลงจาก session อื่น (`keepWarm()`, ฟีเจอร์เหตุผลแก้ไขออเดอร์,
  `OrderEditLog`) วางทับแล้วชน syntax error ที่ Apps Script Editor (`SyntaxError: Invalid or unexpected token`)
  — แก้โดยส่งไฟล์เวอร์ชันล่าสุด (หลัง merge) ให้ใหม่ พร้อมย้ำให้ Ctrl+A ลบโค้ดเดิมทั้งหมดก่อนวางทับ (ไม่ใช่วางแทรก/
  วางทับบางส่วน) — deploy สำเร็จแล้ว **ผู้ใช้ยืนยันแล้วว่า deploy แล้ว**

⚠️ **บทเรียนสำหรับ session ถัดไป**: ถ้า `git push` เจอ remote มีคอมมิตใหม่จากอีก session ระหว่างทำงาน
(โดยเฉพาะที่แตะ `order-app-Code.gs`/`Code.gs` ร่วมกัน) **ต้อง merge เสร็จเรียบร้อยก่อน แล้วค่อยส่งไฟล์ `.gs` ให้ผู้ใช้
ทาง SendUserFile เสมอ** อย่าส่งไฟล์ก่อน merge เด็ดขาด ไม่งั้นไฟล์ที่ส่งจะเป็นเวอร์ชันเก่ากว่าที่ push จริงและทำให้
ผู้ใช้ deploy โค้ดที่ไม่ครบ/ชนกับของเดิมในหน้า Editor

⚠️ **ยังไม่ได้รับการยืนยันผลจริงว่า error timeout หายไปแล้วหรือไม่** — เพิ่งยืนยันแค่ deploy สำเร็จไม่มี syntax error
เท่านั้น ถ้าคุยเรื่องนี้ต่อให้ถามผลทดสอบจริง (เปิดแท็บภาพรวมอีกครั้งหลัง deploy) ก่อนเป็นอันดับแรก

### สถานะล่าสุด (อัปเดต 17 ก.ย. 69) — cache แบบ localStorage ให้หน้าแอดมินเปิดซ้ำเร็วทันที + แก้บั๊กรูป/ราคาหาย

**บริบท**: ผู้ใช้บ่นว่าหน้าแอดมิน (โดยเฉพาะบนมือถือ) เปิดช้า อยากได้ 1-3 วิ — สังเกตว่าเดสก์ท็อปเร็วกว่ามือถือมาก
("กดปั๊บมาปุ๊บเลย" บนคอม แต่มือถือรอ 20-30 วิ) พร้อมส่งสกรีนช็อต Apps Script Executions log มาให้ดู (ยืนยันว่า
`doGet` ใช้เวลา 1.5-6 วิเต็มๆ แทบทุกครั้ง)

**แก้แล้ว push ครบแล้วทั้ง frontend/backend**:
1. `index.html`/`initAdmin()`: เพิ่ม cache-first ด้วย **`localStorage`** (ไม่ใช่ `sessionStorage`) สำหรับ
   `getAdminOrders` (แท็บหลัก) และ `getAdminOrdersFull` (แท็บภาพรวม) — โชว์รายการล่าสุดที่เคยโหลดได้ทันที แล้ว
   รีเฟรชเงียบๆ เบื้องหลัง **เหตุผลที่ต้องใช้ `localStorage` ไม่ใช่ `sessionStorage`**: แอปนี้ติดตั้งเป็น
   "Add to Home Screen" บน iOS ได้ (`manifest.json` มี `display:standalone`) ซึ่ง iOS สร้าง WKWebView instance
   ใหม่ทุกครั้งที่แตะไอคอนเปิดแอป — `sessionStorage` ผูกกับ instance นั้นเท่านั้น ไม่รอดข้ามการเปิดแอปแต่ละรอบ
   (ทดลองใช้ `sessionStorage` ก่อนจริงๆ แล้วพบว่าไม่ช่วยอะไรบนมือถือเลยตามที่คาด ก่อนเปลี่ยนมาใช้ `localStorage`)
   Key ที่ใช้: `samurai_admin_orders_cache_v1` / `samurai_admin_orders_full_cache_v1` — ใส่เพดานขนาด **2MB ต่อ
   key** (ข้ามการ cache เงียบๆ ถ้าเกิน ไม่ error) กันเบียดโควต้า `localStorage` รวมของ origin (~5MB บน iOS Safari)
   จน key อื่นที่มีอยู่แล้ว (`ss_errlog`, `samurai_customer_ref`) เขียนไม่ได้ไปด้วย — เหตุผลที่ต้องกันเรื่องนี้:
   `getAdminOrdersFull` ไม่กรองช่วงวันที่เลย โตขึ้นเรื่อยๆ ตามอายุร้าน ไม่มีวันหด
2. **เจอบั๊กจริงหลัง deploy รอบแรก**: รูปสินค้ากลายเป็นไอคอนถุงช้อปปิ้ง placeholder + ราคาโชว้ ฿0.00 ทุกรายการ
   ตอนโชว้จาก cache (ยอดรวมออเดอร์ยังถูกเพราะมาจาก `o.total` ตรงๆ ไม่ผ่าน `products`) **สาเหตุ**: ตอน render จาก
   cache ก่อน ยังไม่ทันโหลดตัวแปร `products` เลย (ยังเป็น `[]` ว่างเปล่า) ทำให้ `getOrderProdFullMap` หา prod
   ไม่เจอสักตัว — แถมถ้าข้อมูล orders ที่โหลดจริงตรงกับ cache เป๊ะ ระบบจะไม่ re-render ซ้ำ ทำให้ค้างว่างเปล่าตลอดไป
   **แก้โดย**: (ก) เพิ่ม `localStorage` cache แยกสำหรับ `products` ฝั่งแอดมินด้วย (key
   `samurai_admin_products_cache_v1` — คนละตัวกับ `cacheGet/cacheSet('products_ALL',...)` เดิมที่เป็น
   `sessionStorage` แบบ short-lived ต่อ tab ยังเก็บคู่ขนานไว้เหมือนเดิม) ใช้ seed ตอน paint แรกเท่านั้น ไม่เคย
   ข้ามการยิง fetch จริง (ราคาต้องสดเสมอ) (ข) บังคับ re-render อีกครั้งหลัง `products`/`orders` โหลดเสร็จ ถ้ารอบ
   ก่อนหน้า render จาก cache ไปโดยยังไม่มีข้อมูล products ที่การันตีความสด — กันปัญหา "ไม่ re-render ซ้ำเพราะ
   orders ไม่เปลี่ยน" ไม่ให้ค้างพังตลอดไป
3. `order-app-Code.gs`: ขยาย `ORDER_CACHE_TTL_SEC` จาก 10 → 35 วิ — เดิมสั้นกว่ารอบ auto-refresh ฝั่งแอดมิน
   (30 วิ) มาก ทำให้ cache หมดอายุก่อนรอบถัดไปมาถึงเสมอ แทบไม่เคยได้ใช้ประโยชน์เลย (ตรงกับที่เห็นใน log ว่าแทบ
   ทุก `doGet` อ่านชีตสดเต็มๆ) — เขียน/แก้ไขออเดอร์ยัง invalidate cache ทันทีเหมือนเดิมทุกจุด ไม่กระทบความสด
   ของข้อมูลตอนมีคนกดปุ่มจริงในแอปเลย
4. **ตรวจสอบแล้ว**: `keepWarm()` trigger ของ `order-app-Code.gs` (เพิ่มไว้ตั้งแต่ 13 ก.ย. 69) **ทำงานอยู่แล้วจริง**
   ทุก 5 นาที error rate 0.17% (ผู้ใช้ส่งสกรีนช็อตหน้า Triggers มายืนยัน) — ไม่ต้องแก้อะไรเพิ่มจุดนี้ (คนละ
   โปรเจกต์กับ `keepWarm`/`syncProductUnits` ของแอปเช็คสต๊อกที่เคยมีปัญหา trigger ค้างมาก่อน อย่าสับสนกัน)

⚠️ **บทเรียนสำคัญมาก — เกิดอุบัติเหตุจริงระหว่างทำงาน (production พังชั่วคราว)**: ตอน push `index.html`
(~230KB) ครั้งแรกผ่าน `mcp__github__create_or_update_file` (เนื้อหาทั้งไฟล์ผ่าน parameter `content` โดยตรง)
**เนื้อหาที่ส่งไปกลายเป็นข้อความสั้นๆ (placeholder) แทนที่จะเป็นโค้ดจริงทั้งไฟล์ แบบเงียบๆ ไม่มี error เตือนเลย**
เกิดซ้ำ 2 ครั้งติด (production index.html ถูกทับด้วยเนื้อหาสั้นๆ ทำให้เว็บลูกค้า/แอดมินใช้งานไม่ได้จริงชั่วคราว
จนกว่าจะแก้) **สาเหตุที่แท้จริงยังไม่ชัด 100%** (อาจเป็นข้อจำกัดการ generate เนื้อหายาวมากๆ ในการเรียก tool
ครั้งเดียว) — **วิธีแก้ที่ได้ผลจริง**: เปลี่ยนมาใช้ `git clone`/`git push` ตรงผ่าน `$GITHUB_TOKEN` ที่ environment
เตรียมไว้ให้อยู่แล้วสำหรับ repo นี้ (ผ่าน Bash) แทนการส่งเนื้อหาทั้งไฟล์ผ่าน MCP tool parameter — เพราะเป็นการ
copy ไฟล์จาก disk ตรงๆ ไม่ต้อง generate เนื้อหาซ้ำ **และเพิ่มขั้นตอนตรวจสอบบังคับหลัง push ทุกครั้ง**: ดึงไฟล์
กลับมาจาก GitHub (`curl raw.githubusercontent.com`) แล้วเทียบ byte-ต่อ-byte (`diff`)/SHA-1 กับไฟล์ต้นฉบับ
ก่อนบอกผู้ใช้ว่า "push สำเร็จแล้ว" เสมอ — **ห้ามใช้ `mcp__github__create_or_update_file`/`push_files` ส่ง
เนื้อหาไฟล์ที่ใหญ่ (ประมาณ >50-100KB ขึ้นไป จากที่เจอจริงกับไฟล์ 230KB) อีกโดยเด็ดขาด ให้ใช้วิธี `git`+token
ตามข้างบนแทนเสมอสำหรับไฟล์ใหญ่ (ไฟล์เล็กอย่าง `order-app-Code.gs` การแก้เล็กๆ ยังใช้ MCP tool ได้ปกติ ไม่เจอ
ปัญหานี้ — แต่ถ้าไม่มั่นใจขนาด ใช้วิธี git ไปเลยปลอดภัยกว่า)**

**ผู้ใช้ยืนยันแล้ว**: "วันนี้ใช้งานได้ดีมาก" — cache ทำงานถูกต้อง เปิดซ้ำเร็วทันที รูป/ราคาไม่หายแล้ว

**ค้างไว้ — งานต่อไปที่ผู้ใช้ขอเริ่มแล้วแต่ยังไม่ได้รายละเอียด**: ผู้ใช้บอกว่าหน้า "ภาพรวม" ของหน้าแอดมิน
(`renderAdminOverview`/Owner Dashboard) **"ยังมีที่ไม่ถูกต้อง"** แต่ถามรายละเอียด 2 รอบแล้วยังไม่ได้คำตอบชัดเจน
ว่าผิดตรงไหน/ตัวเลขไหน (ปฏิเสธการใช้ AskUserQuestion แบบเลือกตัวเลือกด้วย) — ก่อนจบ session ได้อธิบายตรรกะ
ปัจจุบันให้ฟังไปแล้ว (นับยอด/ลูกค้าเฉพาะออเดอร์ที่ "จัดเสร็จแล้ว", **นับตามวันที่กด "จัดเสร็จแล้ว" ไม่ใช่วันที่สั่ง**,
ตัดลูกค้า "ซูโม่ Sausage" ออกเสมอ) พร้อมเสนอ 4 สมมติฐานให้เลือก (ตัวเลขผิดจริง / อยากนับตามวันที่สั่งแทน /
ปัญหาอยู่ที่ Owner Dashboard ไม่ใช่หน้าภาพรวมหลัก / อย่างอื่น) **ยังไม่ได้รับคำตอบก่อนจบ session** — session ถัดไป
ต้องถามคำถามนี้ก่อนเป็นอันดับแรกถ้าจะแก้เรื่องนี้ต่อ อย่าเดาแล้วแก้เองโดยไม่มีข้อมูลชัดเจน (ความเสี่ยงสูงที่จะแก้ผิดจุด
เหมือนที่เสียเวลาไปกับปัญหา cache/รูป/ราคาในรอบนี้)

### สถานะล่าสุด (อัปเดต 18 ก.ย. 69) — แก้บั๊กแจ้งเตือน "จัดเสร็จแล้ว" ซ้ำ (root cause: guard ไม่กันสถานะถอยหลัง)

**บริบท**: ผู้ใช้ส่งภาพแชท Telegram มาว่าออเดอร์เดียวกัน (`ORD-180926-113625`) ขึ้น "✅ จัดเสร็จแล้ว" 2 ครั้ง
ห่างกัน 3 นาที (11:37 กับ 11:40) ยอด/รายการตรงกันเป๊ะทั้งคู่ — ไล่ตัดสมมติฐานทีละข้อจนเหลือคำตอบเดียว:
- ไม่ใช่ race condition แบบที่เคยแก้ไปแล้ว 8-10 ก.ย. 69 (ครั้งนั้นเกิดพร้อมกันในวินาทีเดียว ครั้งนี้ห่างกัน 3 นาที
  เกินเวลาที่ `LockService` จะยังถือค้างอยู่)
- ไม่ใช่แถวออเดอร์ซ้ำในชีต — ผู้ใช้เช็คแล้วมีแถวเดียว ไม่มีใครลบมือ
- ไม่ใช่เวอร์ชัน backend เก่ากว่าที่คุยกันไว้ — ผู้ใช้ยืนยัน version ปัจจุบันแน่นอน
- ไม่ใช่คนละคนกดปุ่มพร้อมกัน — คนที่เข้าหลังบ้านได้มีคนเดียว

**Root cause ที่ยืนยันจากโค้ดจริง**: guard เดิมใน `updateDelivery()` (`order-app-Code.gs`) เช็คแค่
"สถานะปัจจุบัน == สถานะที่ขอตั้ง" เพื่อกันกดซ้ำสถานะเดิม แต่ไม่เคยเช็ค**ทิศทาง**ของการเปลี่ยนสถานะเลย —
ถ้ามีคำขอ `delivery_status:'packing'` เข้ามาตอนสถานะจริงเป็น `done` ไปแล้ว (เช่น กดปุ่ม "เริ่มจัดสินค้า" จาก
หน้าจอเก่าที่ยังไม่รีเฟรชตั้งแต่ก่อนออเดอร์นี้จัดเสร็จ) เพราะ `done ≠ packing` guard เดิมปล่อยผ่านทันที
เขียนสถานะย้อนกลับเป็น `packing` ได้เงียบๆ (ไม่มี Telegram แจ้งตอนนั้นเพราะ packing ไม่ notify) พอกด
"จัดเสร็จแล้ว" ซ้ำอีกครั้งทีหลัง (ตอนนั้น backend เห็นว่าเป็น packing จริง ไม่ใช่ UI ค้าง) ก็จัดเสร็จ+แจ้งเตือน
ซ้ำได้จริงอีกรอบ — พิสูจน์ด้วย unit test (Node `vm` sandbox รันโค้ดจริง) จำลองฉากนี้ตรงๆ แล้วเห็น Telegram
ยิง 2 ครั้งจริงก่อนแก้

**แก้แล้ว push+deploy แล้ว (commit `242b457`)**:
- เพิ่ม guard บังคับทิศทางเดินหน้าอย่างเดียวใน `updateDelivery()`: `packing` ต้องมาจาก `pending` เท่านั้น,
  `done` ต้องมาจาก `packing` เท่านั้น ไม่งั้นปฏิเสธพร้อมข้อความบอกสถานะปัจจุบัน+ให้รีเฟรชหน้าจอ — เช็คแล้วมีแค่
  2 จุดในทั้งแอปที่เรียก `updateDelivery` (`markPacking`→packing, `markDone`→done เท่านั้น) และ `cancelOrder`
  เขียน `delivery_status='cancelled'` ตรงๆ ไม่ผ่าน `updateDelivery` เลย จึงไม่กระทบ flow ยกเลิกออเดอร์ — ไม่มี
  ฟีเจอร์ "เปิดออเดอร์กลับมาแก้ไข/ย้อนสถานะ" ในระบบเลยสักจุด ปิดช่องนี้ได้โดยไม่กระทบอะไรที่มีคนใช้งานจริง
- เพิ่ม `Logger.log` ที่ต้นทาง `doGet`/`doPost` (log action + order_id + delivery_status เมื่อมีค่า) — ปัญหา
  เดิมที่เจอตอนไล่บั๊กนี้คือ Executions log โชว์แค่ "doPost" เฉยๆ ไม่บอก action/parameter เลย (เจอปัญหาเดียวกัน
  ตอนไล่บั๊กยอดออเดอร์ไม่ตรงกัน 12 ก.ย. 69 ด้วย) ครั้งหน้าจะเห็นในนี้ทันทีไม่ต้องเดาจากโค้ดอีก

**ทดสอบแล้ว**: unit test 6 เคส (Node `vm` sandbox mock `LockService`/`SpreadsheetApp`/`CacheService`/
`PropertiesService`/`UrlFetchApp` รันกับ `order-app-Code.gs` จริง) ครอบคลุม pending→packing ผ่านปกติ,
packing→done ผ่าน+แจ้งเตือน 1 ครั้ง, done→done (กดซ้ำ) skip ไม่แจ้งซ้ำ (regression เดิม), **done→packing
(บั๊กจริง) ถูกบล็อก**, pending→done (ข้ามขั้น) ถูกบล็อกด้วย, และจำลองฉากบั๊กเต็มรูปแบบ (done→พยายามถอยเป็น
packing→done อีกครั้ง) ยืนยันว่ารวมทั้งฉาก Telegram ส่งแค่ 1 ครั้งเท่านั้น (เดิมจะได้ 2 ครั้งตามที่เจอจริง) —
ผ่านหมดทุกเคส `node --check` ผ่าน

**Deploy**: ผู้ใช้แปะ `order-app-Code.gs` เข้า Apps Script Editor + Deploy → New version แล้ว ยืนยันว่า deploy
เสร็จ — **ยังไม่ได้รับการยืนยันผลทดสอบใช้งานจริง** (ต้องรอเจอสถานการณ์เดิมอีกครั้งหรือลองจำลองเอง) ถ้าคุยเรื่องนี้
ต่อให้ถามผลก่อนเป็นอันดับแรก

## Bill Templates by Supplier

Use this reference to identify supplier from bill photos without needing to ask.

---

### S001 — ลุงทวี
- รูปแบบ: ใบส่งสินค้าเขียนมือ **กระดาษสีเหลืองหรือกระดาษขาว** หมึกสีน้ำเงิน (สีกระดาษไม่ใช่ตัวชี้ขาด — ลุงทวีส่งทั้งสองแบบ)
- หัวบิล: "ชื่อลูกค้า ซามูไร ไส้กรอก 1" หรือ "ชื่อลูกค้า ซามูไร ไส้กรอก 2" เขียนมือ, "ใบส่งสินค้า"
- คอลัมน์: ลำดับ | รายการ | จำนวน | ราคา | จำนวนเงิน
- ไม่มีชื่อซัพพลายเออร์บนบิล — แยกจาก S025 ตานะ ด้วยรูปแบบหัวบิล ("ชื่อลูกค้า ซามูไร ไส้กรอก..." vs "ใบเก็บเงิน") และจากบิลเขียนมือเจ้าอื่นด้วยรายการสินค้า
- สินค้าหลัก: แฮมหมู (โทสต์แฮม 500g, กุ๊กแฮม 1000g), เอ็นเนื้อ/เอ็นหมูปอง (พรชัย, พงษ์เจริญ), เนื้อขาย (ปอง), ลูกชิ้นปลา/เนื้อหลายชนิด, ยอ/จ้อ/หมูยอ, ไส้อั่ว, น้ำจิ้ม, เต้าหู้หมู — รายการเยอะมาก 20-30 รายการต่อใบ
- หมายเหตุ: ทุกใบที่หัวบิลเขียน "ชื่อลูกค้า ซามูไร ไส้กรอก 1/2" คือลุงทวีทั้งหมด ไม่ว่าจะเป็นล็อตแฮมหมูหรือล็อตเอ็นเนื้อ — ไม่ต้องแยกเป็นซัพพลายเออร์อื่น

### S002 — แปดเชียน ฟู้ด (แปดเซียน ฟู้ด)
- โลโก้: อักษรจีน "八仙" ในกรอบสี่เหลี่ยม มุมบนซ้าย
- หัวบิล: "บิลเรียกเก็บเงิน — แปดเชียน ฟู้ด" ตัวใหญ่
- รูปแบบ: ตารางสองคอลัมน์ รายการ | จำนวน | เป็นเงิน
- ร้านค้า: เจ๊พัชร์ลิตา

### S004 — SINTA (บริษัท ชินต้า จำกัด)
- หัวบิล: "บริษัท ชินต้า จำกัด (สำนักงานใหญ่)" หรือ "SIMTA"
- ที่อยู่: 97 หมู่ 8 ซ.สุขสวัสดิ์ 78 ถ.สุขสวัสดิ์ ต.บางจาก อ.พระประแดง สมุทรปราการ 10130
- รูปแบบ: ใบส่งของ/ใบกำกับภาษี มีรหัสลูกค้า

### S005 — CPF Global Food Solution
- หัวบิล: "บมจ. ซีพีเอฟ โกลบอล ฟู้ด โซลูชั่น / CPF Global Food Solution Public Company Limited"
- ที่อยู่: เลขที่ 3 อาคาร ช.พี.ทาวเวอร์ 2 ชั้น 28,29 ถนนรัชดาภิเษก แขวงดินแดง กรุงเทพ 10400
- Tax ID: 0107566000135
- รูปแบบ: ใบกำกับภาษี/ใบส่งสินค้า พื้นขาว ตัวอักษรสีน้ำเงิน
- รหัสสินค้า: 8 หลัก (เช่น 23062883)

### S006 — PFP (บริษัท พี.เอฟ.พี. เทรดดิ้ง จำกัด)
- หัวบิล: "บริษัท พี.เอฟ.พี. เทรดดิ้ง จำกัด / P.F.P. TRADING CO., LTD."
- โลโก้: "Pfp" script หรือตัวย่อ P.F.P. ในกรอบ
- ที่อยู่: 11/12-14 ถนนรัชดาภิเษก แขวงช่องนนทรี เขตยานนาวา กรุงเทพ 10120

### S007 — มหาชัยฟู้ดส์ (บริษัท มหาชัยฟู้ดส์ จำกัด)
- หัวบิล: "บริษัท มหาชัยฟู้ดส์ จำกัด / Mahachai Foods Co., Ltd."
- ที่อยู่: 71/11 หมู่ที่ 6 ท่าทราย เมืองสมุทรสาคร 74000
- Tax ID: 0-1055-32104-62-9
- รหัสลูกค้า: C2408005, สายส่ง MF011-N
- รูปแบบ: ต้นฉบับใบส่งสินค้า/ใบกำกับภาษี เลขที่ SM260XXXXXX รหัสสินค้า V + 6 หลัก

### S008 — นำชัย ฟู้ด อินโนเวชันส์
- หัวบิล: "บริษัท นำชัย ฟู้ด อินโนเวชันส์ จำกัด / NAMCHAI FOOD INNOVATIONS"
- โลโก้: NAMCHAI FOOD INNOVATIONS ตัวหนา
- ที่อยู่: 22/2 หมู่ที่ 8 แขวงศาลาธรรมสพน์ เขตทวีวัฒนา กรุงเทพ 10170
- Tax ID: 0105567220455
- รูปแบบ: บิลเงินสด/ใบกำกับภาษีอย่างย่อ

### S009 — สตาร์อัพ มาร์เก็ตติ้ง
- หัวบิล: "บริษัท สตาร์อัพ มาร์เก็ตติ้ง จำกัด"
- Tax ID: 0105562013834, รหัสลูกค้า: 6402051
- รูปแบบ: ใบกำกับสินค้า/ใบกำกับภาษี
- สินค้าหลัก: STUF ไส้อั่วไข่ชีล (1,000g / 16 ชิ้น)

### S010 — เจริญ ฟู้ดส์ โปรดักส์
- หัวบิล: "บริษัท เจริญ ฟู้ดส์ โปรดักส์ จำกัด / CHAROENFOODSPRODUCT CO., LTD."
- ที่อยู่: 18/1 หมู่ที่ 12 ตำบลบึงคอไห ลำลูกกา ปทุมธานี
- รูปแบบ: ใบส่งของ/ใบกำกับภาษี/ใบแจ้งหนี้ DELIVERY / TAX INVOICE / INVOICE

### S011 — GSB International
- หัวบิล: "GSB INTERNATIONAL CO., LTD. / บริษัท จีเอสบี อินเตอร์เนชั่นแนล จำกัด"
- Tax ID: 0135542000508, รหัสลูกค้า: NPU-01
- รูปแบบ: ใบกำกับภาษี
- สินค้าหลัก: ไก่แต่งรมควัน, ไส้กรอกรมควัน

### S012 — TVI (บริษัท อุตสาหกรรมทวีวงษ์ จำกัด)
- หัวบิล: "บริษัท อุตสาหกรรมทวีวงษ์ จำกัด / THAVEEVONG INDUSTRY CO., LTD."
- ที่อยู่: 22 หมู่ 1 ซ.อ่อนนุช 62 เขตสวนหลวง กรุงเทพ 10250

### S014 — IOP (แหนมนิตยา) (บริษัท ไอ.โอ.พี.ฟู้ดส์ จำกัด)
- หัวบิล: "บริษัท ไอ.โอ.พี.ฟู้ดส์ จำกัด"
- ที่อยู่: 7/5 หมู่ 3 ตำบลห้วยจรเข้ อำเภอเมืองนครปฐม จังหวัดนครปฐม 73000
- Tax ID: 0735558000892
- รหัสลูกค้า: BK-K003-BK, พนักงานขาย: IOP-IOP
- รูปแบบ: ใบส่งของ/ใบแจ้งหนี้ รหัสสินค้า format 01-01-002

### S015 — โรงงานลูกชิ้นไก่ พี.พี.ฟู้ด
- หัวบิล: "โรงงานลูกชิ้นไก่ พี.พี.ฟู้ด"
- ที่อยู่: 23/310 ม.2 ต.บางเมือง เมืองสมุทรปราการ 10270
- สินค้าหลัก: ลูกชิ้นไก่

### S016 — Betagro (บริษัท เบทาโกรเกษตรอุตสาหกรรม จำกัด)
- หัวบิล: "บริษัท เบทาโกรเกษตรอุตสาหกรรม จำกัด / BETAGRO AGRO INDUSTRY COMPANY LIMITED"
- โลโก้: BETAGRO ตัวหนาสีเขียว
- ที่อยู่: 323 หมู่ที่ 6 ถนนวิภาวดีรังสิต เขตหลักสี่ กรุงเทพ 10210

### S017 — ง่วนเฮงฟาร์ม
- หัวบิล: "ใบส่งของ (ง่วนเฮงฟาร์ม)" พิมพ์สำเร็จรูป
- รูปแบบ: มีช่องค้างลังเก่า / ยอดส่ง / คืนลงมา / สรุปยอด
- สินค้าหลัก: ไข่นกสด, ไข่นกกม

### S018 — SP (ธนนวรรณ / พลขันธ์กสิกร)
- หัวบิล: พิมพ์ดอตเมทริกซ์สีน้ำเงิน ชื่อ "ธนนวรรณ พลขันธ์กสิกร" บรรทัดบนสุด
- รูปแบบ: ใบส่งสินค้า/ใบแจ้งหนี้ มีคอลัมน์ Lot No. หน่วย(จุก) จำนวน ราคา/หน่วย จำนวนเงิน
- รหัสลูกค้า: M4-408

### S019 — แหลมทองโปรตีนฟู้ดส์
- หัวบิล: โลโก้วงกลมสีเขียว ชื่อ "บริษัท แหลมทองโปรตีนฟู้ด จำกัด / LAEMTHONG PROTEIN FOODS CO., LTD."
- พื้นหลัง header: สีเขียว
- รูปแบบ: ใบกำกับภาษี มีรหัสสินค้า (เช่น 533110005) นำหน้าทุกรายการ

### S020 — จัสมิน ฟู้ด แอนด์ มาร์เก็ตติ้ง
- หัวบิล: "บริษัท จัสมิน ฟู้ด แอนด์ มาร์เก็ตติ้ง จำกัด"
- ที่อยู่: 87/283 หมู่บ้านเศรษฐสิริ วงแหวน-ลำลูกกา ลำลูกกา ปทุมธานี 12150
- Tax ID: 0135561004998
- รูปแบบ: ใบกำกับภาษี/ใบส่งสินค้า Tax Invoice/Delivery Order, สาย 6/3
- รหัสสินค้า: ตัวอักษร + 4 หลัก เช่น B0015, M0022, Q0002, N0013

### S021 — ซีเคฟู้ดส์ (บริษัท ซีเค-ฟูดส์ เอ็นเตอร์ไพรส์ จำกัด)
- หัวบิล: "บริษัท ซีเค-ฟูดส์ เอ็นเตอร์ไพรส์ จำกัด"
- ที่อยู่: 114/7 หมู่ที่ 1 ตำบลตะเคียนเตี้ย อำเภอบางละมุง ชลบุรี 20150
- Tax ID: 0205559038685
- รูปแบบ: ใบส่งของ/ใบแจ้งหนี้ รหัสสินค้า FG-CK-XXX (เช่น FG-CK-037)

### S022 — ทรัพย์ไพศาล มาร์เก็ตติ้ง
- หัวบิล: "บริษัท ทรัพย์ไพศาล มาร์เก็ตติ้ง จำกัด"
- ที่อยู่: 55/68 หมู่ที่ 3 ลำลูกกา ปทุมธานี 12150
- Tax ID: 0135565022290
- รหัสลูกค้า (Samurai): S040

### S023 — C&J
- หัวบิล: โลโก้ "C&J" ตัวหนา มุมบนซ้าย
- รูปแบบ: ต้นฉบับใบส่งของ/ใบเสร็จรับเงิน พนง.ขาย: สถาพร (เก่ง)
- สินค้าหลัก: ไม้เสียบ, น้ำจิ้ม

### S024 — เล็กโคราช
- รูปแบบ: บิลเงินสด (CASH SALE) สำเร็จรูป พื้นฟ้า มีอักษรจีน 現兌單
- ไม่มีชื่อบริษัทพิมพ์ไว้ — เขียนมือทั้งหมด
- สินค้าหลัก: อีสาน 17 ไม้, อีสานสด

### S025 — ตานะ
- รูปแบบ: ใบเก็บเงินเขียนมือ กระดาษขาว/เหลือง ตาราง ~70 ช่อง แบ่งสองคอลัมน์
- หัวบิล: "ใบเก็บเงิน" เขียนมือ มีช่อง "ร้าน" และ "วันที่"
- ไม่มีชื่อซัพพลายเออร์บนบิล
- สินค้าหลัก: เต้าหู้ไข พบ., เส้น, วุ้นเส้น, ยอ, เอ็นเนื้อ, จ้อ หลากหลายชนิด
